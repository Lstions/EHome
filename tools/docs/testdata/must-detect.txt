@@ tools/docs/testdata/must-detect.txt
@@ 固定语料：**已修正声称的原文**（从 git 历史取出，不依赖仓库当前内容）。
@@ 每一条都必须在 claims-audit.sh 的输出里被报出；带 @@ 的行是分隔/注释，不参与断言。
@@ 来源 A：G-1 —— backend/simulation/catalog/notify.go 修正前的头注释（git show 31125d31:<path>）
// 产品缺口（设计 §7.1，本域无法验证，不为不存在的能力写断言）：
// 全仓没有任何外发通道 —— Notification 只落库 + WS 广播，无 webhook / 机器人 / 邮件 / 短信。
// 因此"用户人不在系统前面时能不能收到告警"这一自动化最核心的价值**当前不可验证**。
@@ 来源 B：G-4 —— docs/设计/自动化引擎场景仿真验证.md:174 修正前抄的同一句（git show a5ac586f:<path>）
> **关于「发送 QQ / 微信机器人消息」**：经全仓核实，**当前不存在任何外发通道** ——
> `models.Notification` 只落库 + WS 广播，没有 webhook / 机器人 / 邮件 / 短信。
@@ 上下文行（非独立声称：它是判断，不是可证伪断言；但它随上一行一起失效，故一并冻结在语料里）
> 这是一个**产品缺口**，设计方案见 §7.1。
@@ 来源 C：G-3 —— docs/设计/通知中心.md 修正前，长期未回写外发通道（本行是 P1-A 文档里的原话）
> 因此长期声称"全仓没有任何外发通道"，直到 2026-09-15 才被纠正。
@@ 来源 D：F 系列 —— docs/分析/UIUX审计合并结论与优化方案-2026-09-13.md 的陈旧计数（§0 实测已失效）
| **F5 危险确认不规范** | `ElMessageBox.confirm` **20 处仅 1 处用 danger**；且**默认焦点落在"删除"**（诱导性） | 9 个文件 | §3.4.3 | **P1** |
| **F16 主题 token 死代码** | 侧栏 `MainLayout.vue:491` 硬编码 `#1a1f2e` 渐变 → `theme.css:53` 的 `--sidebar-bg-gradient` **零消费点** | D4 | §3.6.1 | P2 |
- `feedback.confirmDanger` 全仓**仅 1 处调用**（`views/layout/MainLayout.vue:438`，退出登录）。
- 追查发现整个 `dataSourceApi`（113 行 / 10 方法 / 4 interface）**除自身测试外零消费者**
| 前端 api 客户端 175 行**零消费** | `api/vendor.ts` | 三个 API 对象无任何调用点 |
@@ 来源 E：P0-A —— 声称"有守护测试"而全仓没有（同一族：可证伪的绝对化声称）
且注释声称的守护测试 `TestRedactTargetURLMatchesNotify` **全仓不存在** —— 比缺测试更危险。
@@ 来源 F：规则覆盖度补充（每条至少钉一个规则，防止某条规则被悄悄改坏后无人发现）
本域尚无清理器，该能力未实现，相关文档待更新。
经核查，整个项目里仅 3 处消费方，且找不到任何调用点。
该模块只有两处引用点，且全仓没有统一的清理器入口。
回看调用链，全仓仅一处调用点，没有任何兜底。
