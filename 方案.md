# 边缘设备数据生命周期管理方案（v3.3 定稿版）

> v1 → v2：吸收两路子任务审核 23 项发现逐条闭环。
> v2 → v3：复审双路 APPROVED_WITH_COMMENTS，闭环 9 项新发现。
> v3 终审：双路 APPROVED_WITH_COMMENTS（均明确"无需 v4"），7 条非阻塞意见已补入定稿。
> v3.1：吸收千问评估 3 项中等澄清 + 4 项低级建议 + 过度设计自评 3 项裁剪。
> v3.2：吸收千问复审 2 High + 1 Medium + 4 Low。
> v3.2 终审：前端/设计 APPROVED、后端 APPROVED_WITH_COMMENTS（2 Medium + 7 Low 条件性修订已补入）。
> v3.3：复审补 1 Medium + 2 Low（purge 竞态、合并目标 retention、followMergeChain 往返）。修订对照见附录 A。

## 背景与需求

删除边缘设备后，其数据（unified_data / device_data / calibration_cache）成为孤儿，无管理机制。

需求约束（用户已确认）：
1. 数据有业务价值，删除设备后**默认保留数据**
2. 历史数据**继承/合并是可选行为**，用户显式触发
3. 数据保存时间**可配置**，默认 1 年（365 天）
4. **跨节点**也能继承/合并；合并对象可能有多个、同时存在于不同节点，用户要能选择

## 〇、备选方案对比（为什么选逻辑身份）

| 方案 | 做法 | 优点 | 缺点 | 结论 |
|------|------|------|------|------|
| A. 数据迁移 | 重建设备时 `UPDATE unified_data SET device_id = 新id` | 零新表、零查询变更、前端零改动 | ① device_id 单值无法表达 **N:1 多源合并血缘**（合并 3 个源后无法追溯每条数据来自哪个实例）；② 跨节点移动后无法回答"这条数据是哪个节点采的"；③ 误合并不可恢复 | 否决 |
| B. 逻辑身份（本方案） | 数据挂 logical_device_id，实例可增删 | 多源血缘保留（每条数据仍有 device_id 原值 + logical_device_id 归属）；合并可追溯（merged_into 链）；删除/重建天然连续 | 新表 + 索引 + backfill + 查询解析层 | **采纳** |

关键：本方案**不改写** unified_data/device_data 的 device_id 原值（血缘），只新增 logical_device_id（归属）。合并不 UPDATE 存量数据的 device_id，只 UPDATE logical_device_id。

## 一、核心设计：逻辑设备身份（Logical Device Identity）

### 1.1 数据模型

```sql
CREATE TABLE logical_devices (
    id SERIAL PRIMARY KEY,
    identity_key VARCHAR(64) UNIQUE NOT NULL,  -- 创建后只读，不可编辑
    name VARCHAR(128) NOT NULL,                -- 用户可编辑
    device_type VARCHAR(32) NOT NULL,
    retention_days INTEGER DEFAULT 365,
    merged_into INTEGER REFERENCES logical_devices(id),
    merge_status VARCHAR(16) DEFAULT NULL,      -- NULL / pending / done
    purge_requested BOOLEAN DEFAULT FALSE,      -- 删除设备时勾选"同时删除数据"
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);

ALTER TABLE edge_devices ADD COLUMN logical_device_id INTEGER REFERENCES logical_devices(id);

ALTER TABLE unified_data ADD COLUMN logical_device_id INTEGER;
ALTER TABLE device_data   ADD COLUMN logical_device_id INTEGER;

-- 索引（CRITICAL，v1-H2 闭环）
CREATE INDEX CONCURRENTLY idx_unified_logical_ts ON unified_data(logical_device_id, timestamp DESC);
CREATE INDEX CONCURRENTLY idx_devdata_logical_ts ON device_data(logical_device_id, timestamp DESC);
-- 原 device_id / timestamp 单列索引保留：写入路径 consumers_heavy 仍按 device.ID 双写两列，
-- 按实例查最新（latest-data）等场景继续走 device_id 索引
```

- `identity_key` 生成规则：`type:hardware_id`（如 `bms_jbd:0x76`），hardware_id 为空时 `type:{uuid}`。**创建后只读**（v1-D1 闭环），仅 name 可编辑。
- `logical_device_id` 列**不带 GORM 外键级联**，删除 logical_device 只准在数据清理完成后由清理任务执行。

### 1.2 大表不引入 GORM 软删除（v1-H4 闭环）

unified_data / device_data 是纯追加时序表，**不加 DeletedAt 字段**。原因：
- 项目内多处 Raw SQL（handler_data.go:106-120、handler_overview.go:69-73）绕过 GORM scope，软删条件不会自动生效；
- 软删会让现有 device_id/timestamp 索引膨胀、查询退化；
- retention 清理本来就是 `DELETE WHERE timestamp < cutoff` 硬删，不需要软删语义。

所有"删除数据"场景（retention 到期、删除设备勾选"同时删除"、合并后源清理）统一走**后台异步分批硬删**（见 §四）。

### 1.3 候选匹配（创建时系统建议 + 用户确认）

候选查询必须 `Unscoped()`（v1-H1 闭环）：要列出**已软删除**的 edge_devices 实例及其 logical_device。

```sql
-- 伪 SQL：按 logical_device 聚合候选，含已删除实例
-- v3.2-F7 闭环：last_data_at 需走 dataScopeCondition 同构条件
-- （backfill 前旧行 logical_device_id 全 NULL，LEFT JOIN 仅按 logical_device_id
--  会在过渡期失真；实现时按实例集 OR 条件取 MAX，与 §4.3/§六 同构）
SELECT ld.id, ld.name, ld.device_type, ld.retention_days,
       COUNT(DISTINCT ed.id) AS instance_count,
       MAX(ud.timestamp) AS last_data_at
FROM logical_devices ld
JOIN edge_devices ed ON ed.logical_device_id = ld.id   -- Unscoped，含 deleted_at
LEFT JOIN unified_data ud ON ud.logical_device_id = ld.id
                          OR (ud.logical_device_id IS NULL AND ud.device_id = ed.id)
WHERE ld.merged_into IS NULL AND ld.device_type = :type
GROUP BY ld.id;
-- 注意：instance_count 用 COUNT(DISTINCT ed.id)（v3.2-终审 B7 闭环——
-- LEFT JOIN ud 会把每实例放大为其数据行数，裸 COUNT(ed.id) = 实例数×平均行数）
```

匹配权重（排序依据，全部列出由用户选，不做自动继承）：

| 匹配维度 | 权重 |
|---------|------|
| 同 node_id + 同 channel_id + 同 hardware_id + 同 type | 100 |
| 同 node_id + 同 type + 同 hardware_id（不同 channel） | 80 |
| 同 node_id + 同 type | 60 |
| 不同 node_id + 同 type + 相同 hardware_id | 40 |
| 不同 node_id + 同 type | 20 |

**数据量统计用估算而非 COUNT**（v1-B3 闭环）：PostgreSQL 用 `pg_class.reltuples`，SQLite 用截断 COUNT；candidates 接口超时 3s 降级为不显示数据量。逻辑设备数量级为几十到几百（单用户系统），该量级下估算+降级足够，**不引入额外统计缓存表**（v3.1 裁剪：logical_device_stats 缓存表删除，属过度设计——为未验证的性能问题提前建设双缓存层）。

**candidates 返回字段（v3.1-Q6 闭环）**：`id / name / device_type / retention_days / instance_count / last_data_at / row_estimate`——`retention_days` 必须显式返回，删除弹窗"保留 N 天"文案依赖它。

## 二、删除流程（软删除 + 数据默认保留）

### 2.1 删除确认弹窗（单删，P0）

弹窗改为 **ElDialog 组件**（v1-B1 闭环：ElMessageBox 不支持异步内容）：
- 打开即显示基本信息（设备名/节点/通道，本地已有数据，无延迟）
- 逻辑设备信息区（实例数/数据量估算）异步加载，loading 骨架，失败降级为不显示
- 数据处置用 **radio 单选**（v1-B4 闭环）：
  - ○ 保留历史数据（默认，保留 N 天，N=该逻辑设备 retention_days）
  - ○ 同时删除历史数据（红色危险文案："将在后台删除，不可恢复"）

### 2.2 批量删除（v1-A2 闭环）

批量删除沿用逐条 API，弹窗改为**汇总视图**：
- "将删除 N 台设备，其中 M 台属于逻辑设备（数据默认保留）"
- 统一一个 radio：全部保留数据（默认）/ 全部删除数据
- 不逐台展示逻辑设备实例数（成本不可控），详情引导到单删流程

### 2.3 删除后端逻辑（事务内，轻量）

1. 保持现有 GORM 软删除（v1-H1 闭环：删除动作本身不变，措辞修正）
2. 若 `logical_device_id IS NULL`：创建 logical_device（identity_key 按 §1.1 规则），回写 edge_device.logical_device_id。**在删除事务内完成**，保证任何被删设备都有逻辑身份锚点
3. 若勾选"同时删除数据"：`logical_devices.purge_requested = TRUE`（只置标记，**不在 API 事务里删数据**，由后台任务分批硬删，见 §四）
4. ConfigTemplate 处理见 §2.4
5. EmitConfigChange（现有逻辑不变）

### 2.3.1 P0 同步补建（v3-N1/N-6 闭环：消除过渡期查询漏洞）

v2 的"P4 backfill 统一回填"与"P1 双写"之间存在窗口期：存活旧设备在 P1 上线后、P4 完成前的新写入行 `logical_device_id=NULL`，查询协议 resolve 返回 logicalID 后这些行会漏。

v3 修正：**P0 阶段后端启动时对全量 edge_devices（含软删，Unscoped）做一次幂等同步补建**——凡 `logical_device_id IS NULL` 的实例立即创建 logical_device 并回写。该操作只写 logical_devices 小表和 edge_devices 单行，成本低，随 P0 一同上线；P4 只负责 unified_data/device_data 大表回填。由此 P1 双写上线时不存在 logical_id=NULL 的存活实例，过渡期查询漏洞消除。

**多副本竞态防护（v3.1-Q3 闭环，v3.2-F2 修正语义）**：docker-compose 多副本同时启动时，补建并发创建同一 identity_key 会撞 UNIQUE 约束。ON CONFLICT 只作**并发防护**，不作**语义决策**——三条路径分别处理：

| 路径 | identity_key 撞车行为 | 理由 |
|------|----------------------|------|
| P0 全量补建（本节） | 先查既有 logical_device 的存活实例数：**无存活实例才复用**（ON CONFLICT 回读）；有存活实例则 key 追加序号重试（`type:hardware_id#2`） | 存量可能合法存在同 type+hardware_id 不同 channel 的两个存活实例（唯一性约束是 (channel_id,hardware_id,type)），复用会把两个存活实例挂进同一 logical，击穿 §3.3-3 不变量 |
| 创建向导"作为新设备创建"（§3.3-4） | **永不复用**，直接序号 key | 用户显式选择新身份，静默复用=违背显式语义且继承旧数据 |
| 删除时补建（§2.3-2） | **可以复用**（ON CONFLICT 回读），但**复用前若回读到的 logical_device merged_into 非空，先跟随链挂到最终目标**（v3.2-终审 B5 闭环——防挂到链中段导致该实例的历史行 stranded 在已 done 的源 scope）；**purge_requested = TRUE 时禁止复用**（退回序号 key，v3.3-N1 闭环——防新删实例的数据被挂进待 purge 的逻辑设备而遭池鱼之殃） | 被删实例并入同地址既有身份，数据连续性正确；被删实例非存活，无冲突 |

### 2.4 ConfigTemplate 解绑（v1-M2 闭环：先建映射再谈解绑）

现状：ConfigTemplate 无设备归属列，channel.template_ids 是共享池，**无法判定模板属于哪个设备**。

方案：
1. `config_templates` 新增 `edge_device_id INTEGER`（可空）。创建路径 createSingleTemplate（handler_edge_device.go:66-87）写入归属
2. 存量 backfill（P4）：按"该 channel 上该 type 设备的 driver CommandTemplates.WriteData 与模板 WriteData 匹配"尽力回填；匹配不上的留 NULL
3. 删除设备时：
   - 查 `config_templates WHERE edge_device_id = :id` 得候选模板 ID
   - **引用检查**：同 channel 是否存在其他存活设备同 type（multi-drop 场景）——若有，模板保留不删
   - 无其他引用 → 从 channel.template_ids 移除该 ID（字符串解析重建），删除 ConfigTemplate 行
   - 防止 ESP32 maxTemplates=16（sender.go:32）溢出
4. edge_device_id IS NULL 的旧模板：不在删除时动（保守，宁留勿删），由独立的"孤儿模板清理"任务处理（skill 已有记录此问题）

**已知限制（v3-N-4 闭环记录）**：multi-drop 场景（同 channel 同 type 多从机）下，存量模板的 WriteData 可能按 hardwareID 生成地址字节（deprecated getTemplateParamsFromDeviceConfig 路径）或多个设备模板 WriteData 相同（generic_modbus 不支持 per-device 定制），WriteData 匹配无法可靠区分归属 → 这些模板的 edge_device_id backfill 后恒为 NULL，删除时的模板清理对 multi-drop 实际不生效（引用检查保守保留）。ESP32 模板上限由 maxTemplates=16 发送侧兜底。接受此限制，不为此引入更复杂的归属推断。

### 2.5 calibration_cache 处理（v1-M1 闭环）

校准数据是**物理个体**的工厂校准值（consumers_heavy.go:195-209 按 edge_device_id 精确消费），**跨实例继承校准在业务上错误**。

- 删除设备时：calibration_cache 行**保留不删**（unique 约束按实例 ID，不冲突，无害）
- 创建设备时：**仅"原位置重建"场景**（同 channel_id + 同 hardware_id + 同 type，权重 100 档）复制 calibration 行到新 edge_device_id；其余场景不复制，新设备走正常初始化校准流程
- 清理任务：calibration_cache 孤儿行（edge_device_id 对应的设备已软删且 logical_device 数据已清除）纳入 §四 清理范围，防无限累积

## 三、创建流程（继承/合并，可选）

### 3.1 入口覆盖（v1-A1 闭环）

创建入口有两个，**都必须覆盖**：
1. EdgeDeviceList 3 步向导：新增步骤 0"历史数据继承"
2. QuickCreateDeviceDialog（节点详情页）：表单内新增可折叠区"继承历史数据（可选）"，默认折叠；展开后复用同一候选列表组件

候选列表组件 `LogicalDeviceCandidateSelect.vue` 两处复用。

### 3.2 选项收敛（v1-B2 闭环）

只给两个选项，消灭语义重叠：
- ○ **作为新设备创建**（默认）：系统自动新建 logical_device（与删除流程 §2.3-2 对称，任何设备都有逻辑身份）
- ○ **继承历史数据**：从候选列表选择一个 logical_device（单选）

合并（多源）不在创建向导做，见 §3.4。

### 3.3 创建后端逻辑

```go
type CreateEdgeDeviceRequest struct {
    // ... 现有字段
    LogicalDeviceID *uint `json:"logical_device_id,omitempty"` // 继承目标（单选）
}
```

事务内（全部轻量，无大表操作）：
1. 现有 checkDeviceUniqueness 不变
2. LogicalDeviceID 指定：校验存在 + device_type 匹配 + merged_into IS NULL + **purge_requested = FALSE**（v3.3-N1 闭环——目标数据已标记删除时拒绝继承，409"该逻辑设备的数据已标记删除，无法继承"）→ 关联
3. **存活实例唯一性校验（v3-N1 闭环，HIGH）**：同一 logical_device 同时只允许一个存活实例。创建继承时在事务内检查：
   ```go
   var aliveCount int64
   tx.Model(&models.EdgeDevice{}).
       Where("logical_device_id = ?", targetLogicalID).
       Count(&aliveCount) // scoped 查询，只计未软删实例
   if aliveCount > 0 {
       return 409 "目标逻辑设备「<name>」已有关联的存活实例（实例 ID X，节点 Y）。请先删除旧实例（历史数据会保留并自动并入），再重新创建；如需保留两个独立身份，请选择「作为新设备创建」。"
   }
   ```
   理由：双存活实例同 type 同时上报时，按 logical_device_id 查询返回两物理设备混合数据，AVG 聚合无物理意义（两块电池的电压平均不是任何一块的状态）。继承语义 = "旧实例已退役、新实例接管"，旧实例必须先删除（软删保留数据，历史自然并入）。
   **交互顺序说明（终审 C-1 闭环）**：跨节点继承的正确顺序是"先删旧实例（节点 A）→ 再建新实例（节点 B）"。409 文案携带旧实例定位信息（实例 ID + 节点）引导用户完成该顺序；不提供"先在 B 建再删 A"的路径（双存活窗口内数据混合无意义）。文案不引导"去管理页合并"——合并是 logical_device 维度操作，无法解决同一 logical 下双存活实例。
4. 未指定 LogicalDeviceID：新建 logical_device（identity_key 按规则生成；**"作为新设备创建"路径永不复用既有 key，撞车追加序号**，见 §2.3.1 路径表——v3.2-F2 闭环）
5. 创建 edge_device
6. 原位置重建场景：复制 calibration_cache（§2.5）

### 3.4 存量合并（v1-A3 闭环：补 UI 入口）

合并是**独立于创建**的操作，入口在新增"逻辑设备管理"页：
- `GET /logical-devices` 列表页：名称/类型/实例数（含已删除）/数据量估算/最后数据时间/保留天数
- 多选 2+ 个同 device_type 的逻辑设备 → "合并"按钮
- **合并预览**（v1-D2 闭环）：时间范围对比、重叠区提示、数据量合计，二次确认后提交
- 合并**不可简化为可逆**：源标记 merged_into + merge_status=pending，数据搬迁由后台任务执行（§四），完成后 merge_status=done
- 合并后图表重复点（v1-C3 闭环，v3.1-Q1 修正）：同一 timestamp 多点时，查询层**保形去重**（每 (timestamp,sensor_name) 取 MAX(id) 行，非 AVG），仅对有合并历史的逻辑设备条件启用，见 §六；device_id 原值保留，需要血缘分析时可按 device_id 拆分

合并 API：
```
POST /logical-devices/merge
{ "target_name": "客厅BMS", "source_ids": [3, 5, 8] }
→ 创建新 logical_device（或指定 target_id 复用其一），源标记 merged_into，返回 job_id
```
新建目标的 identity_key 规则：`merge:{uuid}`（v3.2-终审 B6 闭环——identity_key 是 UNIQUE NOT NULL，merge 目标与三路径规则的 key 空间隔离，永不撞车）。
新建目标的 retention_days 来源（v3.3-N2 闭环）：按 §4.1 快照语义取创建时系统级 EHOME_DATA_RETENTION_DAYS 当前值；若各源剩余保留期差异大，合并预览响应返回 `target_retention_days` 字段供用户预览确认（见下），合并后亦可在管理页编辑。

409 冲突响应结构（前端 D-1 闭环）：多源合并的 409 响应体带结构化冲突列表，前端逐条呈现并提供实例跳转：
```json
{ "code": 409, "message": "...",
  "conflicts": [{ "logical_device_id": 3, "logical_name": "客厅BMS",
                  "instance_id": 17, "instance_name": "BMS-1", "node_name": "客厅采集器" }] }
```

**并发控制（v3-N-5 闭环）**：合并 API **全部源校验与占位、目标创建在同一事务内**执行（终审 C-2 闭环），任一步失败整体回滚：
```go
// 单事务内：
// 0. v3.2-F1 闭环：逐源校验无存活实例（与 §3.3-3 同一不变量）；
//    v3.3-N1 补强：同时校验源 purge_requested = FALSE（已标记删除数据的源
//    不得进入合并——搬迁任务与 purge 任务对同一份数据一个 UPDATE 一个 DELETE，
//    先后顺序未定义）。违反则并入 409 conflicts 列表
var aliveCount int64
tx.Model(&models.EdgeDevice{}).Where("logical_device_id = ?", srcID).Count(&aliveCount)
if aliveCount > 0 {
    return 409 "逻辑设备 X 仍有存活实例（实例 ID N，节点 Y）。请先删除该实例（历史数据保留），再发起合并。"
}
// 1. 先建目标 logical_device，再逐源乐观占位
res := tx.Model(&models.LogicalDevice{}).
    Where("id = ? AND merged_into IS NULL AND (merge_status IS NULL OR merge_status = '')", srcID).
    Updates(map[string]interface{}{"merged_into": targetID, "merge_status": "pending"})
if res.RowsAffected == 0 {
    return 409 "逻辑设备 X 已在其他合并中或已合并" // tx 回滚，已占位源自动撤销
}
```
受影响行数=0 即拒绝，杜绝同一源被两个 merge 并发引用。后台搬迁任务只处理 merge_status='pending' 的源，完成后置 'done'。

**带存活实例的源为何必须拒绝（v3.2-F1 闭环，HIGH）**：源若有存活实例，合并后该实例仍按 §八 双写逻辑把新数据写到源 id（consumers_heavy 从 device.LogicalDeviceID 取值，不感知 merged_into），而旧数据已搬迁到目标——经该实例查询时旧数据在目标、新数据在源，两头各缺一半，与 §3.3-3 要防的双存活混合是同一类危害。

**failed 处理（v3.1 简化 + v3.2-F3 补强）**：状态机 `NULL/pending/done`（五态 + 手动重置 UI 属过度设计，维持裁剪）。但两个元素不裁：
1. **失败 Notification**：搬迁任务单源失败时产生一条 Notification（复用 models.Notification，一行代码）——失败可见性是运维底线不是装饰；
2. **水位重试**：失败后任务按 merge_jobs.migrated_rows 水位断点续跑重试（§4.3 已有水位机制；同目标重试天然幂等——已搬到目标的行不再属于源 scope，UPDATE 不会重复命中）。重试超限（3 次）才将源 `merged_into=NULL, merge_status=NULL` 复位并通知。

**合并预览端点（v3-N4 闭环）**：
```
POST /logical-devices/merge/preview
{ "source_ids": [3, 5, 8] }
→ { "sources": [{ "id": 3, "name": "...", "first_data_at": ..., "last_data_at": ...,
                   "row_estimate": 12847, "overlap_with_others": true }],
    "target_retention_days": 365 }   // v3.3-N2：新建目标将采用的 retention_days（系统级快照）
```
first_data_at/last_data_at 用 MIN/MAX(timestamp)（logical_device_id 有索引，可走 idx_unified_logical_ts 索引范围扫描，非全表 COUNT）；row_estimate 用 reltuples 估算。前端据此渲染时间轴对比和重叠提示。

**碎小逻辑设备合并引导（v3-N2 闭环，P3 后迭代项，不阻塞）**：管理页对"同 device_type 且同 hardware_id 的多个 logical_device"显示"疑似重复"分组标识，点击直接发起合并预览。创建向导候选列表在用户选"作为新设备创建"时若有权重 ≥80 的候选，显示一行提示"检测到高度相似的历史设备，建议考虑继承"。

## 四、数据保留与清理（统一后台任务）

### 4.1 保留策略层级

| 层级 | 配置项 | 默认值 | 优先级 |
|------|--------|--------|--------|
| 系统级 | EHOME_DATA_RETENTION_DAYS | 365 | 低 |
| 逻辑设备级 | logical_devices.retention_days | 创建时快照系统级当前值 | 高 |

**快照语义（v3.1-Q4 闭环）**：logical_device 创建时将系统级 EHOME_DATA_RETENTION_DAYS 当前值快照写入 retention_days；之后修改系统级配置**不影响**已存在的 logical_device（避免系统默认值变更隐性波及存量数据的保留期），只影响新建 logical_device 的默认值。

### 4.2 到期通知（v1-E③ 闭环）

- 到期前 30 天、7 天各产生一条 Notification（复用 models.Notification 表 + 前端通知中心）
- 通知文案含"延长保留期"入口（跳逻辑设备管理页改 retention_days）
- **到期硬删后不可恢复**，文案明确告知；用户在窗口期内可改 retention_days 延期

### 4.3 清理任务（每日，分批硬删）

**清理范围与查询使用同一套 scope 解析（v3.1-Q2 闭环，v3.2-终审 B1 补强）**：清理任务处理某 logical_device 时，DELETE 条件必须与 §六 查询协议的过渡期兼容条件一致——在 P4 大表 backfill 完成前，`WHERE logical_device_id = ?` 会漏掉 logical_device_id 全 NULL 的旧行，导致 retention 形同虚设、purge_requested 删不干净。统一做法：先按 logical_device 解析其实例集（Unscoped），DELETE 条件为 `WHERE logical_device_id = ? OR (logical_device_id IS NULL AND device_id IN <实例集>)`；backfill 完成后该条件自然退化为仅前者（旧行已全部回填）。封装为共享 helper `dataScopeCondition(logicalID) (cond, args)`，查询与清理同用。

**pending 搬迁窗口（v3.2-终审 B1 闭环）**：merge_status='pending' 期间，未搬迁分片（仍属源 scope）对经 followMergeChain 解析到目标的查询不可见（大数据量搬迁可数小时，易被误判为数据丢失）。修复：dataScopeCondition(目标) 对入边 pending 源做 UNION——`scope(目标) UNION scope(源) WHERE 源.merged_into = 目标 AND 源.merge_status = 'pending'`。每行任一时刻只属一侧（pending 源的行不可能已带目标 logical_id），无重复行，保形去重逻辑不受波及；pending 结束（done/复位）后 UNION 项自然消失。

```
任务 1：retention 到期清理
  找 logical_device（merged_into 已完成的源 + 有活跃/已删实例但数据超期的）
  DELETE FROM unified_data WHERE <dataScopeCondition> AND timestamp < now() - retention_days
  分批：按 id 范围 chunk（每批 1 万行，独立事务），批间 sleep 防 WAL 膨胀
  PostgreSQL 依赖 autovacuum；文档注明大表建议后续做按 timestamp 分区（不在本期范围）

任务 2：purge_requested 清理（删除设备勾选"同时删除"）
  执行前守卫（v3.3-N1 闭环）：若该 logical_device 已有存活实例（用户删后重建并继承了该身份），
  放弃 purge 并清 purge_requested 标志——重建行为本身即撤回删除意图，此时删数据=静默清空用户
  刚选择继承的历史。同理若该 logical_device 已是某 pending 合并的目标/源，顺延至合并终态后再执行
  DELETE FROM unified_data/device_data WHERE <dataScopeCondition>
  同上分批；完成后清理 calibration_cache 孤儿行（§2.5）
  v3.2-F6 闭环（出边）：删除 logical_device 行前，先解除软删实例的 FK 引用——
  UPDATE edge_devices SET logical_device_id = NULL WHERE logical_device_id = ?（Unscoped，含软删实例）
  v3.2-终审 B4 闭环（入边）：若存在 merged_into 指向本 logical 的已完成源，
  先 UPDATE logical_devices SET merged_into = NULL, merge_status = NULL WHERE merged_into = ?（解除入边 FK）
  然后再 DELETE logical_device 行（§1.1 FK 无 ON DELETE SET NULL，显式解除更安全）

任务 3：合并数据搬迁（merge_status = pending）
  UPDATE unified_data/device_data SET logical_device_id = 目标 WHERE <dataScopeCondition(源)>
  按 id 范围 chunk 分批（每批独立事务），进度写 merge_jobs 表
  完成后 merge_status = done
  失败：产生 Notification + 按 migrated_rows 水位断点续跑重试（≤3 次，同目标重试幂等）；
        超限才 merge_jobs.status='failed' + 源 merged_into/merge_status 复位（对齐 §3.4，v3.2-终审 B3）
  —— v1-H3 闭环：搬迁全部移出 API 事务，创建/合并 API 只写标记行

锁交互说明（v3-N-3 闭环）：PostgreSQL 下分批 DELETE/UPDATE 拿行锁（无 gap lock），与 consumers_heavy 高频 INSERT 无死锁风险；SQLite（测试环境）写锁为库级 RESERVED→EXCLUSIVE，INSERT 与批次串行，分批反而降低单次锁占用——策略两边成立，SQLite 测试环境批次大小可缩小至 1 千行。

断点续跑（v3.1 千问建议采纳）：merge_jobs 记录 migrated_rows 水位，任务崩溃后重启从水继续（按 id > watermark 分批）；P4 大表 backfill 复用同一模式（backfill_jobs 表或复用 merge_jobs 加 job_type 字段区分）。
```

merge_jobs 表：id / source_logical_id / target_logical_id / status / migrated_rows / total_estimate / created_at / finished_at。

## 五、跨节点继承/合并

- 设备从节点 A 移到节点 B：创建时选择继承，新实例挂同一 logical_device_id；查询协议（§六）自动带出全部历史
- 配置不继承（v1-风险表闭环）：新实例的 channel/template/bus_config 全部重新生成，只继承数据归属
- 多源合并：§3.4 流程，跨节点候选在权重表 40/20 档

## 六、查询协议（v1-C1/C2 闭环：对前端透明）

**核心决策：前端始终传 edge_device_id，后端解析 logical_device_id 后统一查询。**

**过渡期查询兼容（终审 C-3 闭环 + v3.1 裁剪简化）**：P1（双写）上线后、P4（大表 backfill）完成前，unified_data/device_data 旧行 logical_device_id 全 NULL，单纯 `WHERE logical_device_id = ?` 会漏历史数据。过渡期查询条件必须兼容：

```go
q = q.Where(dataScopeCondition(logicalID)) // 见 §4.3：logical_device_id = ? OR (logical_device_id IS NULL AND device_id IN <实例集>)
```

**backfill 完成后退化为简洁条件**——退化不依赖 backfill_done 标记位（v3.1 裁剪：原 backfill_done 列删除，属过度设计——用永久机制解决一次性窗口），而是**部署顺序强制保证**（见 §十）：P4 backfill 必须在 P1 双写激活前于生产完成，过渡期在生产环境实际不存在；dataScopeCondition 的 OR 分支仅作为 backfill 未完成时的安全网长期保留（成本极低，一次 IN 子查询），无需标记位驱动条件切换。

**合并去重（v3.1-Q1 闭环：保形去重，修复"零改动"矛盾）**：v3 原方案"GROUP BY timestamp, sensor_name 取 AVG"会改变行结构——sensor-data 无 sensor 过滤时多传感器混合返回，GROUP BY 会把不同 sensor_name 行塌缩，downsampleUnifiedData 的 max_points 语义失效，前端"零改动"承诺破产。v3.1 修正：
- 去重**仅当逻辑设备有已完成合并源（merged_into 链非空）时条件启用**；无合并历史的逻辑设备查询保持原样（绝大多数情况，前端零改动成立）
- 去重用**保形去重**：每 (timestamp, sensor_name) 保留一行（取 MAX(id) 最新行，而非 AVG——两个物理设备的电压平均无物理意义，且保留行结构与现有一致）。实现统一用窗口函数 `ROW_NUMBER() OVER (PARTITION BY sensor_name, timestamp ORDER BY id DESC) = 1`（PostgreSQL 与 SQLite 均支持，v3.2-F5 闭环：不写 DISTINCT ON，避免 handler_overview.go:71 那样的 PG-only 语法进入本链路）
- 适用端点：sensor-data / history / historical / historical-batch 全族（v3.1-Q7 提醒纳入）

```go
func resolveDataQueryScope(db *gorm.DB, edgeDeviceID uint) (logicalID uint, fallbackDeviceID uint) {
    var dev models.EdgeDevice
    if err := db.Unscoped().Select("id", "logical_device_id").First(&dev, edgeDeviceID).Error; err != nil {
        return 0, edgeDeviceID // 实例不存在（理论上不该发生）
    }
    if dev.LogicalDeviceID != nil && *dev.LogicalDeviceID > 0 {
        // v3.2-F1 闭环：防御性跟随 merged_into 链至最终目标（上限 8 跳防环）。
        // 正常路径下 §3.4 的源存活校验已保证不遇 merged 源；此跟随是纵深防御——
        // 即使校验被绕过（如直接 SQL 操作），经旧实例查询也不会撕裂
        return followMergeChain(db, *dev.LogicalDeviceID), 0
    }
    return 0, edgeDeviceID // 兼容 backfill 前旧数据
}

// followMergeChain 沿 merged_into 链走到最终目标 logical_device
// 实现指引（v3.3-N3 闭环）：勿逐跳查询（最坏 8 次 DB 往返/每次数据请求）。
// 逻辑设备量级为几十到几百（§1.3 已论证），一次性
// SELECT id, merged_into FROM logical_devices 载入内存走链，
// 把 N 次往返压成 1 次；该小表查询可与 resolve 的实例查询合并同一往返。
func followMergeChain(db *gorm.DB, logicalID uint) uint {
    for i := 0; i < 8; i++ {
        var mergedInto *uint
        db.Model(&models.LogicalDevice{}).Select("merged_into").Where("id = ?", logicalID).Scan(&mergedInto)
        if mergedInto == nil || *mergedInto == 0 {
            return logicalID
        }
        logicalID = *mergedInto
    }
    return logicalID
}

// 查询：
if logicalID > 0 {
    q = q.Where(dataScopeCondition(logicalID))
    // 去重启用判定（v3.2-终审 B8 闭环）：对解析后的目标做反向存在性检查——
    // EXISTS(SELECT 1 FROM logical_devices WHERE merged_into = ? AND merge_status = 'done')
    // 有已完成入边合并源才启用保形去重；无合并历史恒走原样查询（前端零改动）
    // 该判定与 scope 解析同往返实现，不增加额外 DB 往返
} else {
    q = q.Where("device_id = ?", fallbackDeviceID)
}
```

**后端改造清单**（v1-M3/M-4 闭环，全量）：

| 位置 | 现状 | 改造 |
|------|------|------|
| handler_data.go:40-70 /devices/:id/sensor-data | device_id | resolveDataQueryScope |
| handler_data.go:106-120 /nodes/:id/latest（Raw SQL） | JOIN edge_devices | **不改**（v3-N-2 闭环）；但修正描述（v3.2-终审 B9）：现 SQL 子查询无 `deleted_at IS NULL` 过滤，软删设备数据实际仍会出现——这是现状行为，"不改"决策维持，描述对齐事实；如后续要精确"只存活"，需补过滤（与本方案无关，不纳入范围） |
| handler_data.go:169 /devices/:id/history | device_id | resolveDataQueryScope |
| handler_data.go:201 /unified-data/categories | device_id | resolveDataQueryScope |
| handler_data.go:245 /unified-data/historical | device_id | resolveDataQueryScope |
| handler_data.go:315 批量趋势 | device_id | resolveDataQueryScope |
| /unified-data/aggregated、/unified-data/latest（v3-N6 闭环补入；终审 C-2 修正） | — | **端点当前后端不存在**（全仓 grep 零命中，前端 unifiedDataApi 这两个方法是死代码）。不纳入改造清单；如未来新建，必须走 resolveDataQueryScope，去重与 §六 保形去重一致（子查询按 (timestamp,sensor_name) 去重后再按 hour/day/month 桶聚合） |
| handler_edge_device.go:741 latest-data | device_id 直查 device_data | 实例已删 → 404（按实例语义）；存活 → resolve 后查 |
| handler_edge_device.go:753 /:id/data | 同上 | 同上 |
| handler_overview.go:69-73（Raw SQL） | device_id IN 存活实例 | 不变（只统计存活设备，符合总览语义） |
| handler_node.go:220-221 按 node_id 查 device_data | node_id | 不变（node 维度语义） |
| handler_data_report.go:21 /data-reports | 全表 | 不变（管理视图） |
| handler_metrics.go:104 数据点统计 | COUNT | 不变（含保留数据，符合存储语义） |

**前端改动清单**（v1-C1 闭环）：

| 文件 | 改动 |
|------|------|
| api/edgeDevice.ts:181,198 | create 增加可选 logical_device_id；delete 增加可选 delete_data；**candidates 方法也放这里**（端点是 GET /edge-devices/candidates，命名空间一致，终审 C-3 闭环） |
| api/logicalDevice.ts（新增） | list / merge / mergePreview / mergeJob 进度 / update（name、retention_days） |
| EdgeDeviceList.vue:965-1028 | 单删弹窗改 ElDialog 组件（保留/删除 radio + 异步逻辑设备信息）；批删弹窗改汇总视图 |
| components/device/LogicalDeviceCandidateSelect.vue（新增） | 候选列表组件，向导步骤 0 + QuickCreate 复用 |
| QuickCreateDeviceDialog.vue | 折叠区接入候选组件 |
| views/logical-device/LogicalDeviceList.vue（新增） | 逻辑设备管理页（列表 + 合并 + retention 编辑）；合并 409 冲突弹窗按结构化 conflicts 列表逐条呈现 + "查看实例"跳转（§3.4，D-1 闭环） |
| MainLayout.vue 通知跳转 | 按 Notification.source 字段扩展路由（merge 失败 → /logical-devices；retention 到期 → 逻辑设备管理页 retention 编辑）——替代现有 title.includes('离线') 字符串匹配模式（D-2 闭环） |
| 数据查询页（DataPanel/HistoryChartSection/详情页） | **零改动**——查询协议对前端透明，仍传 deviceId |

## 七、现有数据迁移（M 迁移步骤 + P4）

**注意（v3.2-终审 B2 闭环）**：原 §七-1"每个现有 edge_device 创建 logical_device（identity_key=`legacy:{edge_device_id}`）"是**死步骤**——v3.2 后 logical 创建已全部前移到 P0（§2.3.1，key 规则 `type:hardware_id`），P0 上线后不存在 logical_id IS NULL 的实例。本节仅余大表与模板回填。

1. ~~每个现有 edge_device 创建 logical_device~~（已由 §2.3.1 P0 完成，含 ON CONFLICT 三路径分流）
2. `UPDATE unified_data SET logical_device_id = ... WHERE device_id = ...` 分批回填（M 步骤执行，可断点续跑）；device_data 同理
3. ConfigTemplate.edge_device_id 按 §2.4-2 尽力回填（P4）
4. 索引 CONCURRENTLY 创建（§1.1，M 步骤）
5. `unified_data.edge_device_id` / `device_data.edge_device_id` 冗余列（models.go:201/:215，写入路径从未填充，v1-L1 闭环）：backfill 不动它们，标记 deprecated，后续版本删列

## 八、写入路径变更

consumers_heavy.go SensorParserConsumer（:248-307）：
- 查得 device 后（现有 :139-158 逻辑不变，scoped 查询——**软删设备的数据静默丢弃，这是期望语义**，固件配置同步有延迟期间的数据不入库，v1-H1 闭环写入风险表）
- 写 unified_data / device_data 时**双写 device_id（血缘）+ logical_device_id（归属）**，logical_device_id 从 device.LogicalDeviceID 取，**并同样经 followMergeChain 解析到最终目标**（v3.2-F1 闭环：写入与查询同链，即使合并任务进行中实例仍在写入，数据也直接落目标，不产生"目标缺新数据"窗口）

## 九、API 变更汇总

| 端点 | 变更 |
|------|------|
| POST /edge-devices | +logical_device_id 可选（继承时校验目标无存活实例，冲突 409） |
| DELETE /edge-devices/:id | +delete_data 可选（默认 false，置 purge_requested 标记异步删） |
| GET /edge-devices/candidates?type=&node_id=&hardware_id= | 新：候选逻辑设备（Unscoped，数据量估算，3s 超时降级） |
| GET /logical-devices | 新：管理列表 |
| POST /logical-devices/merge | 新：合并（乐观占位防并发，返回 job_id） |
| POST /logical-devices/merge/preview | 新：合并预览（first/last_data_at + row_estimate + 重叠标记） |
| GET /logical-devices/merge-jobs/:id | 新：合并进度 |
| PUT /logical-devices/:id | 新：改 name / retention_days |

## 十、实现优先级（重估，v1-工作量闭环）

| 阶段 | 内容 | 工作量 |
|------|------|--------|
| P0 | 删除弹窗增强（ElDialog + radio + 数据保留标记）+ 后端 logical_device 全量同步补建（§2.3.1）+ purge 后台任务 | 后端 2 天 + 前端 1 天 |
| **M（迁移步骤，P0 后 P1 前执行，v3.2-F4 闭环）** | 大表 backfill 脚本（unified_data/device_data 回填 logical_device_id，可断点续跑，复用 merge_jobs 水位模式）+ 索引 CONCURRENTLY 创建 | 后端 1 天（脚本开发；执行墙钟随数据量，不阻塞开发） |
| P1 | logical_devices 表 + 写入双写（followMergeChain）+ 查询协议 resolveDataQueryScope | 后端 2 天 |
| P2 | 创建继承（两入口候选组件 + 存活实例唯一性校验 + 409 冲突引导）+ candidates API | 后端 1 天 + 前端 2 天 |
| P3 | 逻辑设备管理页 + 合并 UI（预览弹窗 + 进度轮询）+ merge_jobs 后台搬迁 + 乐观并发控制 | 后端 1.5 天 + 前端 2.5 天 |
| P4 | ConfigTemplate.edge_device_id 回填 + calibration 孤儿清理 + retention 到期通知（大表 backfill 已上移至 M 步骤） | 后端 1 天 + 前端 0.5 天 |
| 合计 | | **后端 ~7.5 天 + 前端 ~6 天**（v3.1 裁剪 merge 状态机/手动重置 UI 后 -0.5 天；P4 backfill 若生产数据量大幅增，断点续跑机制控制墙钟风险） |

**部署顺序约束（终审 C-1 闭环，v3.1 强化，v3.2-F4 对齐）**：发布顺序 = **P0 上线（确认补建日志完成）→ M 迁移步骤（backfill 脚本，可断点续跑）→ P1 上线（激活双写）**。由此生产环境不存在"旧行 logical_id=NULL 且双写已激活"的窗口期；M 完成到 P1 激活之间新写入的行仍为 NULL，由 §六 常驻的 dataScopeCondition OR 安全网兜住。P2-P4 顺序自由。

## 十一、风险与缓解

| 风险 | 缓解 |
|------|------|
| 误合并不同设备 | 合并预览（时间重叠对比）+ 二次确认；merged_into 血缘保留可审计；搬迁是异步的，done 前可人工干预 |
| 跨节点继承后配置冲突 | 只继承数据归属，channel/template/bus_config 全新生成 |
| 大批量 UPDATE/DELETE 影响在线写入 | 全部后台分批（1 万行/批独立事务 + 批间 sleep + 进度表） |
| candidates 查询慢 | reltuples 估算 + 3s 超时降级 |
| 软删设备数据静默丢弃语义被重构破坏 | 写入风险表登记（consumers_heavy.go:141 scoped 查询是期望行为） |
| 合并后同 timestamp 多点 | 有合并历史的逻辑设备启用保形去重（(timestamp,sensor_name) 取 MAX(id) 行）；device_id 原值保留可拆血缘 |

## 十二、影响面清单（删除保留数据后各端点行为定义）

| 场景 | 行为 |
|------|------|
| 按实例 ID 查数据（latest-data / :id/data） | 实例已删 → 404；实例存活 → 经 resolve 返回逻辑设备全量历史 |
| 图表/历史查询（sensor-data / history / historical） | 实例存活 → resolve 后返回全量（含继承前历史） |
| /nodes/:id/latest | 现状行为：软删设备数据仍会出现（子查询无 deleted_at 过滤，v3.2-终审 B9 已对齐描述）；与本方案无关，维持现状 |
| HomeAssistant / WS data_update | 只跟随存活实例（软删后无新事件） |
| 命令执行历史（commandexec） | 按 edge_device_id 保留，不随删除清理；实例已删时详情页只读展示 |
| offlinedetector | 只跟踪存活实例（现有 scoped 查询天然正确） |

## 附录 A：审核发现闭环对照

### v1 发现（23 项，v2 已闭环）

| 发现 | 闭环位置 |
|------|---------|
| H-1 软删除语义/Unscoped | §1.3、§2.3-1、§八 |
| H-2 索引缺失 | §1.1（CONCURRENTLY 复合索引 DDL） |
| H-3 MergeFrom 事务矛盾 | §4.3 任务 3 |
| H-4 大表 GORM 软删除 | §1.2 |
| M-1 calibration 迁移语义 | §2.5 |
| M-2 template 解绑无映射 | §2.4 |
| M-3/M-4 查询/消费方遗漏 | §六、§十二 |
| L-1~L-3 | §7-5、§4.3、§六 |
| A1 QuickCreate 入口 | §3.1 |
| A2 批量删除 | §2.2 |
| A3 merge 无 UI | §3.4 |
| B1 弹窗异步 | §2.1 |
| B2 选项语义重叠 | §3.2 |
| B3 候选 COUNT 成本 | §1.3 |
| B4 勾选框互斥 | §2.1 |
| C1/C2 前端改动面/查询协议 | §六 |
| C3 合并重复点 | §3.4、§六 |
| D1 identity_key 可编辑 | §1.1 |
| D2 合并预览 | §3.4 |
| D3 备选方案论证 | §〇 |
| E③ 到期通知/恢复 | §4.2 |

### v2 复审新发现（9 项，v3 闭环）

| 发现 | 严重度 | 闭环位置 |
|------|--------|---------|
| 后端 N-1/N-6 过渡期查询漏洞（P0 只补被删设备） | Medium | §2.3.1（P0 全量同步补建） |
| 后端 N-4 multi-drop backfill 恒 NULL | Medium | §2.4 已知限制记录 |
| 后端 N-5 merge_jobs 缺并发控制 | Medium | §3.4（乐观占位 UPDATE 行数判定） |
| 后端 N-2 /nodes/:id/latest 改造矛盾 | Low | §六（改为不改，维持现 SQL） |
| 后端 N-3 SQLite 分批说明 | Low | §4.3（下方补充） |
| 前端 N1 双存活实例约束缺失 | **High** | §3.3-3（事务内 409 校验 + 前端引导） |
| 前端 N2 碎小逻辑设备无合并引导 | 中 | §3.4（管理页疑似重复分组 + 向导提示，P3 后迭代） |
| 前端 N4 合并预览缺时间范围端点 | 中 | §3.4（POST /merge/preview） |
| 前端 N5 工作量偏乐观 | 中 | §十（前端 5→6 天） |
| 前端 N6 改造清单漏 aggregated/latest | 低 | §六（补两行 + 聚合语义定义） |

### v3 终审意见（7 条，已补入定稿）

| 意见 | 来源 | 闭环位置 |
|------|------|---------|
| C-1 P0/P1 部署顺序未明写 | 后端终审 | §十 部署顺序约束段 |
| C-2 merge 多源占位同事务 + failed 分支 | 后端终审 | §3.4 并发控制段（单事务乐观占位；v3.1 简化状态机为 pending/done + 失败复位） |
| C-3 P1-P4 窗口期大表未回填旧行不可见 | 后端终审 | §六 过渡期查询兼容段（OR device_id IN 实例集安全网常驻；v3.1 移除 backfill_done 标记位，改部署顺序强制） |
| C-4 aggregated/latest 端点不存在 | 后端+前端终审 | §六 改造清单行修正（标注端点不存在，不纳入改造，未来新建须走 resolve） |
| C-1 409 文案"去管理页合并"引导错误 | 前端终审 | §3.3-3（文案改为"先删旧实例/作为新设备创建"+ 交互顺序说明） |
| C-3 candidates 命名空间不一致 | 前端终审 | §六 前端清单（candidates 归 edgeDevice.ts） |
| 跨节点"先删后建"顺序说明 | 前端终审 | §3.3-3 交互顺序说明段 |

### v3.1 修订（千问评估 3 中等 + 4 低级 + 过度设计裁剪 3 项）

| 项 | 类型 | 闭环位置 |
|----|------|---------|
| Q1 合并去重与"前端零改动"矛盾 | 中等澄清 | §六 保形去重（条件启用 + MAX(id) 非 AVG + 窗口函数跨库），§3.4/§十一 同步 |
| Q2 retention/purge 清理缺过渡期兼容条件 | 中等澄清 | §4.3 dataScopeCondition 共享 helper，清理与查询同一 scope |
| Q3 P0 补建多副本竞态 | 中等澄清 | §2.3.1 ON CONFLICT 回读复用（含 §3.3-4 序号竞态统一处理） |
| Q4 retention_days"继承系统"语义模糊 | 低级 | §4.1 快照语义（创建时快照，系统级修改不波及存量） |
| Q5 409 文案跨节点操作成本 | 低级 | 文案已含实例 ID + 节点名定位，评估确认够用，不改 |
| Q6 candidates 接口缺 retention_days 字段 | 低级 | §1.3 返回字段显式列出 |
| Q7 historical-batch 去重纳入提醒 | 低级 | §六 适用端点全族明确 |
| backfill 断点续跑 | 建议采纳 | §4.3 merge_jobs 水位 + backfill 复用 |
| 裁剪 1：merge_status 五态 + failed 恢复链 | 过度设计 | §3.4 简化为 NULL/pending/done + 失败复位 |
| 裁剪 2：backfill_done 标记 + 条件切换 | 过度设计 | §六 删除标记位，改为部署顺序强制 + OR 安全网常驻 |
| 裁剪 3：logical_device_stats 缓存表 | 过度设计 | §1.3 删除，只留 reltuples + 超时降级 |
| 保留：merge/preview 端点 | 不过度（终审认定闭环必需） | §3.4 维持 |

### v3.2 修订（千问复审 2 High + 1 Medium + 4 Low）

| 项 | 严重度 | 闭环位置 |
|----|--------|---------|
| F1 合并流程不校验源存活实例 + resolve 不跟随 merged_into → 数据撕裂 | **High** | §3.4 事务内源存活校验 409 + §六 followMergeChain（查询纵深防御）+ §八 写入同链解析 |
| F2 ON CONFLICT 复用击穿存活实例唯一性 + 违背"新设备"显式语义 | **High** | §2.3.1 三路径分流表（补建：无存活才复用/新建向导：永不复用/删除补建：可复用）+ §3.3-4 |
| F3 merge 失败静默 + 单次失败即复位 | Medium | §3.4 failed 补强：保留 Notification + 水位重试（幂等），超限才复位 |
| F4 P1 行含不存在端点 + backfill 阶段位置矛盾 | Low | §十 拆出 M 迁移步骤（P0 后 P1 前），P1/P4 行修正 |
| F5 保形去重 DISTINCT ON 与窗口函数措辞矛盾 | Low | §六 只保留 ROW_NUMBER() 窗口函数写法 |
| F6 purge 删 logical_device 前 FK 违约 | Low | §4.3 任务 2 先 UPDATE edge_devices 解除引用（Unscoped） |
| F7 候选伪 SQL 的 last_data_at 过渡期失真 | Low | §1.3 LEFT JOIN 加 OR 实例集条件（与 dataScopeCondition 同构） |

### v3.2 终审意见（后端 2 Medium + 7 Low，前端 2 Low，已全部补入）

| 项 | 严重度 | 闭环位置 |
|----|--------|---------|
| B1 pending 搬迁期间查询部分不可见 | Medium | §4.3 dataScopeCondition 对入边 pending 源做 UNION |
| B2 §七-1 死步骤与 §2.3.1/§十矛盾 | Medium | §七 标题改为"M 迁移步骤 + P4"，step 1 标记已由 P0 完成 |
| B3 §4.3 任务3 失败语义未对齐 §3.4 | Low | §4.3 任务 3 失败分支重写（Notification + 水位重试 ≤3 次超限复位） |
| B4 purge FK 只解出边没解入边 | Low | §4.3 任务 2 补清空入边 merged_into |
| B5 删除补建复用未排除已 merged 目标 | Low | §2.3.1 删除补建行补"复用前跟随链至最终目标" |
| B6 merge 目标 identity_key 规则缺失 | Low | §3.4 `merge:{uuid}` |
| B7 候选伪 SQL COUNT(ed.id) 被 JOIN 放大 | Low | §1.3 改 COUNT(DISTINCT ed.id) |
| B8 去重启用条件方向表述歧义 | Low | §六 伪码改为反向 EXISTS 检查 + 与 scope 解析同往返 |
| B9 /nodes/:id/latest 描述与代码事实不符（存量） | Low | §六 改造清单 + §十二 行为表对齐现状（软删数据仍出现，不改） |
| D-1 合并 409 多源冲突需结构化响应 | Low | §3.4 conflicts 响应结构 + §六 前端清单 LogicalDeviceList 行 |
| D-2 MainLayout 通知跳转脆弱（title 字符串匹配） | Low | §六 前端清单新增 MainLayout 行（按 source 路由扩展） |

### v3.3 修订（复审 1 Medium + 2 Low）

| 项 | 严重度 | 闭环位置 |
|----|--------|---------|
| N1 purge_requested 与重建/合并竞态（继承校验、合并源校验、删除补建复用、purge 执行均不查 purge_requested → 用户删后重建继承的数据可被夜间 purge 静默清空） | **Medium** | §3.3-2 继承校验补 purge_requested=FALSE（409）；§3.4 源校验同步（违反并入 conflicts）；§2.3.1 删除补建路径表 purge_requested=TRUE 禁止复用；§4.3 任务 2 执行前守卫（有存活实例即放弃 purge 清标志；pending 合并顺延） |
| N2 合并新建目标 retention_days 来源未定义 | Low | §3.4 明确按 §4.1 快照语义 + merge/preview 响应加 target_retention_days 字段 |
| N3 followMergeChain 逐跳查询最坏 8 次 DB 往返/请求 | Low | §六 实现指引：一次性载入 logical_devices(id, merged_into) 内存走链，N 次往返压成 1 次 |
