# 设备模板 — 实现

## 代码位置

### 后端 (P3 真修复后)
- `backend/internal/models/models.go` (`DeviceConfig` 7 字段: Name/Description/DeviceType/Protocol/HardwareType/ParserID/IsDefault/Status)
- `backend/internal/api/handler_device.go` (7 端点用 `models.DeviceConfig`, 不再用 `ConfigTemplate`)
- `backend/internal/database/gorm.go` (`AutoMigrate(&models.DeviceConfig{})` 注册)

### 前端
- `frontend-shared/src/api/deviceConfig.ts` (114 行, 完整 7 端点)
- `frontend-shared/src/views/config/DeviceConfigList.vue` (790 行)
- `frontend-shared/src/components/forms/DeviceConfigForm.vue` (507 行, 5 总线自适应)
- `frontend-shared/src/views/device/DeviceList.vue` (Step 0 模板下拉, P3 修复)
- `frontend-shared/src/api/driver.ts` (Cascader 转换)
- `frontend-shared/src/api/parser.ts` (driver tree 镜像)

## 已实现 (P3)

- [x] 7 端点完整
- [x] 7 字段持久化 (P3 修复, 之前是 5 个被静默丢弃)
- [x] 必填字段校验 (name/device_type/hardware_type)
- [x] 默认模板互斥 (事务式 UPDATE)
- [x] 设备创建向导 Step 0 集成
- [x] 模板套用自动选解析器 (parser_id 匹配)
- [x] 字段类型 enum (status, is_default)
- [x] 导入/导出 (JSON)
- [x] 克隆 (handleClone)

## 测试覆盖

- ✅ CDP E2E 10/10 (后端 API 路径, commit 2ea4062)
- ✅ 文档一致性 review (commit 2cc59b4)
- ❌ 前端组件无单元测试 (P2)

## 历史 Bug (P3 已修)

- 后端 handler 用 `ConfigTemplate` 而不是 `DeviceConfig` (字段错配)
- 5 字段被 GORM 静默丢弃
- `setDefault` 是 stub (只 echo true)
- `getDefault` 用 `write_data LIKE` 凑合

## 未实现

- [ ] 模板版本化 (P2)
- [ ] 模板使用统计 (P2)
- [ ] 模板"使用差异"对比 (P3)

详见 [设计/设备模板/详细设计.md](../设计/设备模板/详细设计.md) + [验收标准.md](../设计/设备模板/验收标准.md)
