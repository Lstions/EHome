# 前端功能 UI 分类与信息架构实测审计（2026-09-23）

- 状态：提案
- 日期：2026-09-23
- 范围：`frontend-shared/`，main 分支；只读源码，**未改业务代码、未跑测试/build/Playwright/网络**
- 方法：`grep -rn` / `wc -l` / `awk` 批量脚本统计；每条结论附 `文件:行`
- 口径：views 41 个 `.vue`（`find src/views -name '*.vue' -not -path '*/__tests__/*' -not -path '*/dev/*'`）；components 40 个 `.vue`（同法，任务书写 38，见 §6 口径差异）；路由 25 条 `path` 记录
- 去重规则见 §5：`docs/分析/UIUX审计合并结论与优化方案-2026-09-13.md` 已闭环项本节只列清单不展开

**一句话结论**：全站 41 个 view / 40 个 component 里，真正的结构性问题不是「少页面/少组件」，而是 **14 项平铺菜单 + 12 个列表页各写一套壳 + 9 文件 4629 行不可达子树（其中 5 个组件零消费）**；建议先做 C5（删死代码）与 C1/C2（列表壳/加载态），C4/C6/C8 随后，C9 必须等 F7 门禁改造后再动。

> 复核修正（2026-09-23）：原句写作「5 个零消费组件（连带 4629 行不可达子树）」，会把两个**不同基数**的集合读成乘法关系（5 个组件 = 4629 行）。实际是：**零消费组件 5 个**（GaugeChart/ParserBrowser/PeripheralControl/GPIOPinRow/PWMChannelRow）⊂ **不可达子树 9 文件 4629 行**（另含 NodeDetail/ChannelPanel/GPIOResourceList/PWMResourceList 四个两级死亡链文件）。评估清理收益时必须以 9 文件 4629 行为准。

> **执行回写（2026-09-23 后续批次，逐项实测）**：上面三项结构问题已部分闭环，引用前必须按下表复测：
>
> | 审计原结论 | 当前实测 | 处置 |
> |---|---|---|
> | 14 项平铺菜单 | **5 组**（总览/设备与通道/数据/自动化与通知/运维，默认展开） | ✅ 已改（Phase 2.4） |
> | 9 文件 4629 行不可达 | **2 文件 3239 行**（NodeDetail 1272 + ChannelPanel 1967） | ◐ 已删 826 行（GaugeChart 88 + GPIOPinRow 182 + PWMChannelRow 205 + ParserBrowser 351）；3 文件因 E1 接线转为可达 |
> | 列表页三态缺失（C2） | **12/12 页均有加载指示 + 空态** | ✅ DeviceConfigList 补 v-loading；AlertRules/Automation 事件表空态按 rules 有无分两态 |
>
> 复跑：`for f in <12 个列表页>; do grep -c "Skeleton\|skeleton\|v-loading" $f; done`（均应 >0）；
> 死代码按文件逐个 `grep -rn '<Name>' src e2e tools` 并确认无 `from '...<Name>.vue'` 生产 import。
> **教训**：本报告的"12 个列表页各写一套壳"在修复后仍**部分成立**（壳仍在各页内联，F1/F2 抽取未做），
> 但"三态缺失"已不成立 —— 逐条复测比整段引用可靠。

## 1. 路由与菜单 IA 实测表

### 1.1 全部路由（`src/router/index.ts`）

| # | path | name | 组件 | meta 可见性 | 可达性实测 |
|---|---|---|---|---|---|
| 1 | `/login` (`:21`) | Login | `auth/Login.vue` | requiresAuth:false | 可达（守卫 `:185-210`） |
| 2 | `/` (`:27`) | — | `layout/MainLayout.vue` | requiresAuth:true | 容器重定向到 `/dashboard` (`:33`) |
| 3 | `''` (`:32`) | — | redirect `/dashboard` | — | 可达 |
| 4 | `dashboard` (`:36`) | Dashboard | `dashboard/Dashboard.vue` | 菜单① | 可达 |
| 5 | `node` (`:42`) | NodeList | loader `routeLoaders.ts:1` | 菜单② | 可达 |
| 6 | `node/:id` (`:48`) | NodeDetail | `node/NodeOverview.vue` | hidden:true | **别名路由**，见 §1.3 |
| 7 | `node/:id/overview` (`:54`) | NodeOverview | `node/NodeOverview.vue` | hidden:true | **规范 URL**，消费点 `DeviceInfoCard.vue:28,63` |
| 8 | `channel` (`:60`) | ChannelList | `channel/ChannelList.vue` | 菜单⑥ | 可达 |
| 9 | `edge-device` (`:66`) | EdgeDeviceList | loader `routeLoaders.ts:2` | 菜单③ | 可达 |
| 10 | `edge-device/:id` (`:72`) | EdgeDeviceDetail | `EdgeDeviceDetailRouter.vue` | hidden:true | 可达（由列表 `goToDetail` 推） |
| 11 | `logical-device` (`:78`) | LogicalDeviceList | `logical-device/LogicalDeviceList.vue` | 菜单④ | 可达 |
| 12 | `data-sources` (`:84`) | DataSourceList | `data-source/DataSourceList.vue` | 菜单⑤ | 可达 |
| 13 | `data` (`:90`) | DataPanel | `data/DataPanel.vue` | 菜单⑦ | 可达 |
| 14 | `firmware` (`:96`) | FirmwareManage | `firmware/FirmwareManage.vue` | 菜单⑧ | 可达 |
| 15 | `device-configs` (`:102`) | DeviceConfigList | `config/DeviceConfigList.vue` | 菜单⑨ | 可达 |
| 16 | `monitor` (`:108`) | Monitor | `monitor/Monitor.vue` | 菜单⑩ | 可达 |
| 17 | `alerts` (`:114`) | AlertRules | `alert/AlertRules.vue` | 菜单⑪ | 可达 |
| 18 | `automation` (`:120`) | AutomationRules | `automation/AutomationRules.vue` | 菜单⑫ | 可达 |
| 19 | `notification-channels` (`:127`) | NotificationChannels | `notification/NotificationChannels.vue` | 菜单⑬ | 可达 |
| 20 | `notification-deliveries` (`:134`) | NotificationDeliveries | `notification/NotificationDeliveries.vue` | 菜单⑭ | 可达 |
| 21 | `profile` (`:140`) | Profile | `profile/Profile.vue` | **hiddenInMenu:true** | 仅用户菜单 `MainLayout.vue:819` |
| 22 | `/403` (`:149`) | Forbidden | `error/Forbidden.vue` | requiresAuth:false | **全仓零前端导航消费点**（`grep -rn '/403' src` 除路由外 0 命中，验收无网络，不排除反代/后端 302） |
| 23 | `/dev/mock-bms` (`:158`) | MockBmsPanel | `src/dev/MockBmsPanel.vue` | DEV 条件，hidden | 仅 DEV；views 计数排除 |
| 24 | `/dev/bms-demo` (`:164`) | BmsDemoPage | `src/dev/BmsDemoPage.vue` | DEV 条件，hidden | 仅 DEV |
| 25 | `/:pathMatch(.*)*` (`:171`) | NotFound | `error/NotFound.vue` | requiresAuth:false | 兜底可达 |

> `path` 行号：`:21/:27/:32/:36/:42/:48/:54/:60/:66/:72/:78/:84/:90/:96/:102/:108/:114/:120/:127/:134/:140/:149/:158/:164/:171`。

### 1.2 菜单 14 项实测（`MainLayout.vue:397-412`，`allMenuItems` 数组）

| 序 | path | title | 路由是否存在 | 备注 |
|---|---|---|---|---|
| 1 | `/dashboard` | 仪表盘 | ✅ | — |
| 2 | `/node` | 节点 | ✅ | — |
| 3 | `/edge-device` | 边缘设备 | ✅ | — |
| 4 | `/logical-device` | 逻辑设备 | ✅ | — |
| 5 | `/data-sources` | 数据源 | ✅ | — |
| 6 | `/channel` | 通道管理 | ✅ | 图标与「节点」重复（同为 `Connection`） |
| 7 | `/data` | 数据面板 | ✅ | — |
| 8 | `/firmware` | 固件管理 | ✅ | — |
| 9 | `/device-configs` | 配置模板 | ✅ | — |
| 10 | `/monitor` | 系统监控 | ✅ | — |
| 11 | `/alerts` | 告警规则 | ✅ | — |
| 12 | `/automation` | 自动化策略 | ✅ | — |
| 13 | `/notification-channels` | 通知通道 | ✅ | 图标与「告警规则」重复（同为 `Bell`） |
| 14 | `/notification-deliveries` | 投递审计 | ✅ | — |

- 菜单渲染：桌面 `MainLayout.vue:25-36`、移动抽屉 `:78-89`；`menuItems = computed(() => allMenuItems)` (`:413`)，**无角色过滤、无分组、无 `el-sub-menu`**（`grep -n 'el-sub-menu'` = 0）。
- `activeMenu = route.path` (`:694-697`) 做整串等值：进入 `/profile`、`node/:id`、`edge-device/:id` 等隐藏路由时菜单无高亮，`activeMenuIndex` 落到第 1 项 (`:558-561`)。
- 顶栏快捷搜索目的地只有 8 个（`MainLayout.vue:746-754`：dashboard/node/edge-device/channel/data/firmware/device-configs/monitor），缺 logical-device、data-sources、alerts、automation、notification-channels/deliveries 六个菜单项，也缺 profile；不是缺陷，但暴露搜索与菜单两套导航事实源。
- `hiddenInMenu`/`route.meta` **无任何消费代码**：全仓 `grep -rn 'route.meta|meta.hidden|hiddenInMenu' src --include=*.vue --include=*.ts`（排除 router 自身）0 命中，菜单/面包屑均为手写数组（`MainLayout.vue:397-412`、`:708` 起面包屑 map）。即 `meta.hidden:true`（node/:id、edge-device/:id、DEV 路由）只作文档语义，不驱动 UI。

### 1.3 别名、隐藏与死代码

1. **`node/:id` 与 `node/:id/overview` 同组件**：`router/index.ts:50` 与 `:56` 都 `import('@/views/node/NodeOverview.vue')`。前者 name=NodeDetail，后者 name=NodeOverview。
2. **`views/node/NodeDetail.vue` 是死代码**：全仓唯一「导入 NodeDetail.vue」的命中为零（`grep -rn "views/node/NodeDetail.vue" src --include=*.vue --include=*.ts \| grep -v __tests__` 只输出注释）；`ChannelPanel.vue:5-15` 已实测声明「router 里没有 NodeDetail.vue…本组件永远不可达」。
3. **`ChannelPanel.vue`（1967 行）随之不可达**：唯一 import 在 `NodeDetail.vue:444`，而父文件本身不可达（`ChannelPanel.vue:7-11`）。单测仍直接 mount 它（`ChannelPanelEdit/PeriphEdit/UartReconfigure/Route`），只能证明可编译，不能证明可达（`ChannelPanel.vue:18-19`）。
4. **`profile` 隐藏入口**：路由 `hiddenInMenu:true` (`router/index.ts:143`)，入口仅用户下拉 `MainLayout.vue:233-235 + :819`。侧栏 14 项不含它，属有意隐藏而非不可达。
5. **`/403` 不可达**：路由存在 (`:149-152`)，但全仓无任何 `router.push('/403')`/文案导航；401/403 在 `stores/websocket.ts:163` 仅映射为会话失效，不跳 Forbidden。是「有页面无入口」。
6. **`channelStore` 入口分流**：`ChannelList.vue:443-444` 用 `{ name: 'NodeDetail' }` 推别名路由 `/node/:id`，而非 `DeviceInfoCard.vue:28,63` 的 `/node/${id}/overview`。同一目标两条 URL，是「同组件双路径」的现实消费面。

## 2. 41 个 view 页面矩阵（脚本生成）

口径：行数 `wc -l`；控件存在性 = 第一次 `grep -nE` 命中行号（`el-table` 含 `el-table-column`；Skeleton = `SkeletonCard|el-skeleton`；`—` = 0 命中）。角色为人工归类，分类锚点见各行模板首块。

| # | 文件 | 行 | 菜单可见 | 角色 | table | dialog | tab-pane | Empty | Skeleton | 控件锚点（首命中行） |
|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `alert/AlertRules.vue` | 405 | 菜单⑪ | 规则/列表 | 有 | 有 | — | — | — | t:18 d:129 p:113 |
| 2 | `auth/Login.vue` | 439 | 独立/认证 | 认证 | — | — | — | — | — | 根组件 `Login.vue:1` |
| 3 | `automation/AutomationRules.vue` | 850 | 菜单⑫ | 规则/列表 | 有 | 有 | — | — | 有:302 | t:14 d:168 p:154 |
| 4 | `channel/ChannelList.vue` | 621 | 菜单⑥ | 列表（含 I2C 扫描） | 有 | — | — | 有:105 | 有:100 | t:132 p:217 |
| 5 | `config/DeviceConfigList.vue` | 860 | 菜单⑨ | 配置/列表 | — | 有 | — | 有:4 | — | d:212 e:4 |
| 6 | `dashboard/Dashboard.vue` | 1028 | 菜单① | 总览 | 有:264 | — | — | 有:174 | 有:105 | t:264 e:174 p:(无) |
| 7 | `data/DataPanel.vue` | 1243 | 菜单⑦ | 数据/列表 | 有:206 | — | — | 有:170 | 有:195 | t:206 p:243 |
| 8 | `data-source/DataSourceList.vue` | 846 | 菜单⑤ | 列表 | 有:102 | 有:230 | — | 有:199 | — | t:102 d:230 p:216 |
| 9 | `edge-device/bms/BmsCellVoltageChart.vue` | 179 | 子组件 | 详情工作台 | — | — | — | — | — | 纯 ECharts 子组件 `:1` |
| 10 | `edge-device/bms/BmsCellVoltageHistoryChart.vue` | 153 | 子组件 | 详情工作台 | — | — | — | — | 有:14 | s:14 |
| 11 | `edge-device/bms/BmsDetailPage.vue` | 317 | 子路由 | 详情工作台 | — | — | — | — | 有:16 | s:16 |
| 12 | `edge-device/bms/BmsMosStatus.vue` | 131 | 子组件 | 详情工作台 | — | — | — | — | — | 状态子卡 `:1` |
| 13 | `edge-device/bms/BmsProtectionGrid.vue` | 90 | 子组件 | 详情工作台 | — | — | — | — | — | `StatusItemGrid` 消费点 `:20` |
| 14 | `edge-device/EdgeDeviceDetailRouter.vue` | 78 | hidden | 详情工作台（壳） | — | — | — | — | 有:5 | s:5 |
| 15 | `edge-device/EdgeDeviceList.vue` | 2482 | 菜单③ | 列表 | 有:127 | 有:290 | 有:439 | 有:264 | 有:5 | t:127 d:290 p:278 |
| 16 | `edge-device/GenericDeviceDetail.vue` | 125 | 子路由 | 详情工作台 | — | — | — | — | 有:16 | s:16 |
| 17 | `edge-device/inverter/InverterDetailPage.vue` | 216 | 子路由 | 详情工作台 | — | — | — | — | 有:16 | s:16 |
| 18 | `edge-device/inverter/InverterEnergyCard.vue` | 127 | 子组件 | 详情工作台 | — | — | — | — | — | 子卡 `:1` |
| 19 | `edge-device/inverter/InverterMpptCard.vue` | 90 | 子组件 | 详情工作台 | — | — | — | — | — | 子卡 `:3` |
| 20 | `edge-device/inverter/InverterPowerFlow.vue` | 154 | 子组件 | 详情工作台 | — | — | — | — | — | 子卡 `:1` |
| 21 | `edge-device/inverter/InverterStatusGrid.vue` | 121 | 子组件 | 详情工作台 | — | — | — | — | — | `StatusItemGrid` 消费点 `:1` |
| 22 | `edge-device/inverter/InverterTempCard.vue` | 223 | 子组件 | 详情工作台 | — | — | — | — | — | 子卡 `:3` |
| 23 | `edge-device/shared/ActionConfirmationDialog.vue` | 18 | 子组件 | 详情工作台 | — | 有:2 | — | — | — | d:2 |
| 24 | `edge-device/shared/ActionForm.vue` | 76 | 子组件 | 详情工作台 | — | 有:2 | — | — | — | d:2 |
| 25 | `edge-device/shared/CommandFrequencySection.vue` | 38 | 子组件 | 详情工作台 | — | — | — | — | — | 子卡 `:2` |
| 26 | `edge-device/shared/DeviceControlPanel.vue` | 168 | 子组件 | 详情工作台 | — | 有:55 | — | — | — | d:55 |
| 27 | `edge-device/shared/DeviceHeader.vue` | 254 | 子组件 | 详情工作台 | — | 有:51 | — | — | — | d:51 |
| 28 | `edge-device/shared/DeviceInfoCard.vue` | 166 | 子组件 | 详情工作台 | — | — | — | — | — | 信息卡 `:2` |
| 29 | `edge-device/shared/HistoryChartSection.vue` | 362 | 子组件 | 详情工作台 | — | — | — | — | 有:25 | s:25 |
| 30 | `error/Forbidden.vue` | 38 | 路由存在 | 错误 | — | — | — | — | — | 403 专用 `:1` |
| 31 | `error/NotFound.vue` | 59 | 兜底 | 错误 | — | — | — | — | — | `:1` |
| 32 | `firmware/FirmwareManage.vue` | 492 | 菜单⑧ | 配置/列表 | 有:32 | 有:110 | — | 有:96 | 有:12 | t:32 d:110 p:86 |
| 33 | `layout/MainLayout.vue` | 1474 | 框架 | 系统/框架 | 有:1012 | — | — | — | — | 通知表格 t:1012 |
| 34 | `logical-device/LogicalDeviceList.vue` | 968 | 菜单④ | 列表 | 有:49 | 有:124 | — | — | — | t:49 d:124 p:110 |
| 35 | `monitor/Monitor.vue` | 925 | 菜单⑩ | 系统/总览 | 有:7 | — | — | — | 有:199 | t:7 p:(无) |
| 36 | `node/NodeDetail.vue` | 1272 | **死代码** | 详情工作台 | 有:276 | — | — | — | 有:54 | t:276 s:54 |
| 37 | `node/NodeList.vue` | 976 | 菜单② | 列表 | 有:173 | — | — | 有:266 | 有:5 | t:173 p:253 |
| 38 | `node/NodeOverview.vue` | 2431 | hidden 别名 | 详情工作台 | — | 有:730 | — | — | 有:21 | d:730 s:21 |
| 39 | `notification/NotificationChannels.vue` | 388 | 菜单⑬ | 通知/列表 | 有:27 | — | — | 有:101 | — | t:27 e:101 p:114 |
| 40 | `notification/NotificationDeliveries.vue` | 387 | 菜单⑭ | 审计/列表 | 有:67 | — | — | 有:117 | — | t:67 e:117 p:131 |
| 41 | `profile/Profile.vue` | 148 | hiddenInMenu | 系统/账户 | — | — | — | — | — | `PageHeader:3` |

### 2.1 矩阵读数

- 41 文件合计 **21418 行**（`find … | xargs wc -l`）。最大的 10 个：`EdgeDeviceList 2482` > `NodeOverview 2431` > `MainLayout 1474` > `NodeDetail 1272`（死） > `DataPanel 1243` > `Dashboard 1028` > `NodeList 976` > `LogicalDeviceList 968` > `Monitor 925` > `DeviceConfigList 860`。
- `wc -l > 300` 的 view：**20/41**；`src/components` >300 的：**12/40**（§6 口径）。
- 含 `el-table` 标签的 view 文件 **15/41**（`grep -rl 'el-table\b'` 去重计数），其中真正「路由级列表页 + `el-pagination`」12 个（`listPageConventions.spec.ts:22-31` 的口径）；详情页内嵌表（`NodeDetail.vue:276`）与 `MainLayout.vue:1012` 通知表不计入列表页家族。
- 有 `EmptyState` 的 view 文件 **10/41**（`ChannelList/DeviceConfigList/Dashboard/DataPanel/DataSourceList/EdgeDeviceList/FirmwareManage/NodeList/NotificationChannels/NotificationDeliveries`）；`AlertRules`、`AutomationRules`、`LogicalDeviceList` 仍是 `el-table` 默认空文案或自写空态。
- 「详情工作台」页面 14/41（含 bms/inverter/shared 子卡），大量是 90–362 行的单一职责子组件，不是可合并对象；页面级过大的问题是 `EdgeDeviceList`/`NodeOverview`/`NodeList`（§4-C1、§4-C8）。

### 2.2 12 个路由级列表页的「加载/空/骨架/分页」实测（`listPageConventions.spec.ts:22-31` 分母）

| # | 页面 | 分页锚点 | PageHeader | v-loading | EmptyState | Skeleton |
|---|---|---|---|---|---|---|
| 1 | `alert/AlertRules.vue` | `:113` | `:3` | 2 | 0 | 0 |
| 2 | `automation/AutomationRules.vue` | `:154` | `:3` | 2 | 0 | 1 (`:302`) |
| 3 | `channel/ChannelList.vue` | `:217` | `:3` | 1 | 3 | 2 (`:100`) |
| 4 | `config/DeviceConfigList.vue` | `:200` | `:5` | **0** | 3 | **0** |
| 5 | `data/DataPanel.vue` | `:243` | `:3` | **0** | 3 | 1 (`:195`) |
| 6 | `data-source/DataSourceList.vue` | `:216` | `:3` | 3 | 2 | 0 |
| 7 | `edge-device/EdgeDeviceList.vue` | `:278` | **缺**（`:365 EXEMPT_PAGE_HEADER`） | **0** | 2 | 3 (`:5`) |
| 8 | `firmware/FirmwareManage.vue` | `:86` | `:3` | **0** | 2 | 1 (`:12`) |
| 9 | `logical-device/LogicalDeviceList.vue` | `:110` | `:6` | 2 | 0 | 0 |
| 10 | `node/NodeList.vue` | `:253` | **缺**（`:365 EXEMPT_PAGE_HEADER`） | 1 | 2 | 3 (`:5`) |
| 11 | `notification/NotificationChannels.vue` | `:114` | `:3` | 1 | 2 | 0 |
| 12 | `notification/NotificationDeliveries.vue` | `:131` | `:3` | 1 | 2 | 0 |

读数：
- **6/12 页骨架为 0**（AlertRules、DeviceConfigList、DataSourceList、LogicalDeviceList、NotificationChannels、NotificationDeliveries；表中 `Skeleton` 列 0 的 6 行），其中 `DeviceConfigList` 同时 v-loading=0（09-13 文档 `:76` 已判「首屏仍无骨架」）。
- **v-loading=0 的 4 页**：DeviceConfigList、DataPanel、EdgeDeviceList、FirmwareManage（表中加粗 0 共 4 处）；其中 EdgeDeviceList 用整页 Skeleton 替代，DataPanel 在查询按钮 `:36` 与 `el-skeleton:195` 另有 loading 状态（`loading=false` 初值 `:440`、置 `true` `:646`），不算无反馈。
- **空态覆盖 9/12**（表中 `EmptyState` 列非 0 的 9 行：ChannelList、DeviceConfigList、DataPanel、DataSourceList、EdgeDeviceList、FirmwareManage、NodeList、NotificationChannels、NotificationDeliveries）；AlertRules / AutomationRules / LogicalDeviceList 三页 0 命中，用表格自身空文案。
- 两页缺 `PageHeader` 是 09-13 门禁的已登记豁免（`listPageConventions.spec.ts:414-421`），本报告不当作新缺陷。
- `el-pagination` 12/12 全有，且 `listPageConventions.spec.ts:527-538` 门禁已保证 `:total`+`current-page` 绑定，因此本表不再判分页正确性。

### 2.3 页面角色分组计数（矩阵 §2 的人工列）

| 角色 | 页面数 | 文件名 |
|---|---|---|
| 总览 | 2 | Dashboard、Monitor |
| 列表/终端配置 | 8 | NodeList、EdgeDeviceList、LogicalDeviceList、ChannelList、DataSourceList、DataPanel、FirmwareManage、DeviceConfigList |
| 规则 | 2 | AlertRules、AutomationRules |
| 通知 | 2 | NotificationChannels（配置）、NotificationDeliveries（审计） |
| 详情工作台 | 21 | NodeOverview + 20 个 BMS/Inverter/共享子卡/详情壳（含死代码 NodeDetail 单独一行） |
| 系统/框架 | 2 | MainLayout、Profile |
| 认证 | 1 | Login |
| 错误 | 2 | Forbidden、NotFound |
| 死代码 | 1 | NodeDetail（与 NodeOverview 职责重复） |
| 合计 | 41 | 含 10 个 `edge-device/shared` + bms/inverter 子页面 |

> 注：「列表/终端配置」把 ChannelList 归入是因为它在 `:277-297` 有 I2C/设备扫描动作且列操作区（`:188-209`）含扫描按钮，不是纯列表。

## 3. 组件复用矩阵（40 个 components `.vue`）

口径：消费点 = **真实 `import ... from '...'` 语句**跨文件命中（排除 `__tests__`、排除 `components.d.ts` 自动生成声明、排除自身）；模板标签自动注册消费也合并计入。`components.d.ts` 生成器 `vite.config.ts:33-38` 的 `dirs: ['src/components']` 让「仅被测试/无人消费」与「自动全局注册」并存，因此**零 import 必须再查模板标签**，两条都 0 才判死。

| # | 组件 | 行 | 消费点数 | 真实消费方 | 判定 |
|---|---|---|---|---|---|
| 1 | `channel/ChannelManager.vue` | 764 | 2 | `views/node/NodeOverview.vue:812`、`node/ChannelPanel.vue:363` | 生产 1 个（+死 1） |
| 2 | `channel/ChannelTerminal.vue` | 684 | 2 | `NodeOverview.vue:813`、`ChannelPanel.vue:364` | 生产 1 个（+死 1） |
| 3 | `charts/GaugeChart.vue` | 88 | **0** | 模板标签 0（全仓仅自身 import echarts GaugeChart） | **死组件** |
| 4 | `charts/LineChart.vue` | 303 | 3 | `DataPanel/HistoryChartSection/BmsCellVoltageHistoryChart` | 跨 3 页复用 |
| 5 | `charts/TimeRangeSelector.vue` | 80 | 2 | 同上两处 | 复用 |
| 6 | `common/CountUp.vue` | 125 | 2 | `EdgeDeviceList/NodeList` | 双列表页复用 |
| 7 | `common/EmptyState.vue` | 173 | 12 | 10 个 view + `RealtimeDataList/CommandList` | 全站最高复用 |
| 8 | `common/ErrorBoundary.vue` | 97 | 1 | `App.vue:16` | 全局壳 |
| 9 | `common/ErrorPageLayout.vue` | 90 | 2 | `Forbidden/NotFound` | 错误页复用 |
| 10 | `common/MetricStatCard.vue` | 211 | 3 | BMS/Inverter/`dev/MockBmsPanel` | 详情域复用 |
| 11 | `common/NetworkBanner.vue` | 150 | 1 | `App.vue:17` | 全局壳 |
| 12 | `common/PageHeader.vue` | 86 | 15 | 14 view + `DeviceHeader` | 全站页头 |
| 13 | `common/RouteProgressBar.vue` | 63 | 1 | `App.vue:18` | 全局壳 |
| 14 | `common/SkeletonCard.vue` | 214 | 4 | `ChannelList/EdgeDeviceList/Monitor/NodeList` | 仅 4/41 view |
| 15 | `common/StatCard.vue` | 151 | 7 | 6 view + dev | 复用 |
| 16 | `common/StatusBadge.vue` | 64 | 3 | `DeviceInfoCard/NodeDetail/NodeOverview` | 节点域复用 |
| 17 | `common/StatusItemGrid.vue` | 75 | 2 | `BmsProtectionGrid/InverterStatusGrid` | 详情域复用 |
| 18 | `common/ThemeSwitch.vue` | 58 | 1 | `MainLayout` | 单点 |
| 19 | `data/RealtimeDataList.vue` | 251 | 4 | BMS/Generic/Inverter(+dev) | 详情域复用 |
| 20 | `device/CommandList.vue` | 252 | 1 | `CommandFrequencySection` | **单页私用链** |
| 21 | `device/CreateWizardCommandIntervals.vue` | 293 | 2 | `EdgeDeviceList/QuickCreateDeviceDialog` | 双创建入口复用 |
| 22 | `device/DeviceBatchDeleteDialog.vue` | 126 | 1 | `EdgeDeviceList:659` | 与 #23 疑似重复 |
| 23 | `device/DeviceDeleteDialog.vue` | 295 | 1 | `EdgeDeviceList:658` | 与 #22 疑似重复（见 §4-2） |
| 24 | `device/LogicalDeviceCandidateSelect.vue` | 288 | 2 | `EdgeDeviceList/QuickCreateDeviceDialog` | 复用 |
| 25 | `forms/DeviceConfigForm.vue` | 506 | 1 | `DeviceConfigList` | 单页私用 |
| 26 | `forms/InitializeAdminForm.vue` | 201 | 1 | `Login.vue:94` | 单页私用 |
| 27 | `forms/LoginForm.vue` | 126 | 1 | `Login.vue:93` | 单页私用 |
| 28 | `forms/OTAForm.vue` | 438 | 2 | `NodeDetail/NodeOverview` | 复用（含 1 死页） |
| 29 | `node/ChannelPanel.vue` | 1967 | 1 | `NodeDetail:444`（死） | **不可达**（§1.3） |
| 30 | `node/LogHistoryPanel.vue` | 341 | 1 | `LogPanel:72` | 嵌套子件 |
| 31 | `node/LogPanel.vue` | 395 | 2 | `NodeDetail/NodeOverview` | 外层壳，1 个消费方死 |
| 32 | `node/LogRealtimeViewer.vue` | 449 | 1 | `LogPanel:73` | 嵌套子件 |
| 33 | `node/QuickCreateDeviceDialog.vue` | 524 | 2 | `NodeDetail/NodeOverview` | 复用（含 1 死页） |
| 34 | `notification/NotificationChannelFormDialog.vue` | 398 | 1 | `NotificationChannels:143` | 单页私用 |
| 35 | `parser/ParserBrowser.vue` | 351 | **0** | 模板标签 0；测试仅在 `IconActionAccessibilityGate.spec.ts` 登记 | **死组件** |
| 36 | `periph/GPIOPinRow.vue` | 182 | **0** | 模板标签 0 | **死组件** |
| 37 | `periph/GPIOResourceList.vue` | 180 | 2 | `ChannelPanel:365`（死）、`PeripheralControl:56`（也死） | 两级死亡链 |
| 38 | `periph/PeripheralControl.vue` | 163 | **0** | 模板标签 0 | **死组件** |
| 39 | `periph/PWMChannelRow.vue` | 205 | **0** | 模板标签 0 | **死组件** |
| 40 | `periph/PWMResourceList.vue` | 221 | 2 | `ChannelPanel:366`（死）、`PeripheralControl:57`（也死） | 两级死亡链 |

### 3.1 零消费死组件（import 0 且模板标签 0）

| 组件 | 行数 | 关键证据 |
|---|---|---|
| `charts/GaugeChart.vue` | 88 | 全仓命中仅自身 `:9 :13`；无消费方 |
| `parser/ParserBrowser.vue` | 351 | 无生产/模板消费；`IconActionAccessibilityGate.spec.ts` 仅做登记 |
| `periph/PeripheralControl.vue` | 163 | 自身 import 两个 ResourceList（`:56-57`），但自身无消费方 |
| `periph/GPIOPinRow.vue` | 182 | 只有 `GPIOPinRow.spec.ts` 直接 mount |
| `periph/PWMChannelRow.vue` | 205 | 只有 `PWMChannelRow.spec.ts` 直接 mount |

链条判定：`ChannelPanel`（自身因 `NodeDetail` 死）→ `GPIOResourceList/PWMResourceList` → `PeripheralControl` → `GPIOPinRow/PWMChannelRow` 为同一不可达子树；主控若要清理，应整树处理，避免只删叶子把 `PeripheralControl` 暴露成新死组件。

### 3.2 疑似重复对/嵌套关系（只报事实，不直接判合并）

1. **`DeviceDeleteDialog.vue`（295 行）vs `DeviceBatchDeleteDialog.vue`（126 行）**：同在 `EdgeDeviceList.vue:600-608` 分别挂载（import `:658-659`）。二者 props 不同（单个 `device` vs `devices[]`；`:80-86` vs `:45-51`），但都含 `dataAction` radio + `emit('confirm', boolean)`（单 `:116/:161`，批 `:70/:81`），对话框骨架、`update:visible` computed 同形。批量版是单个版的「设备列表摘要」变体，不是独立业务。
2. **`LogPanel` > `LogHistoryPanel` + `LogRealtimeViewer` 三层**：`LogPanel.vue:72-73` 同时 import 两个子件，`:47` 渲染实时、`:62` 渲染历史 ⇒ 是**壳+两 pane**，不是三个并列页面，不构成简单三合一。`LogPanel` 真正消费点 2 个（`NodeOverview` 生产、`NodeDetail` 死）。
3. **`ChannelPanel` vs `ChannelManager`**：名字像同义，实际是**旧整页面板（1967 行，不可达）** vs **生产对话框（764 行，2 消费方）**。ChannelManager 从 ChannelPanel 剥离出去的通道 CRUD 对话框；前者还含硬件资源/终端/DMA，后者只做通道表单（`ChannelManager.vue:5-11`）。不应把两者合并，应删除前者并确认后者覆盖其独有能力。
4. **`GPIOResourceList` vs `PWMResourceList`**：结构同形（`GPIOResourceList.vue:56-60` vs `PWMResourceList.vue:57-72` 都是 configure/edit/remove 三按钮 + emit），各 180/221 行；表头列与 props 不同（`resource.pin` vs `resource.resource.id`）。属「同一模板两个资源族」，可抽上层行渲染，但需保留列差异。
5. **`DeviceHeader`（254 行）含自己的编辑/删除对话框**（`:51/:76`）：与 `EdgeDeviceList` 的 `DeviceDeleteDialog` 形成**第二个删除确认实现**；`DeviceHeader` 仅 BMS/Inverter/Generic 详情 3 消费方，删除入口在详情页。这是详情页的独立删除实现，归入 §4-C4 的四套口径。

## 4. 合并/精简候选（13 条，证据 + 收益 + 风险 + 反证）

> 判定原则：只报**有机械证据**的候选；所有「收益」都是本次脚本可数的页面数/行数/入口数，不含主观体感。

### C1. 抽取「列表页壳」模板：统计卡 + 工具栏 + 筛选条 + 分页 4 段式

> **执行前复测（2026-09-23 后续批次）：收益比原文估计的小一个数量级，建议降级为「暂不做」。**
>
> 原文称「可直接消掉 4 页各自复制的统计卡循环与标签循环；压缩数百行模板」。实测：
>
> | 度量 | 实测值 | 复跑 |
> |---|---|---|
> | `.stats-row` CSS 声明 | 4 页共 **20 行**（每页 3 行声明 + 各页响应式覆盖） | `python3` 取 `.stats-row {…}` 与 `@media *.stats-row` 块 |
> | `.stats-row` 模板体 | NodeList 3 行 / EdgeDevice 3 行 / Config 18 行 / DataSource 18 行 = **42 行** | 取 `class="stats-row"` 起的容器 |
> | 已有公共组件 | `StatCard` / `EmptyState` / `SkeletonCard` / `MetricStatCard` **均已存在** | `ls components/common/` |
> | 页面间差异 | 点击筛卡（2 页有 / 2 页无）、移动端列数（2 页单行 4 列 / DataSource 2 列）、1200px 断点（3 页有 / 1 页无） | 逐页 grep |
>
> **结论**：重复量约 **60 行量级**（模板 42 + CSS 20），而非"数百行"。
> 且 4 页在**点击行为、响应式断点、列数**三处都不同 —— 抽 slot 会得到原文自己警告的
> "万能组件"（§风险），收益 < 维护成本。**真正的重复早已被 `StatCard` 等公共组件消除**，
> 剩下的只是各页 3 行容器声明。
>
> 故本项**不建议按原样执行**。若将来第 13 个列表页出现且前 4 页行为趋同，再抽也不迟 ——
> 抽取的价值来自"第 13 次抄写"被避免，而不是当下这 60 行。
>
> **建议保留更小的一项**：`ListToolbar`（3 页有 `class="toolbar"`）可另评估，
> 但同样需先确认 3 页的工具栏差异（ChannelList 无 stats-row、NodeList/EdgeDevice 有）。

- 候选：`NodeList`/`EdgeDeviceList` 已经三块同构（`class="stats-row"`、`class="toolbar"`、`active-filters`），`DeviceConfigList`/`DataSourceList` 有统计卡与筛选但缺同一套 class；12 个路由级列表页合计 **10518 行**。
- 证据：`NodeList.vue:4,36,84` vs `EdgeDeviceList.vue:4,40,98`（结构逐段对应）；`grep -n 'class="stats-row"'` 命中 4 文件（`NodeList/EdgeDeviceList/DeviceConfigList/DataSourceList`）、`class="toolbar"` 命中 3 文件（`ChannelList/NodeList/EdgeDeviceList`）；12 个分页页见 §2 矩阵 `p:` 锚点。
- 收益：若抽 2 个布局组件（`ListStatsGrid` + `ListToolbar`/`ActiveFilterBar`），可直接消掉 4 页各自复制的统计卡循环与标签循环；在不动业务逻辑的前提下压缩数百行模板；后续新增列表页不再第 13 次抄。
- 风险：各页 stats 卡片数/点击行为/权限禁用不同（`NodeList.vue:4-35` 4 卡无点击 vs `EdgeDeviceList.vue:4-39` 有 `@click="handleStatClick"`），抽 slot 过度会得到「万能组件」。
- 反证：`DeviceConfigList`/`DataSourceList` 统计卡数据源与布局尚不同（`DeviceConfigList.vue:8` vs `DataSourceList.vue:10`），不能一次强并；`listPageConventions.spec.ts:22-31` 已把「有分页的 12 页」固化为门禁分母，抽取必须同步该解析器（`listPageConventions.spec.ts:32-33` 明写新增第三种组件写法即需同步）。

### C2. 重复的列表页加载/空态三件套（v-loading / EmptyState / Skeleton）收敛

- 候选：12 个列表页的加载/空态实现方式 **5 种**：
  - `v-loading`：AlertRules(2)、AutomationRules(2)、ChannelList(1)、DataSource(3)、LogicalDevice(2)、NodeList(1)、NotificationChannels(1)、NotificationDeliveries(1)
  - `SkeletonCard`：ChannelList(2)、EdgeDeviceList(3)、NodeList(3)、Monitor(2)
  - 无 loading 也几乎无骨架：`DeviceConfigList` v-loading=0 / skeleton=0、`DataPanel` skeleton=1 但 v-loading=0、`EdgeDeviceList` v-loading=0（用 Skeleton）
  - `EmptyState` 命中：仅 10 个 view；AlertRules / AutomationRules / LogicalDeviceList 为 0（`grep -c EmptyState`）
- 证据：`DeviceConfigList.vue:188` 只有 `v-if="!loading && filteredConfigs.length === 0"`；文件内 `Skeleton|skeleton|v-loading` 0 命中（`UIUX审计合并结论与优化方案-2026-09-13.md:76` 已判「部分闭环：device-configs 首屏仍无骨架」）。
- 收益：把「加载/空/错」做成一个 `AsyncSection`（Loading slot + Empty slot + error kind），可统一至少 8 页；同时消化 `EmptyState` 已有 12 个消费点，无需新造。
- 风险：`v-loading` 可挂在 `el-table`/`el-card`/`div` 上，抽成 `v-loading` 指令式组件会改变 DOM 层级，影响既有样式与测试选择器（`listPageConventions.spec.ts` 不查 loading，但各页 mount 测试会）。
- 反证：不同页面的 loading 粒度不同（整页骨架 vs 表格局部遮罩），强行统一会把 `Monitor` 的轮询刷新也变骨架，反而闪屏。此条建议**只统一约定、不强行同组件**。

### C3. 抽取「创建/编辑边缘设备」重复实现（EdgeDeviceList 向导 vs QuickCreateDeviceDialog）

- 候选：两个入口都在建同一种实体：
  - `EdgeDeviceList.vue:291-599`：4 步 `el-steps`（`历史数据继承/选择设备型号/选择节点&通道/基本信息`），脚本 `:1472 handleCreate`
  - `QuickCreateDeviceDialog.vue:1-151`：单页表单（型号/通道/设备地址/名称/采集间隔/逐指令间隔/继承折叠区），脚本 `handleSubmit`；被 `NodeOverview.vue:718` 与死的 `NodeDetail.vue` import
- 收益：两者都已 import 同一个 `CreateWizardCommandIntervals`（`EdgeDeviceList.vue:661`、`QuickCreateDeviceDialog.vue:147`）与 `LogicalDeviceCandidateSelect`（`:660`、`:146`），说明「命令间隔/继承候选」已抽；但向导的型号/通道选择、地址规则、提交体仍各写一遍。若抽共享表单模型/composable，可避免只修一处的字段校验漂移（R1 的 `deviceAddress` 修复当前两处都在）。
- 风险：向导的步骤状态机与「编辑态复用同一对话框」耦合在 `EdgeDeviceList` 内（`editingDeviceId`、`createStep`、`before-close`），抽组件要同时迁状态；QuickCreate 的「节点内直接创建、无配置模板」路径依赖父页节点上下文。
- 反证：两入口在信息密度上是刻意不同的（列表页 4 步可回退 vs 节点页一张表单即提交），产品上未必应合一。最小动作应是抽「地址规则 + command intervals 提交模型」，不是合页面。

### C4. 删除确认四套实现收敛

> **执行回写（2026-09-23 后续批次，逐项复测）**：本项的**前提已部分失效**，收敛比审计时好得多。
> 复测结果（可复跑）：
> - 生产页面**裸 `ElMessageBox.confirm` = 0 处**（`grep -rn "ElMessageBox\.confirm(" views components --include=*.vue | grep -v __tests__ | grep -vE ":\s*//"`）。
>   仅剩的两处 `ElMessageBox.confirm(` 在 `utils/feedback.ts:150,190` —— 那是**中央封装自身**
>   （`confirmDanger` 与 `confirm` 两个出口的实现），设计如此不是缺陷。
> - `feedback.confirmDanger` 生产使用 **13 个文件 / 22 处调用**，其中 **1 处落在死文件
>   `views/node/NodeDetail.vue`** ⇒ **可达生产面为 12 文件 / 21 处**。
>   （文件：`grep -rln confirmDanger views components --include=*.vue | grep -v /__tests__/` → 13；
>   调用：同上把 `-rln` 换成 `-rn` → 22。注意 `-v __tests__` 与 `-v /__tests__/` 的差别：
>   路径含目录，必须用后者才排除干净 —— 本轮实测踩过这个坑。）
> - 因此「4 套实现并行」现况为：**① confirmDanger（主路径）+ ② 专用对话框（DeviceDeleteDialog /
>   DeviceBatchDeleteDialog / DeviceHeader 的详情页删除）**，后两者已被门禁登记为**结构性豁免**
>   （`listPageConventions.spec.ts:419-423`：它们比 confirmDanger 提供更多信息，**不应**替换）。
> - 即本项**不需要"收敛 4 套 → 1 套"**；剩下的只有 #2/#4 的内部合并（详情页与列表页的
>   设备删除对话框统一），属**可选项**，收益约 84 行，风险见原文（须保留
>   `assertSessionGeneration`/`invalidate` 防竞态）。
> **结论修正**：C4 从「四套确认收敛」降级为「详情页删除对话框与列表页的合并（可选）」，
> 不再是独立整改项 —— 审计原文的"四套"包含了中央封装自身，属口径放大。

- 构成：全站确认删除有 **4 套**实现：
  1. `feedback.confirmDanger` 统一出口：`src/views`+`src/components` 内 `.vue` 命中 **17 处 / 11 文件**（另 `utils/feedback.ts` 定义、`dev/BmsDemoPage.vue` 演示各 1 处；与 09-13 文档 `:64` 的 21 处口径差异主要来自是否含 `utils/feedback.ts` 定义、`dev/` 与是否把注释算入，见 §5-5）
  2. `DeviceDeleteDialog.vue`（295 行，单设备，含 `dataAction` radio）
  3. `DeviceBatchDeleteDialog.vue`（126 行，批量，含 `dataAction` radio）
  4. `DeviceHeader.vue:76-84` 自写 `el-dialog title="删除边缘设备"`，只留 `submitDelete`（定义 `:160`），没有 `dataAction` 选项
- 证据：三文件 `grep dataAction`（单个 `:116,:130,:161`；批量 `:70,:73,:81`）与 `DeviceHeader.vue:76`；消费点：列表两文件在 `EdgeDeviceList.vue:600/608`，详情页删除在 `DeviceHeader.vue:76`。
- 收益：合并 #2/#4（再评估 #3）可消掉 `DeviceHeader.vue:51-84` 约 34 行模板与 `:120-170` 约 50 行脚本，并统一「删除是否带历史数据」的询问；同时把「删除设备时是否删历史数据」这个必选项在详情页也一并提供（当前详情页直接删、不询问，功能不一致）。
- 风险：详情页删除设备时可能本就没有历史数据概念/权限；`DeviceHeader` 的删除流程带 `assertSessionGeneration`/`invalidate` 防竞态（`:158-170`），合并时必须保留。
- 反证：`listPageConventions.spec.ts:382-392` 明确把 `DeviceDeleteDialog` 记为「结构性豁免，比 confirmDanger 提供更多信息，不应替换」——所以**不能**合并到 confirmDanger，只能 #2/#4 内部合并（#3 为批量变体，见 C4 反证）。批量版也有「逐台查询成本不可控」的注释（`DeviceBatchDeleteDialog.vue:66-68`），信息量大于单个版。

### C5. 死组件清理：9 文件 4629 行（含测试维护面）

> **⚠️ 口径已过期（2026-09-23 后续批次实测更正）**：下文「9 文件 4629 行」是**审计时刻**的数字。
> 同一轮修复里 **E1 把 GPIO/PWM 直控接到了生产可达入口**（NodeOverview 新增「外设控制」TAB，
> 经 `PeripheralControl` 消费 `GPIOResourceList`/`PWMResourceList`），因此其中 3 个文件
> **已不再是死代码**。复测后仍不可达的是 **6 文件 4065 行**：
> `NodeDetail(1272) + ChannelPanel(1967) + GPIOPinRow(182) + PWMChannelRow(205) + GaugeChart(88) + ParserBrowser(351)`。
> 其中 `GaugeChart` 已删除（零引用、无 spec、设计文档无计划），故当前为 **5 文件 3977 行**。
> 复跑判据：对每个文件名做 `grep -rn '<Name>' src e2e tools`（排除 `components.d.ts` 与 `__tests__`），
> 再逐文件确认是否存在 `from '...<Name>.vue'` 形式的生产 import。
> **教训（与本仓"记录是缓存不是事实"同源）**：审计数字在**同轮修复**中就会失效——
> 排期时必须以复测为准，不能引用审计原文。

> 口径更正：4629 行 = `NodeDetail(1272)+ChannelPanel(1967)+PeripheralControl(163)+GPIOPinRow(182)+PWMChannelRow(205)+GPIOResourceList(180)+PWMResourceList(221)+GaugeChart(88)+ParserBrowser(351)`，其中 views 域 1272 行、components 域 3357 行；占 views+components 总行数 33046 的 **14.0%**。合计 9 个 `.vue`，不是 10 个。
>
> **分母口径限定（复核 2026-09-23 补充）**：14.0% 的分母 **仅是 views+components 的 `.vue`**（21418+11628=33046），不是 R1 定义的「前端生产全量」（164 个 `.ts/.vue`，44628 行）。**按后者计为 10.4%**（复跑：`find frontend-shared/src -type f \( -name '*.ts' -o -name '*.vue' \) ! -path '*__tests__*' | xargs wc -l | tail -1` → 44628，其中随本轮修复增至 45034）。
> 引用该比例时须写明分母，否则「前端 14.0% 的代码量」会被读成整个前端。

- 候选文件（行）：`NodeDetail.vue(1272)`、`ChannelPanel.vue(1967)`、`PeripheralControl(163)`、`GPIOPinRow(182)`、`PWMChannelRow(205)`、`GPIOResourceList(180)`、`PWMResourceList(221)`、`GaugeChart(88)`、`ParserBrowser(351)` = **4629 行**。
- 证据：§1.3 路由/import 链；`ChannelPanel.vue:5-15` 自述不可达；`GPIOPinRow/PWMChannelRow/PeripheralControl/GaugeChart/ParserBrowser` 模板标签 + import 双 0（§3.1）。
- 收益：直接减少 **14.0%** 的 views+components 总行数（4629 / (21418+11628) = 0.140；分母限定同上，按前端生产全量为 10.4%）；删 `NodeDetail` 后可再删其专属 spec（`NodeDetail.spec.ts`、`NodeDetailMobileResponsive.spec.ts`、`OtaStatusTruthSource.spec.ts`、`ClickableSemantics.spec.ts` 中引用它的部分），减少后续重构误导。
- 风险：`NodeDetail.spec.ts` 等用例断言的是「NodeOverview 与该文件同款的防竞态/点击语义」（`NodeOverview.vue:847`「NodeDetail 模式」），直接删测试可能把语义守卫去掉；`ChannelPanel` 的 4 个编辑 spec 也可能覆盖 ChannelManager 的边界（`ChannelPanelEdit/PeriphEdit/UartReconfigure/Route`）。
- 反证：`ChannelPanel.vue:18-19` 明写「仍被单测 mount，须能编译」；若清理，应先确认这些用例已迁移到 `ChannelManager` 的 spec，再删文件，否则会丢覆盖（当前 `ChannelManager` 已有 `ChannelManagerHardwareTypeCase.spec.ts`/`ChannelManager.spec.ts` 两个 spec，但不能证明等价）。

### C6. `NodeDetail`/`NodeOverview` 同组件双路由确立规范 URL

- 候选：只保留 `node/:id/overview`（推荐，全站内部已有 `DeviceInfoCard` 在用）作为规范 URL，给 `/node/:id` 加 redirect。
- 证据：`router/index.ts:48-50` 与 `:54-56` 同组件；消费点分裂——`DeviceInfoCard.vue:28,63` 推 `/overview`，`ChannelList.vue:443-444` 推 `name: 'NodeDetail'`（无 `/overview`）；`NodeOverview.vue:1770` 注释「本页路由就是 `/node/:id`」。
- 收益：消除两套 URL 入口事实源，activeMenu/面包屑/埋点只需一套；也消除「NodeDetail 名字实际指向 NodeOverview」的误导（`ChannelPanel.vue:5-7` 已被此误导）。
- 风险：旧 URL 可能被外部书签/通知/后端消息引用（本次只 read 前端，无法证明无外部引用）；直接用 alias 而非 redirect 会让同组件双 URL 继续存在。
- 反证：若外部确有 `/node/:id` 深链，直接删会 404；最小动作 = 保留 alias 但让全站内部一律用 `/node/:id/overview`，并把 `ChannelList.vue:444` 改为显式 path。

### C7. 选择器命名/状态映射重复：`statusLabel`/`statusTagType`/`statusText`

- 候选：在线/离线/异常状态文案与颜色映射至少 **4 份**：`NodeList.vue:119,206` 三元写法；`NodeDetail.vue:577` `if offline`；`NodeOverview.vue:1407` `statusMap`；`EdgeDeviceList.vue:1340-1360` `statusTagType`+`statusLabel`；`DataSourceList.vue:411` `statusTagType`。
- 收益：统一 `status → { label, tagType }` 映射后，同类页面不再各写三元；也便于把「警告/故障/已禁用」的色义一致化。
- 风险：各实体状态枚举不同（node: online/offline；edge_device: active/online/offline/warning/error/disabled；data source: healthy/degraded...），强行一个 map 会造成错误映射。
- 反证：`statusLabel` 的 `active → 在线` 分支只在 EdgeDevice 存在，Node 没有；合并需先统一后端枚举，属跨层工作。

### C8. 超过 300 行的文件拆分优先级（脚本排序）

- 优先级排序（按「行数 × 死代码/页面角色」）：
  1. `EdgeDeviceList.vue` 2482 行：template 1-617 / script 619-1871 / style 1873-end——内含 4 步创建向导（对话框 `:290-597`，步骤条 `:301-306`）+ 卡片/表格双视图 + 统计 + 筛选，至少三块可拆；
  2. `NodeOverview.vue` 2431 行：template 1-794 / script 796-1871 / style 1873-end——**7 个 tab**（`:900-907` 的 `tabs`）是天然拆分线：`:147/:309/:574/:605/:663/:705/:713` 分别对应「基本信息/总线配置/DMA 通道/关联设备/OTA 历史/系统日志/通道终端」；
  3. `NodeList.vue` 976 / `LogicalDeviceList.vue` 968 / `DeviceConfigList.vue` 860 / `AutomationRules.vue` 850 / `DataSourceList.vue` 846：列表页家族，应统一按 §4-C1 抽壳；
  4. `MainLayout.vue` 1474：template 256 / script 603 / style 614，菜单+通知+布局+快捷键混一层；
  5. `ChannelManager.vue` 764 / `ChannelTerminal.vue` 684：通道域两个大组件，但复用点只有 NodeOverview/ChannelPanel，优先级低于列表页。
- 证据：`NodeOverview.vue` tab 分界行全部为 `grep -n 'template v-else-if="activeTab'` 命中（见上）；`NodeOverview.vue:794` 结束 template；拆 5 个非默认 tab 即可回收 template 794 行中的约 640 行。
- 收益：拆出 5 个 tab 子组件即约回收 template 794 行中的 640 行（`:147` 之外 6 段合计），脚本 1076 行可按 tab 拆 composable；这是本次最大单体收益。
- 风险：`NodeOverview` 的状态（`channels/node/activeBusType/selectedResourceId` 等 `:857-898`）在 tab 间共享（如 `editChannel` 由通道健康卡与总线卡两处调用），拆组件需先理清 props/emit，否则 prop 爆炸。
- 反证：`NodeOverview` 最近有大量针对性修复（D1/D2/D4 注释、`:1760-1790` 导航修复），拆分会移动行号、打乱 `NodeOverviewPalette.spec.ts` 等用例锚点；应作为独立重构批次，不与功能修复混做。

### C9. 菜单分组重构：14 平铺项 → 5 组 `el-sub-menu`

- 候选：按「总览 / 资源（节点、边缘设备、逻辑设备、通道、数据源）/ 数据与规则（数据面板、告警、自动化、通知）/ 配置（固件、配置模板）/ 系统（监控）」分 5 组，默认展开当前组。
- 证据：`MainLayout.vue:397-412` 14 项平铺，`grep -n 'el-sub-menu'` = 0（无分组能力）；菜单项 2 项图标重复（`Connection` ×2 `:399,:401`；`Bell` ×2 `:406,:409`）。
- 收益：单侧栏 14 项在折叠态只显图标，重复图标导致无法区分（`Connection` 对应节点/通道，`Bell` 对应告警规则/通知通道）；分组后折叠态可由组图标+悬浮子菜单承担；移动抽屉 14 项纵向滚动也更短。
- 风险：`MainLayoutKeyboardNav.spec.ts`/`MainLayoutDrawerFocusTrap.spec.ts`/`TouchTargets.spec.ts` 与 e2e `uiux-regression.spec.ts:809` 都基于 `.sidebar .el-menu-item` 平铺列表（`uiux-metrics.ts:256-264` 数菜单项个数）；改 `el-sub-menu` 会改变 DOM 与可聚焦序列，直接打破既有门禁。
- 反证：F7 的门禁（09-13 文档当时口径「侧栏 12 个菜单项」，2026-09-23 实测已是 14 项）正是围绕平铺 `el-menu-item` 写的（`UIUX审计合并结论与优化方案-2026-09-13.md:68`）；在门禁改造为 submenu-aware 之前不动菜单，否则用测试假红换视觉收益。

### C10. 通知域两页合并为「通知」双 tab 页面

- 候选：`NotificationChannels.vue`（388 行）+ `NotificationDeliveries.vue`（387 行）= 775 行合并为 1 个路由页 + 2 个 tab（配置 / 投递审计），共享 channel_id 过滤参数。
- 证据：两文件同目录、同 388/387 行、同有 `PageHeader`+`el-table`+`el-pagination`+`EmptyState`（§2 行）；`NotificationDeliveries` 的路由注释自述「与通知通道同域相邻」（`router/index.ts:133`）；`NotificationChannels.vue:143` 的表单对话框只服务通道页。
- 收益：菜单 14→13，通知域入口从 2 个变 1 个；投递审计的 `channel_id` 过滤可直接从前一 tab 选中通道带过去（现在跨页无法带参）。
- 风险：两页的 API/分页/列宽都独立，合并要把两套表格塞进一个 page；`NotificationChannels` 有创建/删除动作，`Deliveries` 是只读审计，权限语义不同。
- 反证：投递审计是「P2-A 只读审计，与通道相邻」的独立交付，历史上刻意拆成两条路由；若审计需要独立 URL 给外部系统，合并反而破坏深链。建议保留双路由，仅在菜单上相邻呈现。

### C11. `DeviceHeader` 内嵌编辑/删除与列表页对话框的第二套实现（C4 的第 4 套）

- 候选：`DeviceHeader.vue:51-84` 自带 `el-dialog` 编辑设备名称/删除设备，并直接调 `edgeDeviceApi.update`（`:126`）/删除（`submitDelete` 后续）——与 `EdgeDeviceList` 的表单/对话框是两套实现。
- 证据：`grep -rn "edgeDeviceApi.update"`：`DeviceHeader.vue:126`；`EdgeDeviceList.vue` 的编辑走 `showCreateDialog` + `editingDeviceId`（`:846/:1390-1405`）。详情页删除走 `DeviceHeader.vue:76`，列表页走 `DeviceDeleteDialog.vue`（含 `dataAction`）。
- 收益：抽「设备基本信息编辑表单」供两处复用，可消掉 `DeviceHeader` 的 `editForm/submitEdit` 约 50 行，并把「编辑不提示冲突、删除不问历史数据」两处行为差异收口。
- 风险：`DeviceHeader` 的保存带 session generation 断言与陈旧请求防护（`:129-136`），列表页向导还承担创建态与模板，抽表单要先保留这些语义。
- 反证：详情页编辑只改名称（其余字段 disabled，`:55-70`），列表页向导承担完整创建/编辑；共享一个「只读字段 + 名称」的轻表单最合适，不必共享整个向导。

### C12. 通知/告警「级别 → tag type」映射与状态标签组件化

- 候选：`StatusBadge.vue`（64 行，3 消费点）已存在，但大量页面仍裸写 `el-tag type="success"`（`grep` 命中 16+ 文件）与局部 `levelTagType`（`NotificationChannels.vue:335`）、`resultTagType`（`AutomationRules.vue:803`）、`tempLevel`（`BmsDetailPage.vue:205`）。
- 证据：§2 矩阵各页 `el-tag` 命中；`StatusBadge.vue` 只被 `DeviceInfoCard/NodeDetail/NodeOverview` 消费，覆盖率极低。
- 收益：把「状态 → 视觉」收敛到 1 个组件，可在 status/level/result 三类场景各减 5~10 行判断，并统一 aria/文本。
- 风险：各域标签的语义层级不同（设备状态=运行状态，告警级别=严重度，自动化结果=执行结果），强行一个枚举会降低可读性。
- 反证：`StatusBadge` 当前 props 若不足以表达 `levelTagType`/`resultTagType` 的语义，强行泛化会得到一个 `any` props 组件；建议按域保留映射，仅消费 `StatusBadge` 的展现层。

### C13. 未清理的 `components.d.ts` 注册面 vs 真实消费面

- 候选：不要因为 `components.d.ts` 列出了 40 个组件就认为都能用；把 5 个死组件在自动注册 `dirs` 中排除（`vite.config.ts:33-38`）或移到 `src/legacy/`，让「自动注册名单 == 真实可达组件」。
- 证据：`vite.config.ts:33-38` `dirs: ['src/components']` 扫描全部 40 个 `.vue`，生成 40 条项目组件声明；其中 5 个零消费组件仍各有一条声明（`GaugeChart`/`GPIOPinRow`/`ParserBrowser`/`PeripheralControl`/`PWMChannelRow` 分别在 `components.d.ts:88/89/103/104/105`）。
- 收益：自动注册声明面从 40 降到 35，消除「类型里存在即可用」的误导；也避免 IDE 补全推荐死组件。
- 风险：`components.d.ts` 是构建生成物（`UIUX审计合并结论与优化方案-2026-09-13.md:131` 记录主控核实其时间戳来源），不应手改；改变 `dirs` 范围会同时改变自动注册能力，可能误伤既有测试里无 import 的组件挂载。
- 反证：40 个组件的自动注册对产物无直接影响（未消费不会被打包）；死组件清理的收益主要在维护面而非包体，优先级低于 C5。

## 5. 与 2026-09-13 已闭环项去重（只列「不再报」清单）

以下问题在 `docs/分析/UIUX审计合并结论与优化方案-2026-09-13.md`（§3 回写、§8 裁决）已有产物或已裁决，本报告**不重复报告**，只登记避免下游误以为遗漏：

| # | 闭环项（该文档锚点） | 处置状态 |
|---|---|---|
| 1 | 失败态三件套统一（EmptyState kind=error 部分接线） | 部分闭环；`device-configs` 首屏无骨架、7 页未接错误态（该文档 `:56/:76`）。本报告 §4-C2 只在「加载/空态收敛」候选里引用，不再单独立缺陷。 |
| 2 | OTA `target_model` 字段静默丢失 | 已闭环（`:58`，后端修复）。不再报。 |
| 3 | automation/logical-device 假分页 `Limit(500)` | 已闭环（`:60`，真分页 + 前端 `el-pagination`）。本报告不重复报。 |
| 4 | 对比度 token 层修复（F4） | 已裁决且主体闭环（`:62/:112`，token 已换）。本报告不重复报；`DeviceConfigList` 等页面颜色不在本次统计范围。 |
| 5 | `confirmDanger` 统一出口（F5） | 已闭环（`:64`，21 处 confirmDanger / 9 处裸 `ElMessageBox.confirm`）。本次复跑 views+components 17 处（`grep -rn confirmDanger src/views src/components --include=*.vue | grep -v __tests__ | wc -l`），与 09-13 基线同量级（基线含定义/DEV，见 §4-C4 脚注），不重复报 F5。 |
| 6 | `@media (pointer: coarse)` ≥44px 触控（F6） | 已闭环（`:66`，2 规则块 + 30 用例）。不重复报。 |
| 7 | 侧栏 roving tabindex 键盘可达（F7） | 已闭环（`:68`，MainLayout roving + 门禁）。本报告 §4-C9 只提示改 submenu 会破坏该门禁，不重报 F7。 |
| 8 | `mobile-table-wrapper` 横滑提示（F8/F10） | 已闭环（`:70`，仅 `Monitor/MainLayout` 非列表页例外）。不重复报。 |
| 9 | `DeviceConfigList` `.filter-right` 裁切（F9） | 已闭环（`:72`，flex-wrap）。不重复报。 |
| 10 | 移动端面包屑显示（F10 主体） | 已闭环（`:74`）。不重复报。 |
| 11 | `device-configs` 首屏骨架缺口 | 已登记为「部分闭环待排期」（`:76`）。本报告 §4-C2 引用，不再单列缺陷。 |
| 12 | `DeviceConfigForm` create/edit 字段抽取 | 已闭环（`:78`，组件 + 用例）。不重复报。 |
| 13 | `DataSourceList` 详情抽屉宽度 | 已闭环（`:80`）。不重复报。 |
| 14 | 统计卡范围词（本页/当前筛选/全局） | 已闭环（`:82`，5 用例）。不重复报。 |
| 15 | WS URL 断言 `toContain → toBe` | 已闭环（`:84`）。不重复报。 |
| 16 | 侧栏渐变硬编码（F16） | 已闭环（`:86`）。不重复报。 |
| 17 | `Monitor` `THEME_COLORS` 静态色（F17） | 已闭环（`:88`）。不重复报。 |
| 18 | 命令 ID 死链、el-switch 名、404/403 文案、术语「设备模板」、占位符、骨架几何（7 子项） | 已闭环（`:90`）。不重复报；注意其 ④「403 硬编码系统管理员」已修，但 `/403` 路由仍无入口，这属本报告 §1.3-5「有页面无入口」，不是同一条。 |
| 19 | 文档 §6 的 7 条存量偏差中已闭环 2 条 | 不逐条复述（`:139`）。本报告全部候选为**新识别**的元信息架构问题。 |

> 本报告与 09-13 文档的重叠只有：菜单/侧栏、失败态、确认出口、`node/:id` 双路由。前四者分别按「F7 已闭环（不重报）/ C2 引用 / C4 以其为基线 / 未在 09-13 出现」处理。

## 6. 口径、方法与不确定性

### 6.1 数量口径差异（任务书 38 组件 vs 实测 40）

- 任务书写 `src/components` 38 个 `.vue`；实测 `find src/components -name '*.vue' -not -path '*/__tests__/*'` = **40**。差异 2 个，本报告按实测 40 输出（清单见 §3 全 40 行），不猜测哪 2 个是任务书排除项。
- views 41 与任务书一致；`src/dev/MockBmsPanel.vue`、`src/dev/BmsDemoPage.vue` 未计入 views 41（按任务书排除 dev）。
- `components.d.ts` 自动注册声明 105 条 `typeof import`（含 Element Plus 组件），其中项目组件 40 条，与 §3 一一对应。

### 6.2 本报告可复跑的统计命令

```bash
# 页面数 / 行数
find frontend-shared/src/views -name '*.vue' -not -path '*/__tests__/*' -not -path '*/dev/*' | wc -l
find frontend-shared/src/views -name '*.vue' -not -path '*/__tests__/*' -not -path '*/dev/*' | xargs wc -l | tail -1
# 控件存在性（首命中行号）
grep -n 'el-table\b' frontend-shared/src/views/x.vue | head -1
# 组件真实消费点（排除测试/声明/自身）
grep -rnE "^[[:space:]]*import[^'\"]*['\"][^'\"]*$NAME(\.vue)?['\"]" frontend-shared/src --include=*.vue --include=*.ts | grep -v __tests__ | grep -v components.d.ts
# 死组件二次确认（import + 模板双 0）
grep -rE "<GaugeChart[ >/]" frontend-shared/src --include=*.vue | grep -v __tests__
```

### 6.3 未验证/不确定项（避免把 grep 当运行时）

1. **未跑任何测试/构建/Playwright/网络**：本报告所有「可达/不可达」是源码导入图判定，不是运行时路由实测。`ChannelPanel` 不可达另有其自身 2026-09-20 实机核验注释（`ChannelPanel.vue:10-11`），可信度高于本报告其余推断。
2. **模板标签消费识别有边界**：`unplugin-vue-components` 的 `dirs` 自动注册（`vite.config.ts:33-38`）使组件可无 import 直接使用；本报告对每个零 import 组件都做了模板标签二次检索（`<Name` 或 kebab-case 变体未穷尽，如 `<gauge-chart>` 未单独查；但 `grep -rhoE '<([A-Z][A-Za-z0-9]+)'` 未在 views 中出现这些名字）。
3. **`components.d.ts` 可能过期**：它是构建生成物（09-13 文档 `:131` 记录主控核实时间戳来源）。本报告用它确认「40 个组件被扫描」的事实，不把它当作运行时可达证明。
4. **文件行数拆分**：`EdgeDeviceList.vue:1-617` 模板 617 行内还有 `<template v-if="loading">` 嵌套模板（`:3`），§4-C8 的 617 行不是单一 DOM 块。
5. **菜单 count 15 vs 14**：`MainLayout.vue` 中 `grep -c "{ path: '/"` 得 15，其中多出的一项是面包屑 `:128` 的 `:to="{ path: '/dashboard' }"`；实际 `allMenuItems` = `:398-411` 共 14 项。本报告全部按 14 项计。
6. **路由 count 26 vs 25**：裸 `grep -c "path:"` 得多出的第 26 条是守卫里的重定向 `router/index.ts:201 return { path: '/login', ... }`；按行首缩进的 `path:` 精确匹配得 **25** 条路由记录（`grep -nE "^[[:space:]]+path:"`，含 DEV 2 条、通配 1 条）。`path:` 行号清单见 §1.1 脚注，逐条可核。
7. **`/403` 可达性可能来自后端重定向**：全仓前端无 `router.push('/403')`（§1.3-5），但不能排除反向代理/后端 302 到该路径；本次无网络，无法验证。
8. **性能/包体收益未量化**：没有构建产物，无法给出删死组件后的 KB 数；§4 收益全部是行数与消费点数。

## 7. 建议实施顺序（纯提案，不含排期）

1. **P0（低风险、机械可验证）**：C5 死代码清理（先迁移/确认 5 个 spec）→ C6 双路由 redirect → C13 自动注册范围。
2. **P1（模板层，需设计确认）**：C2 加载/空态约定 → C1 列表壳抽取 → C3 创建/编辑抽取（先抽模型再抽 UI）。
3. **P2（体验，门禁联动）**：C4 删除确认收敛 → C11 详情编辑/删除收敛 → C8 大文件按 tab/列表家族拆分。
4. **P3（需先改门禁）**：C9 菜单分组（先 submenu-aware 化 `MainLayoutKeyboardNav`/`MainLayoutDrawerFocusTrap`/`TouchTargets` 与 e2e `uiux-metrics`）→ C10 通知双 tab（保留审计独立 URL）→ C7/C12 状态映射收敛（需后端枚举先统一）。
