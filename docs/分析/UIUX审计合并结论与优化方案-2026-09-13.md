# UI/UX 全站审计合并结论与优化方案（2026-09-13）

> **状态**: 待执行（四域审计完成，主控已独立复核关键项）
> **方法**: [UIUX审计契约-2026-09-13.md](UIUX审计契约-2026-09-13.md)（三合一取证：截图 + DOM 度量 + 代码锚点）
> **规范**: [../规范/前端开发与UIUX设计规范.md](../规范/前端开发与UIUX设计规范.md)（唯一权威）
> **分域报告**: D1 / D2 / D3 / D4 四份，共 3882 行，474 张截图

---

## 1. 覆盖与产出

| 域 | 报告 | 条数 | 阻断 | 高 | 中 | 低 |
|---|---|---|---|---|---|---|
| D1 数据分析（仪表盘/数据面板/监控） | `UIUX审计-数据分析域-2026-09-13.md`（1029 行） | 14 | 1 | 5 | 5 | 3 |
| D2 资源列表与详情（节点/通道/边缘设备/逻辑设备） | `UIUX审计报告-D2-资源列表与详情页.md`（784 行） | 17 | 1 | 8 | 6 | 2 |
| D3 配置与管理（自动化/告警/配置/数据源/固件） | `UIUX审计报告-配置与管理类页面-D3D5.md`（1313 行） | 26 | 1 | 8 | 11 | 6 |
| D4 全局骨架与可访问性（登录/布局/个人/错误页） | `UIUX审计报告-全局骨架与可访问性-D2D4D6D7.md`（565 行） | 19 | 0 | 4 | 11 | 4 |
| **合计（含跨域重复计分）** | | **76** | **3** | **25** | **33** | **15** |

**四域一致收敛到同一批根因**，这正是去重后的问题族（§3）。

## 2. 三个阻断项（P0，主控全部独立复现）

### U-1 接口失败伪装成"一切正常"（D1 域，主控复现）
拦截 `/api/v1/overview` 返回 500 → `/dashboard` 4 个 KPI 全渲染 **0**，异常摘要卡显示
**绿色对勾 +「运行正常」+「节点与设备均在线，暂无采集错误。」**，无任何错误态。
`alertOkIconColor=rgb(103,194,58)`、`kpiValues=["0","0","0","0"]`、`elResult=0`。

**同族已在 6 个页面复现**（D2 域）：node-list / edge-device-list / device-configs 的 4 个 KPI 全 0 +
空态冒充；logical-device / channel 空态冒充。**D2 域实测 `okWordHits=[]`、`greenSummaryEls=[]`** ——
即"绿色对勾"是 dashboard 独有，其他页是"0 指标 + 正常空态"，**同因不同形**。

**对照组（同仓已有范式）**：`/node/1` 显示「节点不存在或加载失败」+ 重试；
`/edge-device/:id` 有 `errorStateCount=1` + el-result + 重试；**data-sources 是唯一正确处理错误的列表页**
（顶部 el-alert + 重试、KPI 显示 `—`）→ **修法已有现成参照，不需要新设计**。

根因模式三件套（D2/D3 逐页核实）：
1. `|| 0` 数值回退把"未知"写成 0（`NodeList.vue:371-376`、`DeviceConfigList.vue:316`）
2. `v-else` 无条件渲染"正常/无异常"摘要（`Dashboard.vue:98-101`）
3. `catch` 只弹 `ElMessage` 不置错误态（`LogicalDeviceList.vue:308-317`、`stores/alert.ts:27-35` 只有 finally 没有 catch）

### U-2 固件「目标型号」编辑静默失效（D3 域，主控真实 API 闭环）
前端发 `target_model`，后端 `handler_ota.go:220` 读 `node_model`，`ShouldBindJSON` 返回值被丢弃 → 返回 200 但数据未变。
主控实测：A 发 `target_model` → 200 且值仍为 `MODEL-ORIG`；B 发 `node_model` → 值变为 `MODEL-VIA-NODE-MODEL`。
**截图/DOM/控制台全部看不出异常** —— 只有真实 API 闭环能抓到。

### U-3 自动化触发历史显示裸主键（D3 域）
`AutomationRules.vue:76` 用 `prop="rule_id"` → 显示 `1`/`3`，而上方规则名为「S2-低电强制断充」「S1-SOC低开灯」。
**同页筛选下拉已经用 `r.name`**（`:63-65`），即页面早有映射能力。

## 3. 去重后的问题族（按根因归并，这是执行单元）

| 族 | 问题 | 涉及域 | 规范 | 优先级 |
|---|---|---|---|---|
| **F1 失败态缺失** | U-1 三件套（`|| 0` / 无条件"正常" / catch 不置错态） | 6+ 页面，D1/D2/D3 | §1.2.1、§3.4.2、§3.2.5 | **P0** |
> **2026-09-16 回写**：**部分闭环**。EmptyState.vue:50 已支持 kind 取 error；Dashboard / EdgeDeviceList / ChannelList / data-source / Monitor 已接线错误态+重试（Monitor.spec.ts 38 用例含「F28 失败态不得伪装成 0（A/B 可区分）」；Monitor.vue:410 明写缺陷与修法）。但 NodeList.vue / DeviceConfigList.vue / LogicalDeviceList.vue / DataPanel.vue / AlertRules.vue / FirmwareManage.vue / AutomationRules.vue 实测 kind=error 命中 0、hasError|loadError|errorState 命中 0 ⇒ 「三件套」并未全站收敛。复跑：grep -cE "kind=\"error\"|hasError|loadError|errorState" frontend-shared/src/views/某页面.vue。产物：frontend-shared/src/views/monitor/__tests__/Monitor.spec.ts、frontend-shared/src/components/common/EmptyState.vue。
| **F2 静默数据丢失** | U-2 字段名不匹配 + `ShouldBindJSON` 返回值丢弃 | D3 | §3.2.3 | **P0** |
> **2026-09-16 回写**：**已闭环**。backend/internal/api/handler_ota.go:216-239 已把字段 tag 改为 target_model（注释明写 previous node_model tag silently dropped the value sent by the UI while still answering 200），并恢复 if err := c.ShouldBindJSON(&req); err != nil { Error(c, http.StatusBadRequest, err.Error()); return }。复跑：grep -n "target_model\|ShouldBindJSON" backend/internal/api/handler_ota.go。产物：backend/internal/api/handler_ota.go。边界：全仓仍有多处 ShouldBindJSON（属 P1-B 分母）未逐条判定，不计入 F2 本体。
| **F3 分页缺失导致 DOM 爆炸** | automation 502 行/11902 节点；logical-device 1003 行/28541 节点；后端 `Limit(500)` 硬截断（交叉筛选实测真实 >1006 条） | D1/D2/D3 | §4.5.4、§3.3.5 | **P1** |
> **2026-09-16 回写**：**已闭环**。后端改真分页：handler_automation.go:484 用 Offset((page-1)*pageSize).Limit(pageSize)（注释记录「曾硬编码 Limit(500) 静默截断，实测真实 >1006 条」）；handler_logical_device.go:69 同形；handler_data_source.go:140 与 handler_device.go:216 均有 page_size（默认 20，越界归 20）。前端 AutomationRules.vue:154 与 LogicalDeviceList.vue:110 已挂 el-pagination。复跑：grep -n "Offset\|Limit\|page_size" backend/internal/api/handler_automation.go backend/internal/api/handler_logical_device.go。产物：上述后端文件 + frontend-shared/src/views/__tests__/listPageConventions.spec.ts（11 用例全绿）。
| **F4 亮色对比度系统性不达标** | 跨四域一致：WS 徽标 2.08、`el-tag--success` 2.08、`el-tag--warning` 2.04、`.el-button--primary` 白字 2.78、表头/统计标签/空态文案 3.08、离线徽标 `#909399`+`#f5f7fa` = 2.87。**暗色仅 4 处** | 全部四域 | §4.2.1、§4.5.1 | **P1** |
> **2026-09-16 回写**：**部分闭环（未复跑对比度）**。theme.css:18-50 亮色基色已整体替换：--color-primary #1f5ad8、--color-success #147a3a、--color-warning #9a5b06、--color-danger #b91c1c、--color-info #5e6673，注释记录实测白字对比度 5.98 / 5.42 / 5.42 / 6.47 / 5.79，改前为 2.78 / 2.08 / 2.04 / 2.61；次级文字 #5e6673（原 #909399 = 3.08）、placeholder #6b7280（原 #a8abb2 = 2.30）。但**对比度没有机械门禁**：src 下的颜色相关 spec 只有 NodeOverviewPalette.spec.ts 与 SidebarThemeTokens.spec.ts，WCAG 阈值脚本仍在 frontend-shared/tools/*.mjs（不在门禁内）⇒ 本次未复算 WCAG，判「部分闭环/未核实」。复跑（本次未执行）：node frontend-shared/tools/theme-contrast-measure.mjs。产物：frontend-shared/src/styles/theme.css。
| **F5 危险确认不规范** | `ElMessageBox.confirm` **20 处仅 1 处用 danger**；且**默认焦点落在"删除"**（诱导性）；`feedback.confirmDanger` 已有定义仅调用 3 次 | 9 个文件 | §3.4.3、§4.3.4 | **P1** |
> **2026-09-16 回写**：**已闭环**。非测试代码中 feedback.confirmDanger **21 处**、裸 ElMessageBox.confirm 仅剩 **9 处**（审计时为 20 处仅 1 处 danger）。utils/feedback.ts:141 实现 confirmDanger，:160 confirmButtonClass 设为 el-button--danger，:164 显式 autofocus:false（注释实测记录：EP 默认 autofocus:true 会把初始焦点交给「确定/删除」，回车即删除）。复跑：grep -rn "confirmDanger" frontend-shared/src --include=*.ts --include=*.vue | grep -v __tests__ | wc -l 得 21；grep -rn "ElMessageBox.confirm" 同法得 9。产物：frontend-shared/src/utils/feedback.ts。
| **F6 触控目标不达标** | automation 264 个 <36px（**257 个是"行高 18px"单一根因**）；行操作 67x24/71x24；input-number 24x11；页头图标 32x32。**全仓 `pointer:coarse` 0 命中**（§4.4.7 完全未实现） | 全部四域 | §4.4.5、§4.4.7 | **P1** |
> **2026-09-16 回写**：**已闭环**。theme.css:795 与 theme.css:944 已有两处 @media (pointer: coarse) 真规则块，块内 min-height:44px 密集出现（页头图标 / 抽屉 / 面包屑链接等），不再是「0 命中」。复跑：npx vitest run src/styles/__tests__/touchTargetsF6.spec.ts src/styles/__tests__/touchTargetsF27.spec.ts src/views/layout/__tests__/TouchTargets.spec.ts → **3 文件 30 用例全绿**（F6 14 + F27 7 + TouchTargets 9）。touchTargetsF6.spec.ts:204 还专门防「注释里写 @media (pointer: coarse) 被当成真规则」的假绿。产物：frontend-shared/src/styles/theme.css、frontend-shared/src/styles/__tests__/touchTargetsF6.spec.ts。边界：源码/规则层判据，未实机 elementFromPoint 量热区。
| **F7 键盘可达性缺失** | 侧栏 12 个菜单项 tabindex=-1（30 次 Tab 进不去）；按钮式卡片/TAB/图标无 role+tabindex+aria（D2 域页内实测：/node 5、/channel 3、/edge-device 7、/node/1 15、/edge-device/7053 16）；移动抽屉无焦点陷阱 | 全部四域 | §3.1.3 | **P1** |
> **2026-09-16 回写**：**已闭环**。MainLayout.vue:30 用 :tabindex="idx === sidebarFocusIndex ? 0 : -1"（roving tabindex，仅当前项可 Tab 进入），:58 移动抽屉根 tabindex="-1"，:546 起抽屉内同样 roving；:466-491 有实现注释。复跑：npx vitest run src/components/common/__tests__/IconActionAccessibilityGate.spec.ts → **6 用例全绿**（含「@click 三件套」+ 12 条登记例外 + 失效条目反向守卫）。产物：frontend-shared/src/views/layout/MainLayout.vue、frontend-shared/src/components/common/__tests__/IconActionAccessibilityGate.spec.ts。边界：源码层判据，未实机 Tab。
| **F8 移动横滚未覆盖** | 含 `el-table` 12 文件仅 4 个用 `.mobile-table-wrapper`；node/edge 表格 `fixedSharePct=80`（@360 达 89%）且无 `hint` | D2/D3 | §4.3.1/§4.3.2 | **P1** |
> **2026-09-16 回写**：**已闭环（剩 2 个非列表页）**。复跑：for f in $(grep -rln "el-table" frontend-shared/src/views --include=*.vue); do grep -q mobile-table-wrapper "$f" || echo MISSING: $f; done → 仅输出 views/monitor/Monitor.vue 与 views/layout/MainLayout.vue（审计时为「12 文件仅 4 个用 wrapper」）。复跑：npx vitest run src/views/__tests__/MobileTableWrapper.spec.ts → **10 用例全绿**，含「:168 被包裹的表同时有 .mobile-table-hint 直接子节点（横滑提示不能只在注释里）」与「:193 仍有缺 hint 的表」两条断言。产物：frontend-shared/src/views/__tests__/MobileTableWrapper.spec.ts。
| **F9 真实响应式裁切** | device-configs @360「导入」左溢出 11px（`impBtn.x=10 < cardBody.x=21`，祖先全部 `scrollable:false`）；**修法一行**：`.filter-right` 加 `flex-wrap: wrap` | D2/D3（同一处） | §4.4.1 | **P1** |
> **2026-09-16 回写**：**已闭环**。DeviceConfigList.vue:671 起 .filter-right 已声明 display:flex; gap:8px; **flex-wrap: wrap**，并保留原始裁切证据注释（「三操作按钮总宽 261px，360px 视口下可用宽仅 270px；祖先 overflow 全 visible/auto 且 scrollWidth===clientWidth ⇒ 真实裁切」）；:794 另补了同根因的 .card-footer { flex-wrap: wrap }。复跑：grep -n -A6 "filter-right" frontend-shared/src/views/config/DeviceConfigList.vue。产物：frontend-shared/src/views/config/DeviceConfigList.vue。边界：本次只读源码，未实机 360px 量像素。
| **F10 移动端导航路径缺失** | 节点详情面包屑 + 顶栏面包屑在 ≤768px **同时** `display:none`（`NodeOverview.vue:2007` + `MainLayout.vue:951-952`）。**主控 390px 实测复核**：两个面包屑 `display:none/w=0/h=0`，且 `NodeOverview.vue:598` 的「返回列表」按钮**未渲染**（`backBtn: null`，它在其他分支里）⇒ 移动端**确实只剩页面标题一条线索**，层级位置感丢失 | D2 | §4.1.2 | **P1** |
> **2026-09-16 回写**：**已闭环（主体）**。NodeOverview.vue:2069 起，≤768px 的 .no-breadcrumb 已从 display:none 改为 display:flex 两段式（:first-child 隐藏、末段 flex:0 1 auto + min-width:6em + 省略号、容器 overflow-x:auto 兜底），注释逐条记录改前证据与「必须用 flex 而非 float」的理由；:620 存在 goBack 的「返回列表」按钮。MainLayout.vue:1265 顶栏 .breadcrumb 在 ≤768px 仍 display:none —— 这是有意让位给页内面包屑，非缺陷回退。复跑：grep -n "breadcrumb" frontend-shared/src/views/node/NodeOverview.vue | head。产物：frontend-shared/src/views/node/NodeOverview.vue。边界：未实机 390px 复核。
| **F11 首次加载/刷新态不规范** | device-configs 首屏**完全空白**（skeleton/mask/cards/emptyState 全 0）；data-sources 空态先于数据 | D1/D3 | §4.3.3 | P2 |
> **2026-09-16 回写**：**部分闭环**。data-sources 已有加载态（DataSourceList.vue:102 表格 v-loading、:314/:328 两条 timeline v-loading）；全站 16 个视图已接 SkeletonCard / el-skeleton。**但 device-configs 首屏仍无骨架**：grep -n "Skeleton\|skeleton\|v-loading" frontend-shared/src/views/config/DeviceConfigList.vue → 0 命中，只有 :188 的 v-if="!loading && filteredConfigs.length === 0" 空态 ⇒ 加载窗口内确实只剩空白。复跑：grep -n "Skeleton\|skeleton\|v-loading" frontend-shared/src/views/config/DeviceConfigList.vue。产物：frontend-shared/src/components/common/SkeletonCard.vue、frontend-shared/src/views/dashboard/__tests__/DashboardSkeletonGeometry.spec.ts（4 用例绿）。
| **F12 编辑对话框泄露创建字段** | device-configs 编辑态显示 cascader「传感器驱动」，`isEdit` **只用于按钮文案**，且 `submitData` 不含 `driverPath` → 改了被静默丢弃 | D3 | §4.3.2.2 | P2 |
> **2026-09-16 回写**：**已闭环**。创建字段已抽到 components/forms/DeviceConfigForm.vue：:28 `<el-form-item v-if="!isEdit" label="传感器驱动" prop="driverPath">`（创建态可编辑级联），:54 `<el-form-item v-else label="传感器驱动">`（编辑态只读展示）⇒ isEdit 不再「只用于按钮文案」。复跑：npx vitest run src/components/forms/__tests__/DeviceConfigForm.spec.ts → **4 用例全绿**（含「创建态渲染可编辑的『传感器驱动』级联选择器」）。产物：frontend-shared/src/components/forms/DeviceConfigForm.vue、frontend-shared/src/components/forms/__tests__/DeviceConfigForm.spec.ts。
| **F13 抽屉/弹窗宽度缺陷** | data-sources 详情抽屉 `size="560px"` 无 max-width → 360px 下 `left=-200`，**4/4 个 label 全在 `left=-179` 不可见**（`theme.css:371` 只兜底 `.el-dialog`，漏了 `.el-drawer`） | D3 | §4.3.2.3 | P2 |
> **2026-09-16 回写**：**已闭环（方案更优）**。未走全局 CSS 兜底，改为组件侧计算：DataSourceList.vue:395 `const DETAIL_DRAWER_SIZE = computed(() => `${Math.min(560, Math.round(viewportWidth.value * 0.92))}px`)`，:290 `:size="DETAIL_DRAWER_SIZE"`；:388-392 逐条写明「为什么不能走全局 .el-drawer 兜底」（el-drawer teleport 到 body + 全局兜底会波及所有抽屉）。复跑：grep -n "DETAIL_DRAWER_SIZE\|el-drawer" frontend-shared/src/views/data-source/DataSourceList.vue。产物：frontend-shared/src/views/data-source/DataSourceList.vue。
| **F14 统计卡无范围标注** | 「本次数据点/采集覆盖时长」实为**当前页 20 条**，与分页器「共 137 条」并排；三页均无范围标注 | D1 | §4.3 统计卡 | P2 |
> **2026-09-16 回写**：**已闭环（并修出三层缺陷）**。三页均引入范围词常量：DataSourceList.vue:362 SCOPE_FILTERED=当前筛选 / :363 SCOPE_PAGE=本页（渲染为「总来源（当前筛选）」「权威（本页）」…）；DeviceConfigList.vue:266-267 同形；EdgeDeviceList.vue:619 SCOPE_GLOBAL=全局（「今日数据（全局）」），且 :14/:19/:24 的 mobile-label 也带范围词（「边缘设备（本页）」等）—— 这正是审计漏掉的移动端形态。复跑：npx vitest run src/components/common/__tests__/statCardScopeGate.spec.ts → **5 用例全绿**。产物：frontend-shared/src/components/common/__tests__/statCardScopeGate.spec.ts。
| **F15 WebSocket 完整 URL 二次追加** | 规范 §6 的 P0 **仍在**；`websocket.spec.ts:172` 的 `toContain` 对缺陷 URL **恒真**（主控用真实 vitest 证明） | D4 | §6 P0 | **P1** |
> **2026-09-16 回写**：**已闭环**。toContain 已全部换成逐字符全等：websocket.spec.ts:137 / :158 / :167 / :178-180，并新增 :196「VITE_WS_URL 输入矩阵（new WebSocket URL 逐字符全等）」共 5 组（相对路径+根部署 / 相对路径+子路径 / 完整端点含路径 / 完整端点无路径 / 完整端点+子路径前缀），断言 expect(MockWebSocket.instances[0].url).toBe(expected)。复跑：npx vitest run src/stores/__tests__/websocket.spec.ts → **28 用例全绿**。产物：frontend-shared/src/stores/__tests__/websocket.spec.ts。
| **F16 主题 token 死代码** | 侧栏 `MainLayout.vue:491` 硬编码 `#1a1f2e` 渐变 → `theme.css:53/151` 的亮暗 `--sidebar-bg-gradient` **零消费点**（实测两主题 backgroundImage 逐字符相同） | D4 | §3.6.1/§3.6.5 | P2 |
> **2026-09-16 回写**：**已闭环**。MainLayout.vue:787 已改为 `background: var(--sidebar-bg-gradient, linear-gradient(180deg, #1a1f2e 0%, #1e2538 100%))`；theme.css:65 亮色 token、:259 暗色 token（#141414 → #1a1a1a）⇒ token 有消费点且亮暗不同。复跑：npx vitest run src/styles/__tests__/SidebarThemeTokens.spec.ts → **15 用例全绿**，含 :170「侧栏 background 消费 --sidebar-bg-gradient 而非硬编码（F16 核心）」与 :184 亮≠暗断言。产物：frontend-shared/src/styles/__tests__/SidebarThemeTokens.spec.ts。
| **F17 静态主题常量不随主题** | `Monitor.vue:196/206/229/239` 用 `THEME_COLORS` 静态常量（暗色 el-progress 仍 `rgb(103,194,58)`，而 `--color-success` 已 `#85ce61`） | D1 | §3.6.2 | P2 |
> **2026-09-16 回写**：**已闭环**。grep -n "THEME_COLORS" frontend-shared/src/views/monitor/Monitor.vue → **0 命中**，进度条配色已全部交给 CSS 变量。复跑：npx vitest run src/views/monitor/__tests__/Monitor.spec.ts → **38 用例全绿**，含 :404「源码不再引用 getThemeColors/THEME_COLORS（F17：进度条配色已完全交给 CSS 变量）」。注：utils/theme.ts:17 仍导出 THEME_COLORS，但 :29-37 已改为 resolve(--color-*, THEME_COLORS.x) 的 CSS 变量兜底语义，不再是「静态常量不随主题」。产物：frontend-shared/src/views/monitor/Monitor.vue、frontend-shared/src/views/monitor/__tests__/Monitor.spec.ts。
| **F18 其他** | 命令 ID 死链（唯一反馈是 `console.log`，`AutomationRules.vue:581-584` 的 TODO 占位）；`el-switch` 无可访问名称；404「返回上页」在无历史时跳 `about:blank`；403 硬编码「系统管理员」但 `UserInfo` 无 `role` 字段；术语残留「设备模板」（`QuickCreateDeviceDialog.vue:13`）；`'-'` 未用 `—`；骨架卡 90px vs 内容卡 102px | 各域 | 各条 | P3 |
> **2026-09-16 回写**：**已闭环（逐子项均有产物）**。① 命令 ID 死链 → 已接真实端点 GET /api/v1/device-operations/:execution_id（api/deviceOperation.ts:35、AutomationRules.vue:716 注释）；② el-switch 可访问名 → SwitchAccessibleNameGate.spec.ts（2 用例绿）；③ 404「返回上页」→ NotFound.vue:31-47 改用 vue-router history.state 判据（明写为何 history.length>1 会把用户送到 about:blank），NotFound.spec.ts **8 用例绿**；④ 403 硬编码「系统管理员」→ Forbidden.spec.ts **8 用例绿**（未登录 / 无 role 时不得出现该角色名）；⑤ 术语「设备模板」→ glossaryTermsGate.spec.ts **4 用例绿**（含分类器自检，注释可剔除）；⑥ 半角连字符占位符 → placeholderDashGate.spec.ts **3 用例绿**；⑦ 骨架卡 90px vs 102px → DashboardSkeletonGeometry.spec.ts **4 用例绿**（注释记录浏览器实测 157→102 的变异自证）。复跑：npx vitest run src/utils/__tests__/glossaryTermsGate.spec.ts src/stores/__tests__/websocket.spec.ts src/views/error/__tests__/NotFound.spec.ts src/views/error/__tests__/Forbidden.spec.ts src/views/dashboard/__tests__/DashboardSkeletonGeometry.spec.ts src/components/common/__tests__/SwitchAccessibleNameGate.spec.ts src/utils/__tests__/placeholderDashGate.spec.ts → 全部通过。

## 4. 实施顺序（设计最先，依赖不可颠倒）

```
阶段0 修复基础设施（先做，否则后面所有修复都会漂移）
  0.1 建立"失败态"统一范式：EmptyState.vue 的 kind:'error' 已支持但**实现方未接线**
      → 抽 useAsyncState/FailureState 组合式，作为 F1 的唯一修复入口
  0.2 收敛反馈入口：把 20 处 ElMessageBox.confirm 迁到 feedback.confirmDanger（已有定义）
  0.3 补 pointer:coarse 全局策略（theme.css）

阶段1 P0（3 项，都有现成参照，风险低）
  F1 失败态（6+ 页，按 data-sources / node-detail 的既有正确范式修）
  F2 固件字段名（后端统一 node_model 或前端改发，二选一，并恢复 ShouldBindJSON 错误处理）
  F9 device-configs 一行修（flex-wrap）

阶段2 P1（7 项）
  F15 WS URL（改 toBe 全等断言 + 四类输入矩阵 → 这才是"补测试"而非"改代码"）
  F3 分页（后端加 offset/limit + 前端分页控件；automation 的 1072 处 inline style 随之收敛）
  F4 对比度（**单列全局 token 修复**，不逐页修 —— D2 域明确警告逐页修会漂移）
  F5 危险确认（20 处机械迁移 + 默认焦点修正）
  F6 触控（257/264 是"行高 18px"单一根因 → 改表格行高）
  F7 键盘可达（侧栏 roving tabindex + 页内 role/tabindex/aria）
  F8+F10 移动端（wrapper + hint + 面包屑）

阶段3 P2/P3（打磨，触及面渐进）
  F11-F18
```

## 5. 主控独立复核记录（不采信子代理自报）

| 项 | 复核方式 | 结果 |
|---|---|---|
| U-1 阻断 | 亲自 `page.route` 拦截 500 + 取 DOM | ✅ 复现，`alertOkIconColor=rgb(103,194,58)`、`kpiValues=["0","0","0","0"]` |
| U-2 静默丢失 | **真实 API 闭环**（登录→上传→PUT→列表读回→DELETE） | ✅ 复现：发 `target_model` 无效、发 `node_model` 生效；审计数据残留 0 |
| U-3 裸主键 | 读源码 + 截图 | ✅ `AutomationRules.vue:76` |
| F3 分页 | 读后端 `Limit(500)` + 探针 `totalEls=11902` | ✅ |
| F4 对比度 | 采信两域独立测得的一致值（D1 与 D4 各自测量，值相同） | ✅ 交叉一致 |
| F5 危险确认 | 主控 grep 全仓量化 | ✅ 20 处仅 1 处 danger |
| F9 裁切 | 亲自定位祖先链 + 代码行号 | ✅ `.filter-right` 无 `flex-wrap`（`DeviceConfigList.vue:646-649`） |
| F15 WS URL | **真实 vitest 跑 5 组输入** | ✅ 2 组 MISMATCH；证明 `toContain` 对缺陷恒真 |
| D2 域报备 | 核实 `components.d.ts` 时间戳来源 | ✅ 确认为主控 12:20 构建生成，非子代理改动 |

## 6. 审计自身的四个方法论产出（写进契约，可复用）

1. **截图会骗人**：node-list mobile-390「设备ID被切」实为视口高度截断（DOM 实测 `right=341 < 390`）
2. **探针会误报**：EP 表格横滚容器（`overflow:hidden` + `scrollWidth>clientWidth`）被误判为裁切
3. **探针会漏报，且修完会过度修正**：`clickableNotFocusable` 先恒为 0（假阴性）→ 修后虚高 392（假阳性）
   → 最终需**拿已知答案样本校准**（"侧栏 12 个菜单项确实不可聚焦"）
4. **文档会过时**：规范 §6 的 7 条存量偏差实测 2 条已闭环、5 条仍在

## 7. 原待裁决项（已由主控自主裁决，见 §8）

1. ~~对比度（F4）是否接受视觉变化~~
2. ~~分页（F3）真分页 vs 提上限~~
3. ~~触控（F6）行高取舍~~
4. ~~76 条全部修 vs 先做 P0+P1~~

## 8. 主控裁决记录（自主决策，2026-09-13 第八轮）

按"当作未发布产品开发"与"设计最先"原则，对 §7 的四项待裁决**由主控自主裁决**如下：

| # | 事项 | 裁决 | 理由 |
|---|---|---|---|
| 1 | 对比度（F4） | **修，且必须走 token 层** | 对比度不达标是**可访问性缺陷**而非审美偏好（WCAG AA 是硬标准）。D2 域明确警告"逐页修会漂移"，故只改 `theme.css` 的语义 token，不动各页面。视觉变化可接受——未发布产品，且变化方向是"更可读"。**但不得引入装饰性渐变/新配色体系**（规范 §4.2.7）。 |
| 2 | 分页（F3） | **改为真分页** | `Limit(500)` 硬截断导致用户**只看到一半数据且不知情**——这比"看不到"更危险（会基于不完整数据做判断）。提上限只是把问题推迟。已派发实施任务，要求沿用 NodeList/EdgeDeviceList 的既有分页契约。 |
| 3 | 触控（F6） | **只扩热区，不改行高** | 规范 §4.2.3 要求"保持紧凑运维密度"，§4.4.5 允许高密度工具栏降到 36px。故：用 padding / `::after` 扩大**点击热区**至 ≥36px（页头图标 ≥44px），**视觉密度不变**。验收必须用 `elementFromPoint`（`getBoundingClientRect` 测不出伪元素热区）。 |
| 4 | 76 条的处置 | **先做 P0+P1，P2/P3 随触及面渐进** | 规范 §6 明示"存量偏差不要求在无关功能改动中顺带重构"。P0+P1 已派发 5 个并行任务；P2/P3 记入方案等后续。**但新增代码不得复制这些模式**（规范 §6 硬要求）。 |
| 5 | 审计断言沉淀（D3 域提出） | **沉淀为 CI 门禁，但只收可机械判定的** | 规范 §7.6「只有可自动判断、影响面明确且已经证明能防真实回归的规则，才进入强制门禁」。故只沉淀：DOM 溢出/裁切、触控热区尺寸、对比度阈值、关键元素存在性。**主观项（美观/间距节奏）不入门禁。** 已单独派发。 |

