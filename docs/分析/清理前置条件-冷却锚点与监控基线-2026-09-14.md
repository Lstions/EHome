# 清理前置条件 — 冷却锚点与监控基线

> 状态：**v3 已实施完成（A/B 全部落地并测试通过；SQLite + PG 双库验证；变异自证完成）**
> 日期：2026-09-14
> 范围：只做两件事 —— ①`automation_events` 冷却锚点 ②命令域监控基线。**不含任何清理器**。
> 上游裁决：`docs/分析/运行期无界增长表-保留策略设计-2026-09-13.md`（INV-1 / INV-2）
> 取证口径：所有行号均为本文档写作时对工作树的**实测**（`read` 工具逐行核对），非转述。

---

## 0. 本文档要钉住的两条不变式（先写结论）

| 编号 | 不变式（必须永远成立） | 不成立时的后果 |
|---|---|---|
| **A-INV** | 删除 `automation_events` 中任意 `result='executed'` 行后，对任意规则，**冷却窗不得提前解除** | 条件仍满足的规则在重启后**立即重触发** → **真实设备动作多发**。这是改变系统行为，不是丢审计 |
| **B-INV** | 删除 `command_executions` 中任意历史行后，`GET /api/v1/metrics/summary` 的 `control.*` 累计计数**不得减少** | 面板"操作失败 9"变"0"、告警高亮消失 → **看起来像故障自愈，实际是证据被删** |

两条不变式的共同性质：**它们都不是"数据保真"问题，而是"删数据会改行为"问题。**
所以判据不能是"删完之后表还在不在"，只能是"删完之后**行为/回答**是否相同"。

---

## 1. 现状证据（实测）

### 1.1 前置条件 A：`automation_events` 是冷却状态的唯一持久锚点

**证据 1 — 冷却态是纯内存，重启即失**

`backend/internal/automation/evaluator.go:80-81`
```go
// triggered 规则当前是否处于 triggered 态 (冷却中)。
triggered map[uint]time.Time // rule ID → 触发时刻
```
`evaluator.go:97` 初始化该 map，`evaluator.go:102-103` 在构造时调用 `LoadRules()` + `rebuildCooldowns()`。

**证据 2 — 回填的唯一数据源就是事件表**（`evaluator.go:107-139`）

```go
func (e *Evaluator) rebuildCooldowns() {
    ... e.db.Model(&models.AutomationEvent{}).
        Select("rule_id, MAX(triggered_at) AS max_at").
        Where("result = ?", models.AutomationResultExecuted).
        Group("rule_id").Scan(&rows) ...
    e.triggered[r.RuleID] = at
}
```
⇒ **删掉 executed 行 = 删掉重启后唯一能恢复冷却的东西。** `len(rows) == 0` 时直接 return，`triggered` 保持为空 —— 所有规则被视为"从未触发"（armed）。

**证据 3 — 规则表上没有任何"上次触发"字段**
逐行核对 `backend/internal/models/automation.go:102-149` 的全部字段：
`ID / Name / Enabled / TriggerType / TriggerSensorName / TriggerComparator / TriggerThreshold /
TriggerDurationSec / TriggerWindowStart / TriggerWindowEnd / TriggerWindowEdge / TriggerEdgeDeviceID /
ConditionsJSON / ActionType / ActionDeviceID / ActionID / ActionParamsJSON / ActionLevel /
CooldownSec / RequireConfirmed / MaxDailyExec / CreatedAt / UpdatedAt / DeletedAt` —— **无 last_triggered 类字段**。
全仓 grep `last_triggered|LastTriggered` = **0 命中**（实测）。

**证据 4 — 同一张表还承载另外 6 处业务判定（不是只读审计）**

| # | 位置 | 用途 | 口径 |
|---|---|---|---|
| 1 | `evaluator.go:115-121` | 启动重建冷却 | `MAX(triggered_at) WHERE result='executed'` GROUP BY rule_id |
| 2 | `planner.go:93-97` | 日熔断（自动路径） | 当日 `executed` 计数 |
| 3 | `planner.go:175-179` | 幂等键序号（自动路径） | 当日 `executed` 计数 → `automation:<id>:<yyyymmdd>:<seq+1>` |
| 4 | `planner.go:530-533` | 日熔断（手动触发） | 同日同口径 |
| 5 | `planner.go:553-557` | **手动触发的冷却抑制** | 最近一条 `executed/pending_confirm` 的 `triggered_at` |
| 6 | `planner.go:626-629` | 幂等键序号（手动） | 当日 `executed AND trigger_source='manual'` 计数 |

**⚠️ 与任务书背景的一处出入（以实测为准）**：任务书写"planner.go:93/175/530/553/626 另有 5 处"。
实测这 5 个位置确实存在，但**对应 6 个不同口径**（`:175` 与 `:626` 分别是自动/手动两条幂等序号）；
并且**任务书漏掉了第 7 处**：`planner.go:795-802 sweepExpiredPending`（每 5 分钟把 24h 前的 `pending_confirm` 改写成 `expired`）。
该处是 UPDATE 而非按时间 DELETE，不影响本次前置条件的结论，但清理器实现者必须知道。
**结论方向与任务书一致，差别只在数目与一处遗漏。**

**证据 5 — executed 行有 3 个生产写入点（任务书只点名了 2 个）**

| # | 位置 | 场景 | 是否落 `executed` |
|---|---|---|---|
| ① | `planner.go:210 executeDeviceAction` → `planner.go:281-310 recordRet` | 自动触发执行成功 | 是 |
| ② | `planner.go:657-665 executeManualDeviceAction` | 手动触发执行成功 | 是 |
| ③ | **`planner.go:757-766 ConfirmEvent`** | 人工确认把原 `pending_confirm` 行**条件 UPDATE 成 `executed`** | 是（不是 INSERT） |

⇒ ③ 意味着：**如果只在 `recordRet` 里写锚点，确认制规则（`require_confirmed=true`）的冷却锚点永远不会被写**，
重启后同样解除冷却。这是本次实测发现的、任务书未覆盖的第三个必须修补的点。

**结论**：`automation_events.result='executed'` 行**不是审计**，是运行状态的持久层。
删除它 = 冷却锚点丢失 = 重启后冷却窗提前解除。

---

## 2. 方案对比

### 2.1 前置条件 A：冷却锚点

| 方案 | 做法 | 取舍 |
|---|---|---|
| **A1（选中）规则表加列 `last_triggered_at *time.Time`** | 在 `automation_rules` 加可空时间列；触发并落 `executed` 时**同事务**写该列；`rebuildCooldowns` 改读该列；启动时若锚点为空且事件表有历史则回填一次 | ✅ 锚点与事件表**物理解耦**，删事件行不影响冷却<br>✅ 语义显式（列名即事实）<br>✅ 与 `CooldownSec`/`MaxDailyExec` 同族（都在规则行上）<br>✅ 无需新表、无孤儿行<br>❌ 规则行变成"配置 + 状态"混合体（唯一需要辩护的点） |
| A2 独立锚点表 `automation_cooldown_anchors(rule_id PK, last_triggered_at)` | 状态完全外置 | ✅ 职责最干净<br>❌ 多一张表 + 多一次 upsert + 多一个 AutoMigrate 入口，**对本次目标无增量收益**<br> 规则删除后残留孤儿行需要另加清理 —— **反而制造新的无界增长表** |
| A3 在事件表里补写 `base_cooldown` 哨兵行（上游 §2.1 原始建议） | 清理器删 executed 行时同事务补一行锚点 | ❌ **锚点仍与事件表生命周期绑定**：清理器漏写一次/事务边界写错/未来有人 TRUNCATE 表，缺陷立刻复现<br> 把正确性寄托在**尚未实现的清理器**上 —— 而前置条件的意义恰恰是"不依赖清理器实现正确"<br>✅ 唯一优点：不需要 schema 变更 |
| A4 冷却窗改为纯内存、不重建 | 承认重启后冷却丢失 | ❌ 直接保留缺陷 |
| A5 启动时扫 `command_executions` 反推 | 不用事件表 | ❌ 关联是弱关联（无 FK，且 `notification`/`failed_gate` 类根本没有 command 行）<br>❌ **命令表自己也要被清理** —— 等于把锚点从一个会被删的表搬到另一个会被删的表 |

**裁决：A1。** 一句话理由：**要让"删事件行"在物理上不可能影响冷却，锚点就必须不在事件表里**；
A3 只是把"必现缺陷"改成"取决于清理器实现质量"，本任务的存在意义就是不接受这种依赖。

**字段名与类型的辩护（任务书要求说明）**
- 名用 `last_triggered_at` 而非 `cooldown_base` / `last_executed_at`：
  它记录的是**触发**而非**执行成功** —— 冷却窗起点是 evaluator 决定 `armed→triggered` 的那一刻
  （`evaluator.go:460` `e.triggered[rule.ID] = at`），不是命令真正下发成功的时刻。
  旧 `rebuildCooldowns` 用 `executed` 行**近似**它（因为只有 executed 才落行），新列把"近似"变成"就是这个值"。
  名字若与语义不一致，下一个读代码的人必然用错。
- 类型 `*time.Time`（可空）：**必须能表达"从未触发"**。用零值 `time.Time` 会让
  "从未触发"与"1970 年触发过"不可区分，且 `now.Sub(zero)` 是巨大正数，
  一旦有人写 `if now.Sub(rule.LastTriggeredAt) < cooldown` 就会把新规则静默永久冷却。
- **不加** `gorm:"default:..."`（同 `CooldownSec`/`Enabled` 的既有教训）：默认值会吞掉显式零值语义。

### 2.2 前置条件 B：监控基线

| 方案 | 做法 | 取舍 |
|---|---|---|
| B1 改成 Prometheus counter | `control.failed` 等读 `readCounterTotal(...)` | ✅ 天然不受清理影响<br>✅ 与已有的 HTTP/MQTT 3 个字段同范式（`handler_metrics.go:82-88`）<br>❌ **进程重启归零** —— 面板数字照样"跳回 0"，只是换了触发条件<br>❌ 现成的 `DeviceActionTransitionsTotal` 是 counter，counter 不是"当前状态数"，拿它当 `active/queued` 是语义错位<br>❌ `unresolved_unknown` 本质是**集合状态**，不是事件计数 |
| B2 加时间窗 | `WHERE created_at >= now-Nd` | ❌ **改变产品语义**：面板从"累计"变成"最近 N 天"；前端文案"操作总数"未变，用户无从知道口径变了<br>❌ **且不解决问题**：窗口内被删的行照样让数字变小 |
| **B3（选中）计数快照表** | 清理器删除前，在**同一事务**内把即将删除的行按状态累加进快照；面板读 = 存活行 `COUNT(*)` + 快照。**只覆盖 5 个事件累计量 `operations_total/succeeded/failed/unknown/cancelled`**（见下方单独裁决） | ✅ 语义**完全不变**（仍是"累计"，与面板文案、前端契约、既有测试断言逐字一致）<br>✅ 与 A1 同构：**把易失事实搬到清理器碰不到的地方**<br>✅ 单行 upsert，写放大 O(1)，表不可能增长<br>✅ 清理器未实现时本改动**可独立上线且行为零变化**（基线恒为 0）<br>❌ 需要清理器配合（**本任务只交付读侧 + 累加 API + 可用性证明，不交付调用方**）<br>❌ 快照写错无法自愈（对策：单调性不变式 B-INV-3） |
| B4 新建行时双写计数器 | 每次 Create 时 `counter + 1` | ✅ 同样不受清理影响<br> 仍需一次性历史回填（与 A1 回填同型）<br>❌ 把写放大压到控制域最不能慢的热路径 `Create` 上<br> 本质与 B3 相同但多一次热路径写 |

**裁决：B3。** 一句话理由：**"累计"这个语义必须原样保住**（B2 改语义、B1 退化成"本次进程内累计"），
而保住累计的唯一办法是让被删掉的那部分计数有一个清理器不会碰的落点。

**明确否证的一个直觉**（写给复核者）：`command_manual_resolutions` + `security_audit_events`
**不能**充当基线 —— 前者只覆盖"被人工处置的 UNKNOWN"，后者自己有保留期（上游 INV-6）。
**两个都会被时间削掉的表不能互为基线。**

#### 2.2.1 `unresolved_unknown` 不在 B3 的适用范围内（独立裁决，主控复核后再修正）

**这条曾写错，记录在此以免复发**：初稿把它和另外 7 个字段并列成"基线 + 存活行"，
并把"有人给它做基线"写成变红条件 —— **方向恰好是反的**。性质其实完全不同：

| 字段 | 性质 | 会不会自己下降 |
|---|---|---|
| `operations_total / succeeded / failed / unknown / cancelled` | **事件累计量**（历史发生过的次数） | 不会（单调不减） |
| `unresolved_unknown` | **集合成员数**（*当前*处于 UNKNOWN **且尚未**被人工处置的行数） | **会** —— 有人处置就减一 |

**为什么给它做基线会坏（不是措辞问题，是产品损坏）**：

```go
// backend/internal/commandexec/service.go:594-600 ResolveUnknown
tx.Clauses(clause.Locking{Strength: "UPDATE"}).First(&execution, "command_id = ?", in.CommandID)
    // 行不存在 → 直接 return err, 处置失败
if execution.Status != StatusUnknown { return ErrNotResolvable }
```
⇒ **人工处置的入口必须先加载 execution 行；行被清理掉之后，这条悬案从此再也不可能被处置。**
此时若把它计入基线：基线里这 1 条**永久留存**，而"处置后减一"的机制**永远不会对它生效**
⇒ 面板"未处置异常"**永久虚高且用户怎么处置都降不下来** ⇒ 用户去追一个已不存在的悬案。

对比：变 0 是**少报**（可察觉、可恢复）；虚高是**多报且不可纠正**。后者更坏。
而 `unresolved_unknown` 恰恰是 `Monitor.vue:141` 的**唯一告警高亮**字段。

**裁决（与上游 §2.4 取齐，两者一致）**：
1. **B3 只覆盖 5 个事件累计量**；`unresolved_unknown` **显式排除**，**不设基线**。
2. `unresolved_unknown` 清理后的正确性由**上游 §2.4 已有裁决**保证：
   *"若一条 UNKNOWN 从未被 `command_manual_resolutions` 处置，**不参与时间窗删除**"* ——
   即「未处置的 UNKNOWN 永不随时间删」。`handler_metrics.go:120-121` 的 SQL **一个字都不用改**。
3. 因此 B 的交付物里对它只有**一条更强的守卫**（见 §3 B-INV-1b）：
   **清理器不得删除未被处置的 UNKNOWN 行** —— 这比"删了之后数字还对"强得多，
   因为它同时保住了「这条悬案还能被处置」这个能力（基线方案保不住）。

**答主控问题 1/2/3**：① 是，B3 应只覆盖 5 个累计量，`unresolved_unknown` 显式排除；
② 上游 §2.4 该裁决已实测核对属实，两者已取齐，不需要给它做基线；
③ 变红条件已改为"有人给它做了基线 / 有人删除了未处置的 UNKNOWN 行"。

**这条也顺带否掉了 B3 的一个隐含前提**：不是"所有 `COUNT(*)` 都该基线化"，
而是"**会因删除而永久失真的量**才该基线化"。`unresolved_unknown` 的失真来源不是删除，
是"处置入口依赖被删的行"—— 那要靠**不删**来解，不是靠计数补偿。

### 2.3 为什么不选"先实现清理器再谈基线"

前置条件的定义就是**"不做就会造成功能可见损坏"**。A/B 两条都满足"清理器一旦上线且未做前置条件 → 损坏必现且静默"。
因此顺序只能是：**A1 + B3 落地 → 再写清理器**。

---

## 3. 不变式（写成测试，且必须能变红）

> 每条都写成「**什么样的改动会让它变红**」，让它成为约束而不是当前取值的快照。

### A-INV-1 冷却窗不因删除 executed 行而提前解除（本仓当前**应为红**）
**性质**：造规则 R（`cooldown_sec = 3600`），制造"1 小时前触发过"的事实并落一条 `executed` 行；
**删除该行**（模拟清理）；新建 Evaluator（模拟重启）；在同一时刻再次求值 ⇒ **不得触发**（冷却未到期）。
**测试**：`backend/internal/automation/cooldown_anchor_test.go`
**变红条件**：`rebuildCooldowns` 回退成依赖 `automation_events`；锚点列被移除；触发路径不再写锚点；**或确认制路径（③）忘了写锚点**。

### A-INV-2 锚点写入与 executed 事件同事务（崩溃窗口不得存在）
**性质**：让 `executed` 事件的落库失败（例如规则行被同步删除导致外键/约束失败，或注入了失败的 DB 句柄），
断言**事件行与锚点列一起不存在** —— 不允许"事件落了锚点没落"或反之。
**测试**：`cooldown_anchor_test.go` 的 `TestCooldownAnchorAndExecutedEventAreAtomic`。
**变红条件**：有人把锚点更新挪到事务外（这正是任务书点名的崩溃窗口）。

### A-INV-3 一次性回填只填"锚点为空"的规则，绝不覆盖已有锚点
**性质**：若某规则已有锚点 T0，而事件表 MAX = T1 > T0，回填**不得**把它改写为 T1（锚点自持）。
**变红条件**：有人把回填写成无条件 `UPDATE ... = (SELECT MAX(...))` —— 那会让事件表的清理再次影响冷却。

### B-INV-1 清理后累计计数不减少
**性质**：造 3 条 `FAILED`（1 条 now、2 条 800 天前）+ 1 条 `SUCCEEDED` + 1 条 `CANCELLED`
⇒ 记录面板值 ⇒ **删除历史行并在同一事务累加基线** ⇒ 面板值**逐字段不得减少**。
**测试**：`backend/internal/api/command_metrics_baseline_test.go`（走真实 `GET /api/v1/metrics/summary` HTTP 链路）
**变红条件**：基线补偿被漏掉；或累加写成覆盖（不增反减）。

### B-INV-1b `unresolved_unknown` 只反映**真实存在且未被处置**的悬案（**比数字更强**）
**性质（两条，缺一不可）**：
1. **反向**：给 `unresolved_unknown` 造一个非零基线后，面板值**必须不变**
   —— 它**不接受**基线补偿（接受 = 永久虚高，见 §2.2.1）。
   构造：1 条存活未处置 UNKNOWN + 基线里写 `unresolved_unknown=5` ⇒ 断言面板仍是 **1**。
2. **正向（更强）**：**清理器不得删除未被 `command_manual_resolutions` 处置的 UNKNOWN 行**
   —— 这同时保住了"这条悬案还能被处置"的能力（`service.go:594-600` 的处置入口依赖该行存在）。
**测试**：同上文件，`TestUnresolvedUnknownIgnoresBaselineAndKeepsActionable`。
**变红条件**：
- 有人把 `unresolved_unknown` 也算进"存活行 + 基线" ⇒ **红**（这是本条真正要防的方向）；
- 有人写了删除未处置 UNKNOWN 的清理片段 ⇒ **红**（在 `backend/internal/api` 侧以"处置入口可用性"断言体现：
  删掉行之后 `ResolveUnknown` 必须仍能成功 —— 它必然失败，于是红）。

### B-INV-2 清理前后 control 字段名与语义不变（前端零改动）
**性质**：`MetricsResponse.Control` 的 12 个 JSON 字段名与今天**逐字一致**。
**测试**：对响应体做**原始 JSON 键集合**断言（不是反序列化到结构体 —— 那会掩盖字段缺失/改名）。
**变红条件**：有人把字段改名成 `failed_total` 之类，或把某个计数从 int 改成对象。

### B-INV-3 基线只增不减（单调性）
**性质**：连续累加，各字段单调不减；在**无新命令**时 `基线 + 存活行` 恒定。
**变红条件**：有人把 upsert 写成覆盖而不是累加（第二次清理会抹掉第一次的 `failed`）。

---

## 4. 代价（丢失了什么能力）

| 改动 | 丢失的能力 | 是否可接受 |
|---|---|---|
| A1 加锚点列 | 锚点只保留**最后一次**触发时刻；删掉 executed 行后，"之前那些触发分别是什么时候"不可答 | ✅ 可接受：冷却只需最近一次（`cooldown_sec` 上界 86400 = 1 天，实测数据跨度 5.36 天）；逐次历史仍在事件表（清理前） |
| A1 一次性回填 | 回填**依赖事件表仍存在**。若在回填代码上线前有人先删了事件行，锚点永久为空 | ️ **这正是"锚点必须先于清理器上线"的原因**；本任务把该顺序固化 |
| A1 加列 | 规则行变成"既是配置又是状态"，规则导出/导入需忽略该列 | ✅ 可接受：`json` 只读暴露，文档标注 |
| B3 基线表 | **"被清理掉的那部分计数发生在哪一天"永久不可答**（只剩总数） | ✅ 可接受：面板今天也只显示总数，没有时间维度 |
| B3 基线表 | 基线一旦写错（重复累加/漏累加）**没有第二个数据源可对账** | ⚠️ 用 B-INV-3 单调性 + 清理器"删除行数 == 累加行数同事务"断言兜住 |
| B3 读侧 | `operations_total` 不再等于"表里行数"，DBA 直接 `count(*)` 会与面板不一致 | ✅ 可接受且**必须如此**：这正是"删数据不改行为"的定义；需在面板/文档注明口径 |

**明确不接受的代价**：❌ 重启后面板归零（B1）；❌ 面板只显示最近 N 天（B2）；❌ 冷却锚点由清理器补写维护（A3）。

---

## 5. 影响面清单（逐文件）

### 5.1 前置条件 A（冷却锚点）

| 文件 | 改动 | 风险 |
|---|---|---|
| `backend/internal/models/automation.go` | `AutomationRule` 增加 `LastTriggeredAt *time.Time` | 低：PG `ADD COLUMN`，无表重写 |
| `backend/internal/database/gorm.go:127` | **无需改动**：`&models.AutomationRule{}` 已在 AutoMigrate 列表内（实测），新列自动迁移 | — |
| `backend/testutil/db.go:81` | 同上，已在列表内，测试库自动获得新列 | — |
| `backend/internal/automation/evaluator.go` | `rebuildCooldowns` 改为**先读 `automation_rules.last_triggered_at`**；再对"锚点为空"的规则做一次性回填（读事件表 MAX） | 中：回填必须"只填空"，见 A-INV-3 |
| `backend/internal/automation/planner.go` | ①自动路径：`executed` 事件 Create + 锚点 Update **同一事务**<br>②手动路径同理<br>③`ConfirmEvent` 翻转成 `executed` 时**也更新锚点**<br>④手动冷却判定改读锚点（或保留事件表兜底并在文档说明） | **高**：③ 是任务书未点名但实测存在的第三个写入点 |
| `backend/internal/api/handler_automation.go` | 无（`Updates(map)` 白名单不含新列）；需实测确认 update 白名单不包含该列，防止用户改锚点 | 中 |
| 前端 | **无** | — |

### 5.2 前置条件 B（监控基线）

| 文件 | 改动 | 风险 |
|---|---|---|
| `backend/internal/models/command_metrics_baseline.go`（新增） | 单行表：`id`(PK 恒 1)、`operations_total / succeeded / failed / unknown / cancelled`、`updated_at` | 低：新表 |
| `backend/internal/database/gorm.go` | AutoMigrate 列表加入该模型 | 低 |
| `backend/internal/commandexec/metrics_baseline.go`（新增） | `AccumulateMetricsBaseline(tx, delta)`（upsert 累加）+ `GetMetricsBaseline(db)` | 中：upsert 需同时兼容 SQLite 与 PG（`clause.OnConflict` + `Expr` 累加） |
| `backend/internal/api/handler_metrics.go` | 5 个"累计型"指标改为 `COUNT(存活行) + 基线`：`operations_total / succeeded / failed / unknown / cancelled` | 中：口径必须逐字对齐今天 |
| `backend/internal/commandexec/retention_scope.go`（新增） | `PrunableStatuses` / `PrunableExecutionsQuery` —— **把"未处置 UNKNOWN 不得删"落到生产代码**，供下一任务的清理器直接复用；测试引用它，因此"把 UNKNOWN 加进白名单"会立刻变红 | 低：纯查询构造，无副作用 |
| 前端 | **无**（JSON 字段名不变） | — |

**`unresolved_unknown` 的处置**：见 §2.2.1 独立裁决 —— **不基线化、SQL 一字不改**，
正确性由"未处置的 UNKNOWN 永不随时间删"（上游 §2.4）保证，并用 §3 的 **B-INV-1b** 双向钉住。

### 5.3 共同影响面

- AutoMigrate 顺序：新表/新列均为**纯追加**，无破坏性变更。
- 生产库 `ehome` / `ehome_test` **本次一律不碰**；PG 验证走隔离 schema（`test_*`，用后即删）。
- **清理器本次不实现**：B3 的写方（清理器同事务累加）留待下一任务。

---

## 6. 实施结果（**已执行完毕，实测记录**）

### 6.1 缺陷先红（证据）

在**只加模型字段、不改任何行为**的状态下跑测试，得到真实红灯：

```
--- FAIL: TestCooldownAnchorSurvivesExecutedEventDeletion (0.03s)
    --- PASS: 正对照: executed 行保留 ⇒ 重启后仍在冷却 (0.02s)
    --- FAIL: 主断言: executed 行被删 ⇒ 重启后不得提前解除冷却 (0.01s)
        cooldown_anchor_test.go:85: 删除 executed 行后重启立即重触发 ——
        冷却窗被清理提前解除, 条件仍满足的规则会多发真实设备动作 (A-INV-1 被破坏)
--- FAIL: TestCooldownAnchorBackfillOnlyFillsEmpty —— 锚点为空的规则未被一次性回填
```

⇒ **缺陷真实存在，且正对照证明该断言非空洞**（行保留时不触发、被删后触发）。

### 6.2 落地清单（逐文件）

| 文件 | 改动 |
|---|---|
| `backend/internal/models/automation.go` | 新增 `LastTriggeredAt *time.Time`（`gorm:"index"`） |
| `backend/internal/automation/evaluator.go` | `rebuildCooldowns` 改读锚点列；新增 `backfillCooldownAnchors`（只填空 + UPDATE 自带 `IS NULL` 守卫） |
| `backend/internal/automation/planner.go` | 新增 `recordExecuted`（事件 + 锚点**同事务**）；手动路径同事务；`ConfirmEvent` 翻转时同事务写锚点；新增 `cooldownBase`（锚点优先，事件表仅作历史兜底） |
| `backend/internal/models/command_metrics_baseline.go` | 新增单行基线表模型 |
| `backend/internal/commandexec/metrics_baseline.go` | 新增 `AccumulateMetricsBaseline`（upsert 累加）/ `GetMetricsBaseline` |
| `backend/internal/commandexec/retention_scope.go` | 新增 `PrunableStatuses` / `PrunableExecutionsQuery` —— 把"未处置 UNKNOWN 不得删"落到生产代码 |
| `backend/internal/api/handler_metrics.go` | 5 个累计量改为 `存活行 + 基线`；`unresolved_unknown` 刻意不加基线并写明理由 |
| `backend/internal/database/gorm.go` + `backend/testutil/db.go` | AutoMigrate 注册新模型 |
| 前端 | **零改动**（JSON 契约字段名逐字不变，B-INV-2 断言） |

### 6.3 测试与不变式对应

| 不变式 | 测试 | 位置 |
|---|---|---|
| A-INV-1 | `TestCooldownAnchorSurvivesExecutedEventDeletion`（含正对照） | `internal/automation/cooldown_anchor_test.go` |
| A-INV-2 | `TestExecutedEventAndAnchorAreAtomic`（手动）+ `TestAutoTriggerEventAndAnchorAreAtomic`（自动） | 同上 |
| A-INV-3 | `TestCooldownAnchorBackfillOnlyFillsEmpty` + `TestBackfillConcurrentWriteIsNotOverwritten` | 同上 |
| A-INV-4 | `TestConfirmEventWritesCooldownAnchor`（确认制第三写入点） | 同上 |
| B-INV-1 | `TestMetricsCountsSurviveCommandPruning` | `internal/api/command_metrics_baseline_test.go` |
| B-INV-1b | `TestUnresolvedUnknownIgnoresBaseline` + `TestUnresolvedUnknownStaysActionable` | 同上 |
| B-INV-2 | `TestControlContractFieldNamesUnchanged`（原始 JSON 键集合） | 同上 |
| B-INV-3 | `TestBaselineIsMonotonic` | 同上 |
| 迁移 | `TestPreconditionSchemaMigrates` + `TestPreconditionModelsAreInProductionAutoMigrate` | `internal/database/` |

### 6.4 变异自证（改坏 → 红 → 恢复 → md5 一致）

| # | 变异 | 结果 |
|---|---|---|
| A1 | `rebuildCooldowns` 忽略锚点、退回事件表 | ✅ 红（A-INV-1 主断言） |
| A2 | 回填去掉 `last_triggered_at IS NULL` 守卫 | ✅ 红（**首次漏网**，见 6.5） |
| A3 | `ConfirmEvent` 不写锚点 | ✅ 红（A-INV-4） |
| A4 | `recordExecuted` 锚点更新挪出事务 | ✅ 红（**首次漏网**，见 6.5） |
| B1 | 去掉 `failed` 基线补偿 | ✅ 红（"failed 从 3 降到 1"） |
| B2 | 给 `unresolved_unknown` 加基线 | ✅ 红（"unresolved_unknown=6, 期望 1"） |
| B3 | 把 UNKNOWN 加进删除白名单 | ✅ 红（"未处置的 UNKNOWN 行被清理器删除"） |
| B4 | 基线 upsert 改成覆盖写 | ✅ 红（"第二次累加抹掉了第一次"） |

恢复后 5 个受改文件的 md5 与变异前备份**逐字节一致**：
`evaluator.go 35d6a3db…`、`planner.go 79c5d831…`、`automation.go a76996a0…`、
`handler_metrics.go 0dfa503f…`、`metrics_baseline.go 7e2a4b15…`、`retention_scope.go 799d339c…`。

### 6.5 两个"首次漏网"的变异（本次最有价值的发现）

A2 与 A4 第一次跑**没有变红**，暴露了测试本身的覆盖缺口 —— 已补齐：

- **A2 漏网**：`TestCooldownAnchorBackfillOnlyFillsEmpty` 只验证了**调用方**的判断，
  没验证 **UPDATE 自身的 `IS NULL` 守卫**。而真正的危险是"Scan 出空锚点清单之后、
  UPDATE 执行之前"有并发触发写入 —— 只有 UPDATE 自带条件才原子。
  ⇒ 新增 `TestBackfillConcurrentWriteIsNotOverwritten`（直接调 `backfillCooldownAnchors`，
  绕过调用方判断），重放 A2 即红。
- **A4 漏网**：原子性测试只覆盖了**手动路径**，而 `recordExecuted` 服务的是**自动路径** ——
  两段独立实现。⇒ 新增 `TestAutoTriggerEventAndAnchorAreAtomic`，重放 A4 即红。

> 这正是"变异自证"的价值：**不变式测试本身也会漏**。如果只跑一次绿灯就收工，
> 这两个缺口会一直留着，直到线上以"冷却偶尔提前解除"的形式出现。

### 6.6 双库验证与迁移

```bash
# SQLite（默认）与 PG 隔离库均通过
go test -buildvcs=false -count=1 ./internal/automation/ ./internal/commandexec/ ./internal/api/ ./internal/database/
EHOME_TEST_DB=postgres EHOME_DB_HOST=127.0.0.1 EHOME_DB_PORT=5432 \
  EHOME_DB_USER=ehome EHOME_DB_PASSWORD=ehome123 EHOME_DB_NAME=ehome_sim_pg \
  go test -buildvcs=false -count=1 ./internal/automation/ ./internal/commandexec/ ./internal/api/ ./internal/database/
```

- 结果：**两侧全绿**。跑完 `SELECT count(*) FROM pg_namespace WHERE nspname LIKE 'test_%'` = **0**（无残留 schema）。
- **生产迁移路径已确认**：`models.AutomationRule` 与 `models.CommandMetricsBaseline` 都在
  `database/gorm.go` 的 `AutoMigrate(...)` 实参列表内；且新增
  `TestPreconditionModelsAreInProductionAutoMigrate` 直接读 `gorm.go` 源码断言这一点
  —— 防止"只在 `testutil` 列表里加模型，测试库有列而生产库没有"的静默迁移缺失。
- **跨库精度差异（实测）**：PG `timestamp` 只到**微秒**，SQLite 保留**纳秒**。
  `TestConfirmEventWritesCooldownAnchor` 最初用 `Equal` 比较在 PG 下变红（差 116ns）。
  已改为 **1ms 容差**断言 —— 该用例里触发时刻比确认时刻早 2 分钟，容差仍能抓住
  "写成了确认时刻"这类真实错误。**这是精度差异，不是缺陷。**

---

## 7. 变更记录

| 日期 | 变更 |
|---|---|
| 2026-09-14 | v1 初稿：现状实测证据、A/B 方案对比与裁决、不变式 A-INV-1..3 / B-INV-1..3、代价、影响面、实施步骤 |
| 2026-09-14 | **v3 实施完成**：A1 + B3 落地；§6 补入"缺陷先红"实测输出、逐文件清单、不变式↔测试对照、8 项变异自证结果（含 A2/A4 两个**首次漏网**的覆盖缺口与补齐）、双库验证与生产 AutoMigrate 断言、PG 微秒精度差异记录 |
| 2026-09-14 | **v2 修订（主控复核）**：新增 §2.2.1 `unresolved_unknown` 独立裁决 —— 它是**集合成员数**（会自己下降）而非事件累计量，**显式排除出 B3**；给它做基线会导致**永久虚高且不可纠正**（处置入口 `service.go:594-600` 依赖被删的行）。新增 **B-INV-1b**（双向：不基线化 + 未处置 UNKNOWN 不得删），修正 B-INV-1 原"写了基线才红"的**反向**变红条件。与上游 §2.4 取齐 |
