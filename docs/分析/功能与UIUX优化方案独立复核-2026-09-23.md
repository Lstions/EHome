# 功能与 UI/UX 优化方案独立复核（2026-09-23）

> **状态**: 评审中
> **日期**: 2026-09-23
> **复核人**: independent-verifier-2（对抗式独立复核，不采信作者自报）
> **基线**: main `7a3522b1`（`git rev-parse HEAD`，工作区仅本复核文件与四份被复核报告为未跟踪，无业务代码改动）
> **被复核对象**:
> - `docs/分析/功能模块与业务功能实测盘点-2026-09-23.md`（574 行，下称 R1）
> - `docs/分析/前端功能UI分类与信息架构实测审计-2026-09-23.md`（432 行，下称 R2）
> - `docs/分析/核心操作流程合理性审计-2026-09-23.md`（915 行，下称 R3）
> - `docs/分析/前端语义与命名一致性审计-2026-09-23.md`（369 行，下称 R4）
> **闭环基线**: `docs/分析/UIUX审计合并结论与优化方案-2026-09-13.md`（159 行，含 2026-09-16 回写）
> **规则**: 只读。未改业务代码、未改四份报告、未跑测试/build/Playwright/网络；全部裁决以当前 main 代码为准。

---

## 1. 复核方法

- **抽样复算**：每份报告抽 6 条关键量化结论（任务下限 5 条，共 24 条），用独立 grep/wc/read/只读脚本复算，记录命令与原始输出、判「一致/部分一致/不一致」。
- **引用验证**：对约 30-40 条合并/删除/精简候选中的最高风险 10 条，逐一验证真实引用关系：静态 import、动态 `import()`、`defineAsyncComponent`、`componentMap` 字符串键、router loader、`components.d.ts` 自动注册、仅测试 `mount`、注释误导。
- **去重检查**：核对候选是否落在 09-13 方案 + 09-16 回写闭环内；对 R2 §5 的 19 条「不再报」清单抽查 3 条。
- **矛盾检查**：跨报告数字/结论冲突，以及口径差异是否已在原报告解释。
- **判定等级**：成立 / 部分成立 / 不成立 / 无法复现（未跑运行时且静态无法证明）。每条「不成立/部分成立」均给独立证据与修正建议。
- **高风险误判单列**：若按候选执行会造成功能丢失或用户可感回归的，逐条给出具体功能损失。
- **样本随机性声明**：抽样条目按报告章节均匀抽取（各报告前中后段），非按结论重要性挑选，避免只验对自己有利的项。

---

## 2. R1 抽样复算（6 条，超出任务下限 5）

| # | 报告结论 | 独立命令（仓库根） | 原始输出 | 结论 |
|---|---------|--------------------|----------|------|
| R1-1 | commit 基线 `7a3522b1`，main | `git rev-parse HEAD; git branch --show-current` | `7a3522b1...`；`main` | **一致** |
| R1-2 | Go 生产文件 224 / 68833 行；前端生产 164 / 44628 行 | `find backend -name '*.go' ! -name '*_test.go' \| wc -l` 等 | `224`、`68833 total`、`164`、`44628 total` | **一致** |
| R1-3 | 端点 178（GET 81 / POST 57 / PUT 22 / DELETE 15 / PATCH 3；A+B+C+D=110+17+14+37=178） | 独立复算：`187 - 10`（data-source 两套 503 占位分支）+ `main.go:457 /ping` | 生产可达 177（GET 80/POST 57/PUT 22/DELETE 15/PATCH 3）+ /ping = **178**（GET 81）；110+17+14+37=178 | **一致（核心数字正确，但报告未写明 187→178 的扣减过程，见下）** |
| R1-4 | R1-3 可复跑命令自报第二项「routes.go v1 显式注册 23」 | `grep -rnE '^\s*register[A-Za-z]+\(v1' backend/internal/api/routes.go \| wc -l` | **24**（`registerAccountRoutesWithLimiter` 等 24 行，routes.go:116-168） | **不一致（23 vs 24）** |
| R1-5 | 前端 `from '@/api/` import 语句 98 条 | 同命令 | **97**（含 `stores/*.ts` 与 `views/*.vue`；无任何变体补差） | **不一致（98 vs 97）** |
| R1-6 | api 导出 159（HTTP-mapped 149）；domain 表合计 178 | 独立对象/函数/箭头解析 + 逐 domain 表求和 | 159 落在合理解析区间（字符串正则 161-163 属边界差异）；domain 表逐行求和 = **178** | **一致（导出计数可复现；域名表可加总）** |

R1 抽样小结：核心事实（基线、规模、端点总数、分类互斥求和、FR=63）均可复现；两处展示性数字（23、98）有 1 个偏差，均属报告附录自报命令输出，不改变主体结论。另发现 R1 §9.1 命令注释「187 含 ... 公开下载」表述正确，但正文未解释 187→178 扣除了 10 条**不可达 503 占位**分支，容易让复核者误以为 187-6-3=178，建议补算式。

---

## 3. R2 抽样复算（6 条，超出任务下限 5）

| # | 报告结论 | 独立命令 | 原始输出 | 结论 |
|---|---------|----------|----------|------|
| R2-1 | views 41 / 21418 行；components 40 / 11628 行；总 33046 | `find ... -name '*.vue' ... \| wc -l; ... \| xargs wc -l \| tail -1` | `41`、`21418 total`、`40`、`11628 total` | **一致** |
| R2-2 | C5 死组件 9 文件 4629 行 | `wc -l` 9 文件 | `4629 total`（1272+1967+163+182+205+180+221+88+351） | **一致（行数正确）** |
| R2-3 | C9 菜单 14 项平铺、`el-sub-menu` 0 命中 | `sed -n '397,412p' MainLayout.vue`；`grep -rn 'el-sub-menu' src \| wc -l` | `allMenuItems` 14 项（397-411）；`0` | **一致** |
| R2-4 | C4 `confirmDanger` 在 views+components 命中 17 处/11 文件 | `grep -rn confirmDanger src/views src/components --include=*.vue \| grep -v __tests__ \| wc -l`（再 `-l`） | `17`、`11` | **一致** |
| R2-5 | §2.2 列表页控件矩阵（12 页的 v-loading/Empty/Skeleton） | 对 12 文件独立 grep 计数 | 与报告矩阵逐格一致（如 `DeviceConfigList` v-loading 0/Skeleton 0；`AlertRules` Empty 0；`EdgeDeviceList` 0/2/3） | **一致** |
| R2-6 | §5 第 5 条自报「views+components 17 处，与 09-13 基线同量级，§4-C4 已列」 | 检查 C4 正文与 §5 | C4 正文明写 17 处/11 文件；报告脚注解释了与 09-13 的 21 处口径差（含 utils 定义/dev/注释） | **一致（口径已在文内自洽）** |

R2 抽样小结：数量类结论质量高；主要风险不在数字，而在 C5 的「不可达」判定口径与「删 4629 行」的处置建议（见第 5、第 7 节）。

---

## 4. R3 抽样复算（6 条，超出任务下限 5）

| # | 报告结论 | 独立命令/读码 | 原始输出 | 结论 |
|---|---------|---------------|----------|------|
| R3-1 | 侧边菜单 14 项；全局搜索只映射 8 个页面 | `sed -n '397,412p' MainLayout.vue`；`sed -n '736,758p' MainLayout.vue` | 14 项；搜索目的地数组 8 项 | **一致** |
| R3-2 | 流程 2「添加节点」是死入口：`/node?action=add` 全仓无消费者 | `grep -rn 'query.action\|action=add' src` | 仅 `NodeList.vue:73` 产生，无任何 `route.query.action` 读取 | **一致** |
| R3-3 | 流程 5「全仓业务源码无 dry-run 实现」，仅 node_modules/迁移脚本 | `grep -rn 'dry_run\|DryRun\|dryRun\|dry-run' backend/internal frontend-shared/src` | 前端 0 命中；后端源码 0 命中；设计文档大量要求（`docs/设计/边缘设备控制.md:62-67`、`总体设计.md:93`、`功能需求总览.md:79`） | **一致（事实成立；但“任务书期望项缺失”应升级为「已勾选 FR 的验收缺口」，见 §6）** |
| R3-4 | 流程 6 数据面板导出 CSV = 当前页 20 行，与设备详情图表导出不同源 | 读 `DataPanel.vue:928-956`、`:438-442`、`:670`；`HistoryChartSection.vue:303-339` | `pageSize=20`，`historyData` 只装接口当前页；导出循环 `for of historyData`；详情页导出 `chartSeries` | **一致** |
| R3-5 | 流程 7「事件列表无筛选」；图/表分页均为真分页 | 读 `AlertRules.vue:78-125`；`grep -n el-pagination` | 事件区无任何 `el-select/el-date-picker/el-input` 筛选；规则表/事件表各 1 个 `el-pagination` | **一致（“无筛选”成立；但”只能翻 N 页“低估了已有 `page_size` 选项 20/50/100 的定位能力）** |
| R3-6 | 流程 9 `/firmware?node=` 参数被忽略且页面无 OTA 创建 | `grep -n 'useRoute\|route.query' FirmwareManage.vue`；读全文件 | 0 命中；页面只有上传/编辑/复制/下载/删除 | **一致** |

R3 抽样小结：流程事实与静态代码高度一致；风险主要是叙述强度而非事实错误（dry-run 被说成“任务书期望”，实际是 `功能需求总览.md` 已标 ✅ 的 FR-E5；批删入口限制的表述与 `EdgeDeviceList.vue:106-120` 条件渲染一致，但 R3 流程 4 表头写“批删仅在表格视图可见”后，正文建议又写放宽条件——该处**不是**矛盾）。步数计数属人工口径，本次只复算构成控件，不重算整数分。

---

## 5. R4 抽样复算（6 条，超出任务下限 5）

| # | 报告结论 | 独立命令/读码 | 原始输出 | 结论 |
|---|---------|---------------|----------|------|
| R4-1 | 动作词裸词计数：创建 142 / 新建 38 / 添加 19 / 删除 136 / 保存 61 / 编辑 110 / 修改 23 | `for w in ...; grep -rho "$w" src/views src/components --include='*.vue' \| wc -l` | 与报告逐项相同 | **一致** |
| R4-2 | 状态词计数：在线 45 / 离线 63 / 警告 14 / 故障 15 / 异常 19 / 未知 58 | 同法 | 与报告逐项相同 | **一致** |
| R4-3 | 14 个一级菜单中 `/node`、`/edge-device` 页面无 PageHeader；`/notification-channels` 菜单“通知通道”vs 页头“外发通知通道” | `grep -n PageHeader` 两文件；`grep -n 外发通知通道` | 两文件 0 命中；页头确为“外发通知通道” | **一致** |
| R4-4 | T10 `hardware_id` 同名异物：Channel 端注释为“总线上的硬件地址 (e.g. I2C 0x76)”，EdgeDevice 端为从站地址；前端 `edgeDevice.ts` 注释“总线名字” | `sed` 读 `models.go:159,90`；`api/edgeDevice.ts:19,39-41` | 两边注释确实冲突（Channel:“硬件地址”；EdgeDevice:“物理设备地址/从站地址”；前端 Channel 侧注释“总线名字”） | **一致（措辞风险成立）** |
| R4-5 | T11 投递审计字段为 `state`（非 `status`）；前端筛选标签“状态” | `grep -n 'json:"state"' notification_channel.go`；读 `NotificationDeliveries.vue` | `NotificationDelivery.State json:"state"`；筛选标签确为“状态” | **一致** |
| R4-6 | 类型计数：术语冲突 12 / 文案风格 10 / 后端契约命名 13（合计 35）；执行摘要写 11 主术语冲突 + 4 状态 + 3 菜单 + 5 动作词 + 10 字段 = 33 | 叠加 §1-§5 表 | 摘要的 33 按分类与 §7 的 35 不同：§7 把 T7 计入文案、把 M2/M3 计入术语冲突、把 A5 计入术语冲突，摘要则按“组”计 33 | **部分成立（两套计数并存，未给 33↔35 对账公式；建议统一）** |

R4 抽样小结：计数与字段事实可靠；唯一需修正的是自身总表与摘要的口径对账。

---

## 6. 合并候选真实引用验证（最高风险 10 条）

| # | 候选（来源） | 验证命令/读码 | 真实引用事实 | 判定 |
|---|-------------|---------------|--------------|------|
| V1 | **C5/R1-表三：9 文件 4629 行可整树删除**（R2-C5；R1 §5.3、R3-C2） | 对 9 个文件逐一做 `grep -rn 'Name\.vue'`（含 `.ts/.vue/.mjs`、排除 node_modules/docs），再查模板标签、kebab-case、`defineAsyncComponent`、`componentMap`、router loader、`components.d.ts` | 9 文件确实**无生产 import/模板消费**；`NodeDetail.vue` 仅 4 个 spec（含 `?raw`）引用；`ChannelPanel.vue` 仅被 `NodeDetail.vue:444` 与 4 个 spec 引用；`GPIOResourceList/PWMResourceList` 仅由 `ChannelPanel`、`PeripheralControl`（自身死）与 spec 引用；`PeripheralControl/GPIOPinRow/PWMChannelRow/GaugeChart/ParserBrowser` 模板+import 双 0。`EdgeDeviceDetailRouter.vue` 的 `componentMap` 只映射 Bms/Inverter/Generic，不含这些名字；router loader 只含 NodeList/EdgeDeviceList；`components.d.ts` 只是自动注册声明，不构成运行时消费 | **部分成立**：9 文件均**不可达**成立；但「4629 行可直接删」**不成立**——`NodeDetail.vue` 是 4 个 spec 的 `?raw` 真源（`OtaStatusTruthSource.spec.ts:5` 用它反向校验 OTA 状态映射、`ClickableSemantics.spec.ts:108` 用它断言点击语义），`ChannelPanel` 4 个 spec 覆盖通道编辑/外设编辑/UART 重配置，直接删会丢覆盖；且 `ChannelPanel` 路径含 GPIO/PWM 配置编辑能力，而生产 `ChannelManager` 只做通道 CRUD，**删除前必须先迁移/确认能力覆盖** |
| V2 | **C1 抽取列表页壳**（R2-C1） | 读 `NodeList.vue:4,36,84` vs `EdgeDeviceList.vue:4,40,98`；`grep class="stats-row"` / `class="toolbar"` | `stats-row` 命中 4 文件（NodeList 2 段、EdgeDeviceList 2 段、DeviceConfigList、DataSourceList）；`toolbar` 命中 3 文件（NodeList/EdgeDeviceList/ChannelList）；4 页统计卡数量/点击行为/数据源不同；`listPageConventions.spec.ts:22-33` 已把「内联 import/loader 两种路由写法」固化为解析器 | **部分成立**：结构同构事实成立；但报告称「12 个路由级列表页各写一套壳」在数字上夸大——`stats-row` 只有 4 页、`toolbar` 只有 3 页真正同名，其余 8/9 页并未共享该结构。修正建议：候选改为「先抽 4 页统计卡+3 页工具栏」，并在 `listPageConventions.spec.ts` 解析器补第三种 component 写法前不动分页绑定 |
| V3 | **C3 双创建路径合并**（R2-C3；R3 流程 4-C1） | 读 `EdgeDeviceList.vue:289-597`、`QuickCreateDeviceDialog.vue:1-151`；`grep CreateWizardCommandIntervals/LogicalDeviceCandidateSelect` | 两入口都 import 同两个子件（EdgeDeviceList:660-661；QuickCreate:161-162）；但向导有 4 步状态机 `createStep/editingDeviceId/before-close`，快速创建是单弹窗且依赖父页节点上下文；报告自身也给出反证「信息密度刻意不同，最小动作是抽地址规则+command intervals 提交模型，不是合页面」 | **部分成立**：重复事实成立；「抽取共享实现」成立，但**合并页面不成立**，且 R2-C3 的收益段与反证段结论不一致，建议候选改名为「抽共享表单模型/composable」，删除「先抽模型再抽 UI」中隐含的页面合并 |
| V4 | **C6 双路由规范 URL**（R2-C6；R3 流程 2-U2） | 读 `router/index.ts:48-57`；`grep goToNodeDetail`；全仓 `router.push('/403')` 等 | 两条路由组件都指向 `NodeOverview.vue`；`DeviceInfoCard.vue:28,63` 推 `/node/:id/overview`；`ChannelList.vue:443-444` 推 `name:'NodeDetail'`；`NodeOverview.vue:1770` 注释自认“本页路由就是 `/node/:id`” | **成立（事实正确）**；但「加 redirect」前必须保留 alias/兼容外部深链，报告已给「保留 alias + 内部统一 `/overview`」的最小动作——建议采纳最小动作，不要直接删 `/node/:id` |
| V5 | **C9 菜单分组 14→5 组**（R2-C9；R4-M1） | 读 `MainLayout.vue:397-412,16-37`；`grep el-sub-menu`；`grep .el-menu-item` 测试 | 14 项平铺；`el-sub-menu` 0；`MainLayout.spec.ts:133-134`、`MainLayoutKeyboardNav.spec.ts:132`、`MainLayoutDrawerFocusTrap.spec.ts:114` 均断言 14 个 `.el-menu-item`；`e2e/helpers/uiux-metrics.ts:256-264` 用 `.sidebar .el-menu-item` 计数 | **成立（事实正确）**，且 R2 已明确「必须等 F7 门禁 submenu-aware 后再动」；**风险不在候选本身，在执行顺序**：在门禁改造前动菜单会直接红 3 个 vitest + e2e 度量 |
| V6 | **C10 通知两页合并**（R2-C10） | 读 `NotificationChannels.vue`/`NotificationDeliveries.vue`；读 `router/index.ts:127-139` 注释 | 两文件 388/387 行；`NotificationDeliveries` 路由注释自述「与通知通道同域相邻」且为只读审计；报告反证已给（审计独立 URL 的深链价值） | **不成立（作为合并建议）**：报告自己的反证更充分（只读审计、权限语义不同、P2-A 独立交付、外部深链），建议仅保留「菜单相邻呈现/增加相互跳转」，撤回合并候选 |
| V7 | **R1 表二 29 个零消费者 API 导出中的「死包装」**（R1 §5.2a） | 对每类代表：`grep "channelApi.write"`、`nodeApi.getConfig`、`dataApi.getNodeDevicesData`、`pwmApi.setFreq`、`automationApi.getRule`、`haApi.syncNode`、`getMetrics(` | 生产 `.vue/.ts` 均无调用；仅 `api/monitor.ts:66` 自身定义命中 | **成立**（抽样 7/7） |
| V8 | **R1 表二 15：`getOperationHistory` 指向不存在的后端路径**（R1 §5.2a/c） | `grep -rn 'operations/history' backend frontend-shared/src` | 前端 `api/edgeDevice.ts:377` 有；后端仅测试 `handler_edge_device_crud_test.go:708` 明确断言 legacy routes 不提供该端点 | **成立**；修正建议：该 API 方法应删除或标注为不可用，避免后续开发者接线到必 404 路径 |
| V9 | **R1 表二 25-29：`unifiedDataApi` 5 方法缺 `/api/v1` 前缀/后端不存在**（R1 §5.2a/c） | 读 `api/unifiedData.ts:50-85`、`api/client.ts:42-46`；`grep unifiedDataApi` | `baseURL` 默认空；5 个方法均相对 `/unified-data*`；后端真实路径为 `/api/v1/unified-data/*`；`unifiedDataApi` 生产零消费者；同功能生产代码全部走绝对路径 | **成立**；修正建议：整组删除或补前缀后接入，不应保留两套 URL 语义 |
| V10 | **R3-C1/R3-C12：`PeripheralControl` 接线到节点详情即“不可达→可达”**（R3 §5、流程 11） | 读 `NodeOverview.vue:838,961-966,978,899-906`；`grep gpioApi/pwmApi NodeOverview.vue` | `NodeOverview` 的 tabs 7 项**没有“外设控制”**；GPIO/PWM 被建模为资源类型但在总线页 `busSupportsChannels=false`，按钮显示“此资源不支持通道”；`PeripheralControl.vue` 未传 `registerPending`（`:16-38`），`GPIOPinRow/PWMChannelRow` 写后状态回填依赖它 | **部分成立**：接线方向正确，但「组件已具备、只差挂载」不成立——必须先补 `registerPending`/WS 回填，否则写后状态不更新；且需明确挂到新 Tab 还是总线页 GPIO/PWM 段（两者 DOM/可发现性不同） |

> 补充验证（去重相关）:
> - `components.d.ts` 自动注册 **40 条项目组件**（`grep -cE "typeof import\\('\\./components/"` = 40），确认 R2 §6.1 的 40 vs 任务书 38 差异；任务书原始文件未在工作区检索到，无法进一步判定被排除的 2 个。
> - `router/index.ts` 行首缩进 `path:` 精确计数 = **25**（与 R2 一致）；`name:` = 23；裸 `path:` = 26（多出守卫重定向 `:201` 与通配/redirect 重复计数），R2 §6.3-6 已解释。
> - `NodeDetail.vue` 的 4 个 spec 均为 `?raw` 或直接 mount，**没有 e2e 引用**；`GaugeChart` 全仓（除自身/components.d.ts）零引用，是最干净的死组件。


### 6.1 候选覆盖与未验证项（控制规模，非全量背书）

四份报告合并后的「合并/精简/删除」候选约 37 条（R2-C1..C13 13 条、R3-C1..C12 12 条、R1 三张孤儿表隐含的删除候选约 8 条、R4 6.1/6.2 的文案与字段合并建议 4 条）。按任务「只对风险最高的 8-10 条验证真实引用」的要求，本次深度验证 10 条（§6-V1..V10），另 5 条做了去重闭环抽查（§7-D1..D5）。其余约 22 条明确**未做真实引用验证**，不得据本复核直接执行：

| 未深度验证的候选 | 不验证的原因 | 执行前必须补 |
|------------------|--------------|--------------|
| R2-C2 加载/空态 AsyncSection 收敛 | 跨 12 页样式/DOM 层，风险集中在界面回归而非引用误杀 | 逐页截图/mount 测试基线 |
| R2-C7/C12 状态映射/标签组件化 | 需先统一后端枚举，前端单改会错映射 | 契约评审 |
| R2-C8 大文件按 tab/列表拆分 | 纯重构，不删引用；风险是测试锚点漂移 | `NodeOverviewPalette.spec` 等锚点同步方案 |
| R2-C11 DeviceHeader 编辑/删除收敛 | 依赖 C4 结论；含 session generation 防竞态语义 | 保留 `assertSessionGeneration` 的等价测试 |
| R2-C13 `components.d.ts` 注册面收口 | 属构建配置（`vite.config.ts:33-38`）+ 自动生成物，不是业务源码 | 先确认 unplugin 扫描范围策略 |
| R3-C4..C10 深链/筛选/schema/导出统一 | 多为新增接线，不删现有引用 | 各自 mount/e2e |
| R3-C11 初始化后自动登录 | 认证契约变更 | 后端安全评审 |
| R1 表一/表二 51 条无消费端点处理 | 高风险误判 H6；其中 FR 相关不可删 | 逐条 FR 映射 + 后端能力台账 |
| R4 T1-T11/S1-S7/A1-A5 文案词表 | 不涉及删代码，属规范冻结 | 术语表/UI 规范评审 |

**结论**：本复核只对 §6 表中的 10 条候选给出可执行裁决；未验证候选保留原报告「提案」状态，不因本复核视为可信。

---

## 7. 已闭环去重检查（抽查 5 条 + 候选落点）

**抽查 R2 §5「不再报」清单 3 条**：

| # | 清单条目 | 独立复算 | 结论 |
|---|---------|----------|------|
| D1 | #5 `confirmDanger` 已闭环（21 处/9 处裸 confirm） | 非测试全 src：`grep -rn confirmDanger src --include=*.ts --include=*.vue \| grep -v __tests__ \| wc -l` = **21**；`ElMessageBox.confirm` 同法 = **9** | **一致，确已闭环**；R2 用 views+components 得 17 处并解释了差额（utils 定义+dev 页+注释），口径自洽，不是重复报 |
| D2 | #3 automation/logical-device 假分页已闭环 | `handler_automation.go:484 Offset((page-1)*pageSize).Limit(pageSize)`；`handler_logical_device.go:69` 同形；前端两页 `el-pagination`（AutomationRules:154、LogicalDeviceList:110） | **一致，确已闭环**；R2/R3 均未把「假分页」列为新问题 |
| D3 | #6 `@media (pointer: coarse)` 已闭环 | `grep -n '@media (pointer: coarse)' theme.css` = 2 处（:795、:944）；块内 `min-height: 44px` 出现 15 次 | **一致，确已闭环**；未见候选重复报 F6 |
| D4 | #7 侧栏 roving tabindex 已闭环 | `MainLayout.vue:30 :tabindex="idx === sidebarFocusIndex ? 0 : -1"`，:552-555 有实现注释；`uiux-metrics.ts:263` 统计 tabindex=0 | **一致，确已闭环**；R2-C9 只提示「改 submenu 会破坏该门禁」，属正确引用而非重复报 |
| D5 | #1 失败态三件套「部分闭环」 | NodeList/DeviceConfigList/LogicalDeviceList/DataPanel/AlertRules/FirmwareManage/AutomationRules 七页 `kind="error"|hasError|loadError|errorState` 均 **0**；Dashboard/EdgeDeviceList/ChannelList/data-source/Monitor 已接线 | **一致，确为部分闭环**；R2 §4-C2 引用而非单列缺陷，处理正确 |

**候选落点判定**：
- R2-C1/C2/C4/C5/C6/C8/C9/C10/C11/C12/C13 相对 09-13 文档属**新识别**（09-13 只有 F1-F18 问题族 + 少量线索），未发现「把已闭环项当新问题」。
- 例外提示：R2-C4 的「删除确认四套实现」以 09-13 的 F5 为基线（报告已注明），不构成重复；R3-U4「导出当前页 20 条」与 09-13 F14「统计卡范围词」是**同根因不同出口**（F14 已闭环只覆盖统计卡范围词，导出按钮无范围说明未闭环），但不应当作已闭环项忽略。
- R3-C2「`/firmware` 接线 OTA 任务创建」与 R2-C10 无关，不与任何 09-13 项冲突。

---

## 8. 矛盾清单

| # | 冲突点 | 四份报告说法 | 代码事实 | 判定/建议 |
|---|-------|--------------|----------|----------|
| X1 | views 41 vs components 40 vs 任务书 38 | R2 §6.1 已解释 40 vs 38；R1 用「前端生产 .ts/.vue 164」是另一口径（含 api/stores/utils/router） | `find src/views` = 41；`find src/components` = 40；`164` = src 全量非测试 ts/vue | **已解释**：三个数字口径不同，不构成矛盾；但 R1 未在正文显式说明 164 与 41+40 的关系，建议补一句「164 含 api/stores/utils/router，非 views/components 之和」 |
| X2 | 端点 178 vs R1 自报 187 | R1 正文 178，附录 187（含 503 占位与公开下载） | 真实生产可达 = 187 - 10（data-source unavailable 分支 9 + failover unavailable 1）+ `/ping` = 178 | **已解释但不完整**：报告 §1.2 只写「含 routes.go 6 条与公开下载」未写扣除 503 占位；建议在算式处补「187 − 10 占位 = 177 + /ping = 178」 |
| X3 | `routes.go` v1 显式注册 23 vs 24 | R1 §1.2 注释 23 | 实际 `grep -E '^\s*register[A-Za-z]+\(v1'` = 24（含 `registerFailoverLogRoutes`） | **数字错误**：应改 24，或说明按「不含 failover 子注册」口径为 23（当前未说明） |
| X4 | `from '@/api/` import 98 vs 97 | R1 §1.3 注释 98 | 单双引号合计 97 | **数字错误**：应改 97（若坚持 98 需给第二条命令/文件路径） |
| X5 | R2 一句话结论「5 个零消费组件（连带 4629 行不可达子树）」 | C5 实际列 9 个文件；其中 `NodeDetail/ChannelPanel/GPIOResourceList/PWMResourceList` 不是零消费组件（有内部/单测引用） | 零 import+零模板组件的严格集合是 **5 个**（GaugeChart/ParserBrowser/PeripheralControl/GPIOPinRow/PWMChannelRow）；4629 行是「9 文件不可达子树」 | **部分成立（表述不严谨）**：应改为「9 文件 4629 行不可达子树，其中 5 个组件零消费」；否则「5 个零消费组件 = 4629 行」会导致读者误算收益 |
| X6 | R2-C5 证据段「GPIOPinRow/PWMChannelRow/PeripheralControl/GaugeChart/ParserBrowser 双 0」是否准确 | C5 文件清单含 9 个；§3 矩阵给 `GPIOResourceList/PWMResourceList` 消费点=2 | C5 证据段仅列这 5 个双 0 组件，与 §3.1 完全对应；`GPIOResourceList/PWMResourceList` 属「两级死亡链」另列。两处本身无误，但一句话结论把「5 个零消费组件」与「9 文件 4629 行」并置易被误读为 5 个组件即 4629 行 | **成立（表述需拆句）**：C5 正文正确，建议把一句话结论拆成「5 个零消费组件 + 4 个两级死亡链文件，合计 9 文件 4629 行」 |
| X7 | R3 流程 11 称 `PeripheralControl.vue:16-38` 未传 `registerPending`；R3-C1 称组件“已具备” | 两个说法同页并存 | `PeripheralControl` 自身未实现 pending 等待，`GPIOResourceList` 有 `registerPending` 回调能力但未接 | **已解释**：C1 风险栏已写「需补 registerPending」，不是矛盾；建议把「组件已具备」改为「行控件已具备，容器缺 pending」 |
| X8 | R4 执行摘要 33 项 vs §7 类型计数 35 项 | 摘要 11+4+3+5+10；§7 12+10+13 | 两套计数均基于同一批表，但分类归属不同（T7/A5/M2/M3 归组差异） | **部分成立**：建议加一行对账（11+4=15 术语、T7 入文案、A5/M1 入IA 等），否则读者会认为少报 2 项 |
| X9 | R3 流程 4 表头「批删仅在表格视图可见」 vs 候选 2「批删改为跨视图可用」 | 事实与建议 | `EdgeDeviceList.vue:106-120` 批量栏在 `viewMode==='table'` 条件内；卡片视图无选择 UI | **不矛盾**；仅提醒：移动端默认卡片视图，跨视图选择会增加误触风险，R3 风险栏缺失该点 |
| X10 | R2-C4 称有「4 套删除实现」，列了 #1 confirmDanger/#2 DeviceDeleteDialog/#3 Batch/#4 DeviceHeader | 09-13 已闭环 F5（confirmDanger 统一） | 事实成立：#1 是统一出口，后 3 套是专用对话框；`listPageConventions.spec.ts:401-406` 明确把 #2 登记为“不应替换为 confirmDanger”的结构性豁免 | **成立但有误导**：把 confirmDanger 算作“一套”会与 #2/#3 的豁免冲突；建议改述为「3 套专用删除对话框（含确认出口差异）」 |

---

## 9. 高风险误判清单（若照做会造成功能丢失/用户可感回归）

| # | 误判来源 | 危险动作 | 具体功能损失 | 必须前置条件 |
|---|---------|----------|--------------|--------------|
| H1 | R2-C5 / R3-C2「整树删除 4629 行」 | 一次性删除 9 文件及其测试 | ① `NodeDetail.vue` 的 `OTA_STATUS_TYPES` 是 `OtaStatusTruthSource.spec.ts:5` 反向校验后端 8 状态的真源之一，删除后该门禁失效；② `NodeDetail.vue:751` 是 `ClickableSemantics.spec.ts:108` 的点击语义样本；③ `ChannelPanel` 4 个 spec 覆盖通道编辑/外设编辑/UART 重配置/路由；④ `ChannelPanel` 的 GPIO/PWM 硬件资源、DMA 绑定、终端能力当前**没有**生产等价入口（`ChannelManager` 只做通道 CRUD，`NodeOverview` 总线页对 GPIO/PWM 只显示“不支持通道”） | 先迁移 4 个 spec 的语义断言到 `NodeOverview`/`ChannelManager`；先确认 GPIO/PWM 配置编辑能力有生产入口（V10）后再删 ChannelPanel |
| H2 | R2-C6 / R3-U2「双路由加 redirect 或删 `/node/:id`」 | 只保留 `/node/:id/overview`，删除 alias | `ChannelList.vue:444` 用 `name:'NodeDetail'` 推路由；外部书签/通知/后端消息可能使用 `/node/:id`；删除或强制 redirect 可能让旧深链失效，通道列表“点行查看节点”可能 404 | 保留 `name:'NodeDetail'` 的 alias/redirect，内部消费改为显式 `/node/:id/overview`；验证 `/node/:id` 重定向后再删 |
| H3 | R2-C9 / R4-M1「立刻改 5 组 submenu」 | 直接把平铺 `el-menu-item` 换成 `el-sub-menu` | `MainLayout.spec.ts:134`、`MainLayoutKeyboardNav.spec.ts:132`、`MainLayoutDrawerFocusTrap.spec.ts:114` 断言 14 个 `.el-menu-item`；`e2e/helpers/uiux-metrics.ts:256-264` 与 `uiux-regression.spec.ts:817-822` 基于平铺项计可聚焦数——直接改会让键盘可达门禁整批红，且在门禁改造完成前无法区分“真回归/假红” | 先改 `uiux-metrics`、e2e 断言、3 个 vitest 为 submenu-aware；再动菜单 |
| H4 | R3-C1/R3-C12、R1-B 类判定「接线 `PeripheralControl` 即可恢复 GPIO/PWM」 | 直接把组件挂到节点详情并宣告功能恢复 | 组件只提供 UI/API 调用，**未接 `registerPending`/WS 状态回填**；写 GPIO/PWM 后设备 ACK 不会驱动 UI 更新，用户会看到“点了没反应/状态回跳”；且 GPIO/PWM 的创建配置入口当前同样不可达 | 先在 `PeripheralControl` 接 `registerPending` 并补 WS 回填；补创建/编辑资源配置入口或确认复用 `ChannelPanel` 能力；加危险操作确认 |
| H5 | R2-C10「通知两页合并」 | 按候选合并 `/notification-channels` 与 `/notification-deliveries` | 投递审计是只读审计页、有独立 `state/channel_id` 过滤与深链价值；合并会改变权限语义与外部可引用 URL，且要重做两套 API/分页 | 报告自己的反证已足够：**撤回合并**，只做菜单相邻/互跳 |
| H6 | R1-B 类（51 条无前端消费）被直接当作“可删后端端点” | 删除 37 条无包装端点（vendors、device-models、test-parser、rollback、/nodes POST、logout-all 等） | 其中 FR 明确要求的能力（FR-A3 全部终端登出、FR-D3 parser 在线测试、FR-E3 init、FR-I2 PWM 频率、OTA rollback）是**后端已实现、前端未接线**，删除后端会丢失能力后再补成本更高；vendors/device-models 是未接线元数据域 | 只可清理真正重复/占位端点（如 `/devices/:id/*` 旧数据端点、`GET /ota/status` 与既有进度重叠部分需逐一判定），FR 相关端点保留并接线 |
| H7 | R3「dry-run 不存在 = 任务书期望缺失」被降级为一般候选 | 只加前端参数预览，不补后端 CompilePlan | `docs/设计/总体设计.md:93`、`边缘设备控制.md:62-67`、`功能需求总览.md:79`（FR-E5 已勾 ✅）要求 dry-run 展示“预计步数/读回校验点/风险分级”；仅前端渲染参数表不等于 dry-run，**验收缺口不会被关闭** | 明确定义 dry-run 契约（是否复用 `CompilePlan`）；前端预览与后端编译结果对齐，否则不要宣称补齐 |
| H8 | R2-C1 抽取列表页壳时顺带改分页绑定 | 抽壳动 `el-pagination` 绑定 | `listPageConventions.spec.ts` 是 12 页分母门禁（`:22-33` 明写新增第三种 component 写法必须同步解析器），抽壳若引入新写法/改变 `:total` 来源会导致门禁假红/漏检 | 先抽统计卡+工具栏（无分页语义），分页绑定后置；同步门禁解析器 |

---

## 10. 逐条结论汇总（成立/部分成立/不成立/无法复现）

### 10.1 R1

| 结论 | 判定 | 修正建议 |
|------|------|----------|
| 基线/规模/端点 178/分类互斥求和/FR=63/promauto 46 | 成立 | 仅补 187→178 算式与 /ping 来源 |
| 「v1 显式注册 23」 | 不成立（应为 24） | 改 24 或补口径说明 |
| 「api import 98」 | 不成立（应为 97） | 改 97 |
| 29 个零消费者 API 导出（抽样 7/7） | 成立 | 建议对死包装统一标注 `@deprecated`，而非只列清单 |
| `getOperationHistory` 必 404、`unifiedDataApi` 前缀错误 | 成立 | 删除方法或修正 URL 后接入；不能让两套 URL 语义并存 |
| `NodeDetail/ChannelPanel` 不可达、B 类 17 端点无生产入口 | 成立（静态） | 报告 §8.9 已声明可能被构建期动态解析推翻；本次复核补强：`componentMap`/loader/glob/async 组件均无这些名字，静态判定可信度高 |
| 「registerAuthRoutes 不在生产图内」 | 成立 | 无需修改 |

### 10.2 R2

| 结论 | 判定 | 修正建议 |
|------|------|----------|
| 41/40/21418/11628/4629/14 项菜单等计数 | 成立 | — |
| 5 个零消费组件 + 9 文件 4629 行不可达子树 | 部分成立 | 拆成两个集合表述（见 X5/X6）；C5 删除前先迁移 4 个 spec 并确认 GPIO/PWM 能力覆盖 |
| C1 列表壳 | 部分成立 | 范围从「12 页」缩到「4 页统计卡/3 页工具栏」；不得先动分页绑定 |
| C3 双创建路径 | 部分成立 | 只抽共享模型，不合并页面 |
| C4 删除确认收敛 | 成立（事实）/部分成立（方案） | 不能把 confirmDanger 与专用对话框混合计数；DeviceDeleteDialog 是门禁登记的结构性豁免，只能合并 #2/#4 |
| C6 双路由规范 URL | 成立 | 采纳最小动作（alias 保留 + 内部统一 /overview） |
| C9 菜单分组 | 成立（事实） | 必须在门禁 submenu-aware 后执行 |
| C10 通知合并 | **不成立（撤回）** | 报告自身反证已足够 |
| C12/C13 状态标签/组件注册面 | 成立 | C13 与 `components.d.ts` 实测 40 条一致 |
| §5 去重 19 条 | 成立 | 抽查 3 条全部确已闭环 |

### 10.3 R3

| 结论 | 判定 | 修正建议 |
|------|------|----------|
| 死入口（action=add、?tab=config、?node=、/firmware?node=） | 成立 | 路由 query 消费是实现级修复，可先做 |
| dry-run 缺失 | 成立（事实） | 升级为 FR-E5 验收缺口，不是“任务书期望”软项；需后端 CompilePlan 契约 |
| 数据面板导出当前页 20 行 vs 详情页图表序列 | 成立 | 在按钮/文件名标注范围；统一导出口径属 P2 |
| 告警事件无筛选/无处置、U9 ids:[] 必 400 | 成立 | 修 U9 一行接线（`markAllEventsRead` 已定义）；筛选复用已有参数 |
| GPIO/PWM 不可达 | 成立 | 接线前先补 pending/WS（H4） |
| 「节点接入 UI 整体缺失」 | 部分成立 | `POST /nodes` 存在，但节点实体按代码是设备 Hello 上线落库；接 UI 需定义 node_id/name/config 语义，不能只说复用端点 |
| OTA 回滚无前端、无全局任务列表 | 成立 | 回滚属高风险，接线必须强确认+在线校验 |
| 步数计数（12 流程） | 部分可复现 | 控件构成已抽样核对（流程 5/6/9/11），整数分因口径为人工判定，本次不逐项重算；建议保留「口径表」并标注 ±1 不确定性 |
| 流程 4 单删「3 步」与详情页删除「不一致」 | 部分成立 | 单删确认框确实含数据处置 radio（+1 决策条件），报告正文已说明默认值，表头 0 决策正确；建议在汇总表加脚注 |

### 10.4 R4

| 结论 | 判定 | 修正建议 |
|------|------|----------|
| 动作词/状态词裸词计数 | 成立 | — |
| T1-T6/T8、S2/S3/S5 | 成立 | 术语表补充/dis 类文案收敛 |
| T7/S1/S4/S6/S7 | 成立（文案风格） | 不阻塞 |
| T9/T10/T11/F1-F10 契约字段事实 | 成立 | 全部标「需契约评审」正确；不应前端单改 |
| M2 缺页面标题、M3 三套标题 | 成立 | 与 `listPageConventions.spec.ts` 的 EXEMPT_PAGE_HEADER 登记一致（NodeList/EdgeDeviceList 是已登记豁免，不是新缺陷） |
| 摘要 33 vs §7 35 | 部分成立 | 加对账行 |
| 「不建议现在做」清单（collector 改名、device_type 改名、state→status、UNKNOWN） | 成立 | 建议直接采纳 |

---

### 10.5 抽样结论状态矩阵（实际抽样 24 条：每份 6 条）

| # | 抽样结论（来源） | 状态 | 复算方式 |
|---|-----------------|------|----------|
| 1 | 基线 commit/branch（R1） | 成立 | `git rev-parse HEAD` / `branch --show-current` |
| 2 | Go 224/68833、前端 164/44628（R1） | 成立 | `find`+`wc -l` |
| 3 | 端点 178 / 方法分布 / 分类求和（R1） | 成立（算式需补） | 187−10+1；域表求和 |
| 4 | routes.go v1 注册 23（R1） | **不成立（24）** | `grep -E '^\s*register.*\(v1' \| wc -l` |
| 5 | api import 98（R1） | **不成立（97）** | `grep "from '@/api/"` |
| 6 | 导出 159（HTTP 149）、域明细（R1） | 成立（解析边界） | 对象/函数/箭头独立解析 + 域表求和 |
| 7 | views 41/21418、components 40/11628（R2） | 成立 | `find`+`wc -l` |
| 8 | C5 九文件 4629 行（R2） | 成立（行数）/部分（处置） | `wc -l` 九文件；引用验证见 §6-V1 |
| 9 | 菜单 14 项、el-sub-menu 0（R2） | 成立 | sed 菜单数组 + grep |
| 10 | confirmDanger 17 处/11 文件（R2） | 成立 | `grep -rn` + `-l` |
| 11 | §2.2 12 页控件矩阵（R2） | 成立 | 12 文件逐项计数 |
| 12 | §5 去重清单与 17 处口径自洽（R2） | 成立 | 读 C4 正文 + 复算 21/17 |
| 13 | 14 菜单 + 搜索 8 目的地（R3） | 成立 | 读 MainLayout 菜单/搜索数组 |
| 14 | action=add 死入口（R3） | 成立 | 全仓 grep 仅产生无消费 |
| 15 | dry-run 全仓业务源码 0（R3） | **事实成立，定性部分成立** | grep 0；但 FR-E5 已勾 ✅，应升级为验收缺口 |
| 16 | 数据面板导出当前页 20 行、详情页导出图表序列（R3） | 成立 | 读两处导出循环 + pageSize |
| 17 | 告警事件无筛选、两表真分页（R3） | 成立 | 读事件模板 + `el-pagination` |
| 18 | /firmware 不消费 ?node=（R3） | 成立 | 全文件 0 命中 useRoute/route.query |
| 19 | 动作词 15 项计数（R4） | 成立 | 逐词 grep 抽样 7 项全同 |
| 20 | 状态词 12 项计数（R4） | 成立 | 逐词 grep 抽样 6 项全同 |
| 21 | 两列表页缺 PageHeader、通知通道页头不一致（R4） | 成立 | grep PageHeader + 文案 |
| 22 | hardware_id 同名异物（R4） | 成立 | 读前后端字段注释 |
| 23 | 投递 state 与筛选标签（R4） | 成立 | grep json:"state" + 页内标签 |
| 24 | 摘要 33 项 vs §7 计数 35 项（R4） | **部分成立（需对账）** | 叠加两张表，分类归属不同 |

- **通过率**：24 条中「成立」20 条（83.3%）、「部分成立」2 条（8.3%，R3-15 定性、R4-24 对账）、「不成立」2 条（8.3%，R1-4/R1-5 两个展示性数字）。0 条「无法复现」；但 R3 的步数整数分、R2 的运行时可达性不在本次静态取样范围，未用「成立」背书。
- 补充说明：每份实际抽样 6 条 > 任务要求 5 条，总计 24 > 20；「不成立」均只影响附录展示数字，不影响任何主体结论。

### 10.6 四份报告可直接执行的修正清单

**R1（功能盘点）**
1. `routes.go` v1 显式注册数 23 → **24**（逐行命令已给，避免下一轮审计再抄错）。
2. `from '@/api/` import 语句 98 → **97**（若坚持 98，需补第二条命令与命中文件）。
3. §1.2 端点段补算式：**原始注册 187 − data-source 两套 503 占位 10 条 + main.go `/ping` 1 条 = 178**；注明 `187` 的 grep 同时统计到真实分支与 unavailable 分支。
4. §5.2a 中 `getOperationHistory` 与 `unifiedDataApi` 五方法建议标注「必 404/必 404 或前缀错误，接线前先修」，避免下游当作可用能力。
5. 在 §3.1 对 `registerAuthRoutes` 的说明旁补一句「与 `registerAccountRoutes` 是等价实现，仅测试引用」——当前已给结论，但容易被误读为缺失。
6. 对 B 类 17 端点补「基于静态 import 图（componentMap/loader/glob/自动注册均排查，无动态引用）」的强化说明（见本复核 §6-V1/V9）。

**R2（UI/IA 审计）**
1. 一句话结论与 C5 拆成两个集合：**零消费组件 5 个**（GaugeChart/ParserBrowser/PeripheralControl/GPIOPinRow/PWMChannelRow）与**不可达子树 9 文件 4629 行**；现状表述会让人把 5 与 9、4629 直接乘除。
2. C5 删除建议补前置硬条件：4 个 `NodeDetail.spec?raw` 语义断言先迁移、4 个 `ChannelPanel` spec 先确认等价覆盖、ChannelPanel 的 GPIO/PWM/DMA/终端能力先确认生产等价入口；否则不得执行删除。
3. C1 的「12 个列表页各写一套壳」改为「4 页共享 stats-row、3 页共享 toolbar，其余页面结构不同」；候选范围同步缩小。
4. C3 更名「抽共享表单模型/composable」，并删去「先抽模型再抽 UI」中隐含的页面合并；保留反证段（信息密度不同）。
5. C4 的「四套实现」改述为「confirmDanger 统一出口基线 + 3 套专用删除对话框」；`DeviceDeleteDialog` 已在 `listPageConventions.spec.ts:401-406` 登记为结构性豁免，不可并入 confirmDanger。
6. C9 必须保留「先 submenu-aware 化 `MainLayoutKeyboardNav`/`MainLayoutDrawerFocusTrap`/`TouchTargets`/e2e `uiux-metrics` 的 14 项断言，再动菜单」；本次已独立复算 3 个 vitest + 1 个 e2e 度量依赖平铺项。
7. **撤回 C10 通知两页合并**，改为「菜单相邻/互跳保留审计独立 URL」。
8. §6.1 补一句：任务书原始文件不在当前工作区，38 vs 40 的差额无法点名，只能按实测 40 输出。

**R3（流程审计）**
1. dry-run 段落措辞从「任务书期望项缺失」升级为「`功能需求总览.md` FR-E5 已标 ✅，实现与验收不符，属需求验收缺口」；候选必须以后端编译结果预览（CompilePlan）为契约，不能只做前端参数表。
2. 流程 2 节点接入段落补事实：节点实体按代码由设备 Hello 上线落库；接 UI 需定义 `node_id/name/config` 语义与 409 冲突文案，不只是「复用 POST /nodes」。
3. 步数表加脚注：整数分为人工口径，控件构成可复算，±1 属可接受区间（本次抽样复算了流程 5/6/9/11 的控件构成）。
4. 流程 4 单删「0 决策」正确（数据处置默认保留），但建议脚注写明「改选删除历史数据时 +1 决策」。
5. C1 外设直控补前置：`PeripheralControl` 需先接 `registerPending`/WS 回填，否则写后状态不刷新。
6. 流程 7「只能翻 N 页」建议改「无筛选，但可用 page_size 20/50/100 降低翻页量」。
7. 流程 9 OTA 回滚候选补「必须在线校验 + 强确认 + 回滚失败恢复」的验收条件（当前风险栏写“高”但未给恢复路径）。

**R4（语义审计）**
1. 摘要 33 项与 §7 的 35 项加对账行：按组计数（11 T + 4 S + 3 M + 5 A + 10 F）与按条目分类计数（12 术语冲突 + 10 文案 + 13 契约）因 T7/A5/M1/M2/M3 归属不同而差 2。
2. 第 2 节标题一致性表下方补注：`/node`、`/edge-device` 缺 PageHeader 与 `listPageConventions.spec.ts:414-421` 的 `EXEMPT_PAGE_HEADER` 登记同源，属已登记缺口而非新缺陷。
3. T4/T8 建议保留“需契约评审”标签，同时补一句「UI 文案可按术语表先行，字段名与路径不动」。
4. T10/F6 建议补上 `docs/设计/术语表.md:55` 的权威表述（当前引用 §57 与 model 注释，可再加一句术语表已裁定关系以 unique 约束为准）。


---

## 11. 总体可信度评级

| 报告 | 事实密度 | 数字准确度 | 引用可靠性 | 建议质量 | 综合 |
|------|---------|-----------|-----------|----------|------|
| R1 功能盘点 | 高 | 高（两处展示数字错） | 高（抽样 7/7） | 高（差异表谨慎） | **A-**（修正 24/97 与算式后 A） |
| R2 UI/IA 审计 | 高 | 高（计数全对） | 中高（C5/C1 表述夸大、C10 应撤回） | 中（C5 删除风险、C9 顺序） | **B+**（修正 X5/X6/C10 后 A-） |
| R3 流程审计 | 高 | 中高（步数人工口径） | 高（抽样 6/6 事实成立） | 高（候选多数给反证） | **A-**（dry-run 升级为 FR 缺口、步数标注不确定性后 A） |
| R4 语义审计 | 高 | 高（词频全对） | 高 | 高（区分契约/文案） | **A-**（对账 33/35 后 A） |

- **四份报告整体可信**：无发现“凭空造数”或“把不存在端点写进清单”的严重错误；核心结构判断（死入口、双路径、不可达子树、契约同名异物）均能独立复现。
- **最高风险不在事实，而在执行建议**：C5 整树删除、C9 菜单先改、C10 通知合并、B 类端点清理、dry-run 只做前端——若直接照做分别会造成测试语义丢失、门禁整批红、审计 URL 破坏、未接线 FR 能力被删、验收缺口假关闭。
- **可复现性**：所有计数命令均可在仓库根重跑；涉及的 `/tmp` 脚本输出非本次裁决依据，以命令输出为准。

---

## 12. 附录：本次抽样与验证的原始命令/输出（节选）

```text
# R1
$ git rev-parse HEAD && git branch --show-current
7a3522b102529cf365ffe7f2906422284b3e495e
main
$ find backend -name '*.go' ! -name '*_test.go' | wc -l
224
$ find frontend-shared/src -type f \( -name '*.ts' -o -name '*.vue' \) ! -path '*__tests__*' | wc -l
164
$ grep -rnE '^\s*register[A-Za-z]+\(v1' backend/internal/api/routes.go | wc -l
24
$ grep -rn "from '@/api/" frontend-shared/src --include='*.vue' --include='*.ts' | grep -v '/__tests__/' | wc -l
97
# 真实可达端点 = 187 原始注册 - 10 data-source 503 占位 + 1 /ping = 178
$ python3 - <<'PY'
print(187-10+1)  # 178
PY
178

# R2
$ find frontend-shared/src/views -name '*.vue' -not -path '*/__tests__/*' -not -path '*/dev/*' | wc -l
41
$ find frontend-shared/src/components -name '*.vue' -not -path '*/__tests__/*' | wc -l
40
$ wc -l <C5 九文件> | tail -1
4629 total
$ sed -n '397,412p' frontend-shared/src/views/layout/MainLayout.vue | grep -c "path: '/"
14
$ grep -rn 'el-sub-menu' frontend-shared/src --include='*.vue' | grep -v __tests__ | wc -l
0
$ grep -rn "confirmDanger" frontend-shared/src/views frontend-shared/src/components --include='*.vue' | grep -v __tests__ | wc -l
17
$ grep -rln "confirmDanger" frontend-shared/src/views frontend-shared/src/components --include='*.vue' | grep -v __tests__ | wc -l
11

# R2 C5 引用验证（关键输出）
$ grep -rn 'NodeDetail\.vue' frontend-shared/src --include='*.ts' --include='*.vue' | grep -v components.d.ts
...(仅 4 个 spec 的 ?raw/mount + 注释)
$ grep -rn 'ChannelPanel\.vue' frontend-shared/src --include='*.ts' --include='*.vue' | grep -v components.d.ts
NodeDetail.vue:444:import ChannelPanel ...
...(4 个 ChannelPanel spec)
$ grep -rnE 'defineAsyncComponent|componentMap|import\.meta\.glob' frontend-shared/src --include='*.ts' --include='*.vue' | grep -v __tests__
EdgeDeviceDetailRouter.vue:33-38 (只映射 Bms/Inverter/Generic), router 懒加载若干路由
（无 GaugeChart/ParserBrowser/PeripheralControl/GPIOPinRow/PWMChannelRow/NodeDetail/ChannelPanel 的字符串键引用）
$ grep -cE "typeof import\('\./components/" frontend-shared/src/components.d.ts
40

# R3
$ grep -rn 'action=add\|query\.action' frontend-shared/src --include='*.vue' --include='*.ts' | grep -v __tests__
NodeList.vue:73: router.push('/node?action=add')
（无消费）
$ grep -rn 'dry_run\|DryRun\|dryRun\|dry-run' backend/internal frontend-shared/src --include='*.go' --include='*.vue' --include='*.ts' | grep -v __tests__
（0 行）
$ grep -n 'useRoute\|route.query' frontend-shared/src/views/firmware/FirmwareManage.vue
（0 行）
$ grep -n '<el-pagination' frontend-shared/src/views/alert/AlertRules.vue
115
$ sed -n '88,108p' frontend-shared/src/views/alert/AlertRules.vue
（事件表：状态/规则/值/触发时间/恢复时间，无筛选控件）

# R4
$ for w in 创建 新建 添加 删除 保存 编辑 修改; do printf '%s ' "$w"; grep -rho "$w" frontend-shared/src/views frontend-shared/src/components --include='*.vue' | wc -l; done
创建 142 / 新建 38 / 添加 19 / 删除 136 / 保存 61 / 编辑 110 / 修改 23
$ for w in 在线 离线 警告 故障 异常 未知; do printf '%s ' "$w"; grep -rho "$w" frontend-shared/src/views frontend-shared/src/components --include='*.vue' | wc -l; done
在线 45 / 离线 63 / 警告 14 / 故障 15 / 异常 19 / 未知 58
$ grep -n 'json:"state"' backend/internal/models/notification_channel.go
251:State string ... json:"state"
```

---


---

## 13. 复核不确定性与边界

1. **静态 vs 运行时**：本次全部“不可达/死代码/可达”为源码引用图判定，已排查动态 import、`defineAsyncComponent`、`componentMap` 字符串键、router loader、`components.d.ts` 自动注册、kebab-case 模板标签、测试 mount、注释命中八类伪引用；但未构建运行，理论上不能排除构建插件在字符串模板运行时解析组件（R1 §8.9 已声明同类边界）。
2. **单测未运行**：本复核未执行 vitest/e2e（任务硬性禁止），H3 中“门禁会红”依据是源码断言文本（`toHaveLength(14)` / `.el-menu-item` 选择器），不是实际跑测结果；执行改造前仍应先跑门禁确认红。
3. **步数为人工口径**：R3 的 1/2 步差与“点击数”定义相关，本次只核对了 4 个流程的控件构成；复核结论接受 ±1 不确定性，不把步数整数当作精确可复现量。
4. **任务书 38 vs 40 无法点名差额**：任务书原始文件不在工作区，`find` 实测 40 是唯一可复现事实；被排除的 2 个若是 `components.d.ts`/dev 相关也不影响 40 的结论，但无法判定任务书是否排除其他目录。
5. **外部深链不可证**：C6 中 `/node/:id` 是否被外部书签/通知/后端消息引用无法通过静态仓库证明，故建议保留 alias/redirect 而非直接删除。
6. **`getOperationHistory` 必 404**的判定基于后端路由表 + 测试断言，未实际发请求（禁网络），与 R1 同边界。
7. **`/ping` 归属**：178 中的 `/ping` 来自 `backend/cmd/server/main.go:457`，不在 `backend/internal/api` grep 范围；R1 已计入公开端点，本次复算与其一致。

---

## 14. 一页速览

- **四份报告事实可信度：高。** 24 条抽样中 20 条成立、2 条部分成立、2 条不成立（均为展示性数字）；未发现虚构端点/虚构文件/虚构测试。
- **8-10 条高风险候选验证：** V1 部分成立（不可达成立、整树删除不成立）、V2/V3/V10 部分成立（事实成立、方案需收紧）、V4/V5/V7/V8/V9 成立；**V6（C10 通知合并）不成立，应撤回**。
- **去重：** 抽查 5 条 09-13 闭环项全部确已闭环；R2/R3 新候选未发现重复报旧问题。
- **矛盾：** 10 处，其中 2 处是报告数字错（R1 24/97）、1 处应撤回候选（C10）、其余为表述/口径需拆句或补对账，不影响主体结论。
- **高风险误判 8 条：** 整树删死代码、删 `/node/:id`、先改 submenu、直接接线外设控制、合并通知两页、按 B 类清单删后端端点、dry-run 只做前端、抽列表壳动分页绑定。
- **总体评级：** R1 A-、R2 B+、R3 A-、R4 A-；四份均可作为下一步输入，但 R2 的 C5/C9/C10 与 R3 的 dry-run/节点接入表述必须按 §10.6 修正后执行。

> **复核边界**: 本次未跑任何测试/build/Playwright/网络，所有「不可达/死代码/可达」判定均为静态源码引用图结论（已额外排查动态 import/异步组件/字符串键/自动注册/loader）。R1-R4 中依赖运行时行为的断言（步数体感、元素遮挡、真实 API 响应）本次不重复验证，按原报告「未跑测试」边界处理。
