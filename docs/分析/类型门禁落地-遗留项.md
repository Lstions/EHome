# 前端类型门禁落地 —— 遗留项清单

> 生成：2026-09-12 ｜ 基线 main `bb4bc491` ｜ 由主 Agent 从 `npx vue-tsc --noEmit` 实测输出生成

> 关联：[架构与接口评估及优化方案-2026-09-11.md](架构与接口评估及优化方案-2026-09-11.md) P0-0.6


## 背景与当前状态

前端 `typecheck` 长期是裸 `tsc --noEmit`，而 tsc **完全不检查 `.vue`** —— 实测编译图中 `src/*.ts` 167 个、`src/*.vue` **0 个**，84 个 SFC（约 78% 代码）零类型检查。

| 阶段 | 错误数 | 说明 |
|---|---|---|
| 门禁落地前 | 165 | 首次接入 vue-tsc 时的全部存量问题 |
| **当前（本次已提交）** | **102** | **已修 63 处**；含 3 个真 bug（见下） |
| 目标 | 0 | 清零后方可把 `typecheck` 切到 vue-tsc（见「切换前置条件」） |

## ⚠️ 切换前置条件（硬约束）

`package.json` 的 `build` = `pnpm typecheck && vite build`，且 `Dockerfile:35` 执行 `pnpm build`、`docker-publish.yml` 在 push main 时按 `frontend-shared/**` 触发镜像构建推送。
因此：**错误清零之前，`typecheck` 必须保持 `tsc --noEmit`**（当前已是此状态），否则生产镜像构建立即失败、发布流水线中断。

## 本次已修复的 3 个真 bug（类型门禁的直接产出）

| # | 位置 | 问题 | 修法 |
|---|---|---|---|
| ① | `views/dashboard/Dashboard.vue:337` + 后端 `handler_overview.go:51/113` | 读 `latest_data[].error_code`，而后端 `latestEntry` 结构体**无该字段** → `dataErrorCount` 恒为 0 → Dashboard「数据错误」告警维度**永不触发** | 后端 `latestEntry` 补 `ErrorCode int json:"error_code"` 并从 `dev.ErrorCode` 填充；前端改 `(d.error_code ?? 0) > 0` |
| ② | `components/common/NetworkBanner.vue:28` | 读 `wsStore.lastError`，而 websocket store **无该字段** → 永远 falsy → 网络异常横幅**永不带警告图标** | 改用真实字段 `!wsStore.connected`；补 `NetworkBanner.spec.ts` 真 mount 回归测试 |
| ③ | `components/forms/PeripheralAssignForm.vue:187` | 调用不存在的 `nodeApi.assignPeripheral`，调用即 TypeError | 该组件零消费者（孤儿），随死代码清理整删 |

## 待清零错误分类（102 处）

| 错误码 | 处数 | 含义 | 处理建议 |
|---|---|---|---|
| `TS2322` | 51 | 类型不可赋值 | 多为 element-plus 组件 prop 的字面量联合收窄（如 ElTag type 不接受 ""）、自研组件 prop 类型与传入值不匹配、事件处理器签名不一致 |
| `TS2345` | 27 | 实参类型不匹配 | 函数/组件调用传参类型不符，部分涉及 union 收窄 |
| `TS2339` | 6 | 属性不存在 | ⚠️ 需逐条对照后端契约裁决：可能是真断头字段（后端不返回）或前端类型漏定义（后端确实返回） |
| `TS7006` | 5 | 参数隐式 any | 回调参数缺类型标注，补显式类型即可 |
| `TS18048` | 4 | 可能为 undefined | 需加空值判断或断言非空（真修，不要用 ! 掩盖） |
| `TS2724` | 3 | 模块无此导出 | ⚠️ echarts/core 无 EChartsOption，应改用 EChartsCoreOption（真实 API 变更，非类型噪音） |
| `TS7053` | 3 | 隐式 any 索引 | 对象索引签名缺失，补 Record<K,V> 或显式键联合 |
| `TS2559` | 2 | 无可兼容属性 | 对象字面量与目标类型结构不兼容，多因字段名/形状漂移 |
| `TS2352` | 1 | 类型转换可疑 | 断言过于激进，需换更精确的窄化方式 |

### 按文件分布（top 20）

| 文件 | 处数 |
|---|---|
| `src/components/node/ChannelPanel.vue` | 13 |
| `src/views/automation/AutomationRules.vue` | 8 |
| `src/views/edge-device/EdgeDeviceList.vue` | 7 |
| `src/views/node/NodeOverview.vue` | 7 |
| `src/views/edge-device/inverter/InverterDetailPage.vue` | 6 |
| `src/components/node/LogPanel.vue` | 5 |
| `src/components/channel/ChannelTerminal.vue` | 4 |
| `src/components/forms/DeviceConfigForm.vue` | 4 |
| `src/views/alert/AlertRules.vue` | 4 |
| `src/views/edge-device/bms/BmsDetailPage.vue` | 4 |
| `src/views/edge-device/shared/ActionForm.vue` | 4 |
| `src/views/firmware/FirmwareManage.vue` | 4 |
| `src/views/node/NodeDetail.vue` | 4 |
| `src/components/channel/ChannelManager.vue` | 3 |
| `src/views/channel/ChannelList.vue` | 3 |
| `src/views/edge-device/GenericDeviceDetail.vue` | 3 |
| `src/components/periph/PWMChannelRow.vue` | 2 |
| `src/components/periph/PWMResourceList.vue` | 2 |
| `src/views/data/DataPanel.vue` | 2 |
| `src/components/channel/__tests__/ChannelTerminalRoute.spec.ts` | 1 |

### 需优先裁决的高价值项（TS2339 / TS2724）

**TS2339（6 处）**

- `src/components/forms/DeviceConfigForm.vue:53` — manufacturer
- `src/views/data/DataPanel.vue:799` — unit
- `src/views/node/NodeOverview.vue:1029` — adc

**TS2724（3 处）**

- `src/components/charts/GaugeChart.vue:11` — EChartsOption
- `src/components/charts/LineChart.vue:16` — EChartsOption
- `src/dev/BmsDemoPage.vue:564` — EChartsOption

## 执行建议

1. 按文件分批清零（同文件错误往往同源），每批跑 `npx vue-tsc --noEmit` 复测计数。
2. **禁止**用 `@ts-ignore` / `@ts-nocheck` / `as any` 掩盖 —— 必须真修类型。
3. TS2339 逐条对照 `backend/internal/models/*.go` 的 json tag 与 handler 响应结构裁决：后端确实返回 → 补前端类型；后端不返回 → 是断头字段，按真 bug 处理。
4. 清零后再把 `package.json` 的 `typecheck` 切为 `vue-tsc --noEmit`（并确认 `typecheck:vue` 可删除或合并），随该次提交同步验证 `pnpm build` 与 `pnpm test:run`。