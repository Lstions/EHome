# 优化方案执行台账（2026-09-11 起）

> 方案：[架构与接口评估及优化方案-2026-09-11.md](架构与接口评估及优化方案-2026-09-11.md)
> 模式：主 Agent 统筹（核事实/写任务书/亲验/提交）+ 子代理执行
> 并发上限 2，文件集不重叠的批次可并行

## 进度总览

| 批次 | 内容 | 状态 | 证据 |
|---|---|---|---|
| Phase 1 后端 | 0.1 写路径吞错误×3 + 0.2 错误语义×4 + 测试 | ✅ **完成并提交** | `bb4bc491`（7 文件，135+/8-）＋ `cc98964f`（gofmt 预存问题） |
| Phase 1 前端 | 0.6 vue-tsc 门禁（部分）+ 0.8 三真 bug + 0.7 前端死代码 | ✅ **完成并提交（0.6 为第一批，余 102 错误）** | `ed2985fd`（死代码 −4276 行）＋ `761924b0`（类型门禁 + 3 真 bug） |
| Phase 2-A | 0.3 CI 固件 job + 0.4 覆盖率门禁对齐 | ✅ **完成并提交** | `e5a14705`（+38/−8） |
| Phase 2-B | 0.5 数据库备份/恢复脚本 + 运维文档 | ✅ **完成并提交** | `66d9f152`（5 文件，+354） |
| P1.1 批次1 | 响应方言收敛 · `handler_device.go` 63 处手搓 `gin.H{"code"}` | ✅ **完成并提交** | `afd3a160`（80+/94−，含 2 条契约测试；同批修正 `POST /channels` 裸结构体响应） |
| P1.1 批次2/3 | `handler_node/terminal/data/ota/logstream` 方言 + 8 处 `{"error"}` | 待排 | 批次2 需 `ErrorWithCode`（3 处字符串 code）；批次3 需前端同批发布 |
| P0-0.6 批次A | 前端类型错误 components/+dev/ 39 处 | **执行中** | deleg_a05b5fbb/task-0（前 900s 侦察瘫痪，已 steer 纠偏后开始落盘） |
| P0-0.6 批次B | 前端类型错误 views/ 59 处（15 文件） | 任务书就绪待派 | `/tmp/task-B-frontend-views.md`（逐模式规范修法 + 逐行处置表要求） |
| P0-0.6 收尾 | 余错清零 → 切换 `typecheck` | 待排 | 依赖已就位（devDeps 已钉 `vue-tsc@2.2.10`/`typescript@5.9.3`）；注意 `Dockerfile:35 pnpm build` 会连带门禁，须在清零后切换 |
| P0-0.7 余项 | 后端死代码（globalRegistry 门面 + getTemplateParamsFromDeviceConfig） | ✅ **完成并提交** | `048f2c4a`（7 文件，−136 行净删；含测试改写 1 + 删除 5 用例，注释残留 4 处一并清零） |
| ⚠️ 产品决策 | `dataSourceApi` + `/data-sources` 9 路由 + `failover-logs` 整片「有后端无 UI」 | **需拍板** | 见评估报告 §二 修正 |

## 本轮新发现与计划修正（2026-09-12）

### ⚠️ `failover-logs` 不可删（评估报告建议已更正）

初版报告 §P0-0.7 写「`failover-logs` 占位端点 → 改 501/410 或整删路由」，**核实后不成立**：
- 前端 `api/dataSource.ts:108` `getFailoverLogs` **确实在调用**该端点（`api-wrappers.spec.ts:310` 有用例）
- 追查发现整个 `dataSourceApi`（113 行 / 10 方法 / 4 interface）**除自身测试外零消费者**
- 后端 `handler_data_source.go` 的 9 条 `/api/v1/data-sources*` 路由同样无前端 UI 消费

→ 这是与 vendor 体系同类的**「后端已建成 + 前端只有 API 客户端 + 没有页面」半成品功能**（主备数据源/failover）。
**属产品决策，不单方面删代码**：要么补 UI 接入，要么整体下线（后端路由+模型+前端客户端+测试一次性清理）。决策前保留现状。

### 死代码删除的消费者核实结论（勿盲删）
| 项 | 生产消费者 | 测试消费者 | 结论 |
|---|---|---|---|
| `drivers/registry.go` globalRegistry 门面（GlobalRegistry/Register/Get/List） | 0 | `drivers_test.go:632` `RegisterBuiltInDrivers(GlobalRegistry())` | 可删，但**必须同步改该测试**为显式 registry 实例 |
| `getTemplateParamsFromDeviceConfig` | 0（仅注释引用） | `handler_edge_device_crud_test.go:931-985` 整段 5 处 | 可删，需**连同该测试段删除** |
| `failover-logs` | 0 视图级 | `api-wrappers.spec.ts:310` | **不可删**（见上） |

## Phase 2 交付与亲验记录（2026-09-12）

### 提交
| commit | 内容 | 规模 |
|---|---|---|
| `e5a14705` | ci: 补固件 host_tests job + 覆盖率门禁 25→35 / 15→25 | +38/−8 |
| `66d9f152` | feat(ops): PG 备份/恢复脚本 + Makefile 目标 + 运维文档 | 5 文件，+354 |

### 亲验结论（全部独立复跑，不采信子代理自报）
**CI（0.3/0.4）**
- YAML 合法，`jobs = [backend, backend-integration, frontend, firmware, summary]`
- `paths` 两处（push/PR）均含 `esp32-collector/**`（程序化断言 True）
- `firmware` job：`working-directory: esp32-collector`、构建目录 `${{ runner.temp }}/host-tests`、`ctest --output-on-failure`、纳入 `summary.needs`
- **本地完整复现 job 三步**：configure / build / ctest 退出码均 **0**，**33/33 passed**（0.03s）——与 job 内命令逐字一致
- 门禁值：后端 `COV < 35`、前端 `COV < 25`（`test.yml:64/155`）；实测 62.6% / 52.13% → 不会红；未改 `vitest.config.ts`

**备份/恢复（0.5）**
- **三道护栏实跑**均按预期 exit 1：① 恢复进 `ehome` 无 `--force` → 中文报错拒绝；② 缺参数 → 打印用法；③ dump 不存在 → 报错
- `sha256sum -c` 校验备份产物 **OK**；`.meta` 含时间/大小/耗时/pg 版本/TOC 445
- **主 Agent 独立恢复演练**（临时库 `verify_by_main`，非采信自报）：pg_restore 退出码 0，
  **表 46 / 分区片 30 / 索引 188** 与生产基准完全一致；演练库已 DROP 清理
- 生产库复核 `pg_tables = 46`，全程未受影响；`backups/` 未进版本库（.gitignore 生效）
- 子代理**遵守了禁 git 纪律**：提交前 `git diff --cached` 为 0 文件

## Phase 1 交付与主 Agent 亲验记录（2026-09-12）

### 提交
| commit | 内容 | 文件数 |
|---|---|---|
| `bb4bc491` | fix(api): 写路径吞错误与错误语义缺失（P0） | 7 文件，+135/−8 |
| `cc98964f` | style(backend): gofmt 修正 alert 域预存未格式化文件 | 5 文件（纯格式，`git diff --ignore-all-space` 实质差异 0） |
| `ed2985fd` | chore(frontend): 清理零消费者死代码 | 14 文件，−4276 行 |
| `761924b0` | build(frontend): 接入 vue-tsc 真类型门禁 + 修 3 真 bug（165→102） | 30 文件，+309/−432 |

### 亲验结论（不采信子代理自报，全部独立复跑）
- 后端（已提交态）：`go build` 0 ｜ `go vet` 0 ｜ `gofmt -l` **0 文件** ｜ `go test ./...` **0 FAIL**
- 前端（已提交态）：`pnpm typecheck` 0 ｜ `pnpm test:run` **92 文件 1008 passed / 1 skipped / 0 failed** ｜ `pnpm build` exit 0
- **变异验证（证伪测试非空心）**：逐条回退修复后确实 FAIL ——
  - 回退 409 → `duplicate node_id expected 409, got 500: {"error":"UNIQUE constraint failed: nodes.node_id"}`（同时暴露原文泄露）
  - 回退 404 → `delete nonexistent expected 404, got 500: record not found`
  - 回退 `ErrInvalidRequest` case → `missing Idempotency-Key expected 400, got 500: create operation failed`
- 7 处代码修改与任务书给定的 old/new **逐字一致**；未超出白名单范围

### 子代理越界的处置
- 后端子代理为让 `gofmt -l` 干净，对 alert 域**预存未格式化文件**执行了 `gofmt -w`（HEAD 版本本就未格式化，已核验）。处置：`git diff --ignore-all-space` 确认纯格式后，**单独提交**为 `cc98964f`，不污染 P0 提交。
- 前端子代理删了 `NodeDetail.vue` 的死函数 `formatOnlineDuration`，触发既有 `?raw` 断言失败（`NodeDetail.spec.ts` 硬要求该字符串存在）。处置：独立 grep 确认该函数组件内与全仓零引用后，**删除该行断言**并加注释说明（这正是用户纪律中「源码断言锁死实现细节」的实例）；同用例其余 6 条断言保留。
- 前端子代理**未完成** 0.6（165→102，余 102）。已生成 `docs/分析/类型门禁落地-遗留项.md` 分类建档，并**保持 `typecheck` 未切换**（避免打断 `pnpm build` → Dockerfile → docker-publish 流水线）。

### ⚠️ 本轮主 Agent 自身失误（已修正，已加固 skill）
按精确路径 `git add <后端文件> && git commit` 时，**前端子代理预先 `git add` 过的删除操作仍在暂存区**，导致 `git commit` 一次提交了 14 个文件（后端 P0 + 未完成的前端改动混在一起）。
处置：`git reset --soft HEAD~1` → `git restore --staged frontend-shared/` → 重新提交，得到干净的 `bb4bc491`。
**加固**：① 派发任务书一律加「**禁止任何 git 暂存操作**」条款；② 提交前先 `git restore --staged .` 清空暂存区；③ 提交前用 `git diff --cached --name-status` 复核（而非只看 `git status` 第二列）。

### P1.1 批次1 预核实结论（主 Agent 已完成，无需子代理探索）

`handler_device.go` 生产代码 71 处 `c.JSON`，**全部可机械替换，0 处需人工裁决**：

| 原形态 | 处数 | 替换为 |
|---|---|---|
| `{code:400,"message":M}` | 21 | `Error(c, http.StatusBadRequest, M)` |
| `{code:200,"message":"ok","data":D}` | 20 | `Success(c, D)` |
| `{code:404,"message":M}` | 12 | `Error(c, http.StatusNotFound, M)` |
| `{code:500,"message":M}` | 9 | `Error(c, http.StatusInternalServerError, M)` |
| `{code:409,"message":M}` | 4 | `Error(c, http.StatusConflict, M)` |
| `{code:200,"message":M≠ok,"data":D}` | 2 | `SuccessMsg(c, D, M)` |
| `{code:201,"message":"created","data":D}` | 1 | `SuccessWithCodeMsg(c, 201, D, "created")` |
| `{code:410,"message":M}` | 1 | `Error(c, http.StatusGone, M)` |
| `{code:504,"message":M}` | 1 | `Error(c, http.StatusGatewayTimeout, M)` |

**关键不变量（程序化核查 ✅）**：这 71 处的 `"code"` 数值**恒等于** HTTP 状态码 —— 因此机械替换语义安全（`envelope` 正是 `Code: statusCode`）。`data: nil` 视为无 data，正是 `Error()` 的产物（故 :447 无需特殊处理）。

**行为差异（必写进 commit message）**：envelope 对成功响应恒定输出 `"message":"ok"`（手搓常省略）；且错误响应恒定输出 `"data":null`。二者均为 **additive**，JSON 消费者安全。

## 失败记录（供复用）

**2026-09-11 后端任务超时（1800s / 39 次调用）**
- 症状：35×terminal + 17×read_file，**零编辑**；`git status` 中 backend 几乎无变化
- 根因：任务书范围过宽（3 工作流 / 10 项 / 3 个文件域），弱模型（qwen3.8-flash）陷入侦察瘫痪
- 纠正：拆为单工作流 + 逐处给出精确 old/new 代码 + 白名单限读 + 直接给测试辅助函数名与行号
- 已固化进 skill：`ehome-system` → 「流水线派发纪律：防侦察瘫痪」

## Phase 1 前端已落盘的改动（待其完成 + 主 Agent 亲验）

```
 D  frontend-shared/src/components/forms/PeripheralAssignForm.vue   (死代码，零消费者)
 D  frontend-shared/src/dev/{NewNodeDemoPage,NodeBusConfigDemoPage,NodeDemoPage}.vue
 D  frontend-shared/src/locales/{en-US,index,zh-CN}.ts              (vue-i18n 全链)
 M  frontend-shared/src/main.ts / package.json / pnpm-lock.yaml     (移除 vue-i18n/sass)
 M  frontend-shared/src/components/common/NetworkBanner.vue          (真 bug ②)
 M  frontend-shared/src/views/dashboard/Dashboard.vue                (真 bug ①)
 M  frontend-shared/tsconfig.json                                    (include .vue)
 M  frontend-shared/src/api/node.ts / monitor.ts / data.ts           (类型补全)
 M  backend/internal/api/handler_overview.go                         (真 bug ① 后端侧：latestEntry 补 ErrorCode)
 ?? frontend-shared/src/components/common/__tests__/NetworkBanner.spec.ts  (新测试)
 ?? frontend-shared/src/views/dashboard/__tests__/Dashboard.spec.ts  (新增用例)
```

### 主 Agent 已验证的前端改动片段（真 bug ① 的后端侧）
```go
// handler_overview.go latestEntry 结构体
ErrorCode     int                `json:"error_code"`
// 填充
ErrorCode:     dev.ErrorCode,
```
后端 `go build ./...` EXIT=0、`go test ./internal/api/` ok —— 该改动连贯且未破坏后端。

## 派发前已核实的关键接口事实（避免子代理重走弯路）

- 类型门禁：`typescript@5.9.3` + `vue-tsc@2.2.10` 可行；`vue-tsc@3.x` + TS 7.0.2 必失败（`ERR_PACKAGE_PATH_NOT_EXPORTED './lib/tsc'`）
- 顺序硬约束：`Dockerfile:35` 的 `pnpm build` = `pnpm typecheck && vite build`，且 `docker-publish.yml` 在 push main 时按 `frontend-shared/**` 触发镜像构建 → 错误清零前**不得**切换 `typecheck`
- CI 固件 job：host_tests 是纯 host 构建（实测 33/33，0.04s），不需 ESP-IDF，仅需 gcc + cmake
- CI 覆盖率门禁：`test.yml:58` 后端 25%→35%、`:144` 前端 15%→25%；**不要动** `vitest.config.ts`（已是 25）
- 备份机制：`pg_dump --format=custom` + `pg_restore` 实测闭环通过（46 表 / 30 分区片 / 188 索引与生产一致）；容器内 pg_dump 18.4；库 904 MB
- 响应方言实测：数字 code 230 处（可机械替换）/ 字符串 code 3 处（须改 ErrorWithCode）/ `{"error"}` 81 + 裸对象 35（需前端同批）
- 测试辅助函数：`setupEdgeDeviceTest`(handler_edge_device_crud_test.go:26)、`setupVendorTest`(handler_ota_vendor_user_test.go:238)、`setupTestRouter`(需 grep 定位)
