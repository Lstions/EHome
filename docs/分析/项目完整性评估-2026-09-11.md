# EHomeSystem 项目完整性评估（2026-09-11）

> 评估人：Hermes 主 Agent ｜ 基线：main @ e8459ae8（2026-09-06 16:47）
> 方法：git 活动定位 → 逐项源码/命令核实 → 门禁实测（不引用任何声明性结论）

## 0. 结论摘要

- 代码/测试/文档三维完整度高，**无孤儿包、无 404 风险端点、门禁全绿**。
- 主要缺口集中在**部署运行态**（当前零服务在跑）与**少量未接线/死代码**（vendor API 体系、PeripheralAssignForm、3 个 dev demo 页）。
- 综合完整性评分：**88%**；生产就绪度：**60%**（未部署 + 高风险动作实机门槛未过）。

## 1. 当前状态快照

| 项 | 实测值 | 命令/证据 |
|---|---|---|
| 分支 / HEAD | main @ e8459ae8 | `git log -1` |
| 最后提交 | 2026-09-06 16:47（距评估日 5 天，09-07 后 0 提交） | `git log --since=2026-09-07` = 0 |
| 工作树 | 干净（0 modified/untracked） | `git status --porcelain` = 0 行 |
| 历史分支 | feat/automation-engine、feat/command-action-evolution、feat/rebase-lost-increment-restore、fix/bms-mobile-ui-fixes 均已合入 main（ahead=0） | `git rev-list --count main..<b>` |
| worktree | 2 个：主树 + `EHomeSystem-bms-mobile-fix`（见 §5.2） | `git worktree list` |
| 运行态 | **ehome-web 容器不存在**，8080/5174 无监听；仅 postgres/redis/emqx 基础设施 Up 5 天 | `docker ps -a`、`ss -ltnp` |

## 2. 模块规模与完整性

| 模块 | 规模 | 测试 | 覆盖率 | 完成度判断 |
|---|---|---|---|---|
| backend（Go） | 159 生产文件 / 39,033 行；136 测试文件 / 40,539 行 | `go test ./...` 全绿（exit 0） | **62.6%**（阈值 35%） | 完整，29 个包无孤儿 |
| frontend-shared（Vue3） | 84 个 .vue / 41,875 行；91 spec | `vitest run` 91 文件 1005 passed / 1 skipped / 0 failed | **49.52%** statements（阈值 25%） | 完整，13 条路由全部有页面 |
| esp32-collector（C） | 195 个 .c/.h / 43,459 行 | host_tests **33/33 passed**（fresh cmake 构建） | — | 完整；实机未验（ttyACM0/ttyUSB0 在位） |
| deploy | monitoring（prometheus/alertmanager） | — | — | 有配置，未在本机运行 |

## 3. 门禁实测（本轮全部亲自执行）

```
后端   go build ./...            exit 0
       go test ./...             exit 0（全包 ok，无 FAIL）
       go vet ./...              exit 0
前端   pnpm test:run             91 files / 1005 passed / 1 skipped / 0 failed，exit 0
       pnpm typecheck (tsc)      exit 0，0 错误
       pnpm test:coverage        49.52% statements / 41.54% branches
固件   cmake + ctest (fresh)     33/33 passed
文档   check_doc_links.py docs   465 个链接，0 断裂
```

后端包覆盖率（降序，前 10）：audit 90.9 / config 89.7 / logger 83.3 / mqtt 82.5 / offlinedetector 81.3 / commandexec 80.2 / terminal 79.3 / websocket 79.2 / alert 76.8 / drivers 76.4。
偏低：ota 37.0 / models 20.9 / cmd/server 4.9（main 装配逻辑，属常态）。

## 4. 接线核查（本审计核心价值项）

### 4.1 通过项

- **后端零孤儿包**：29 个 internal/pkg 包全部被生产代码引用（仅 cmd/、testutil、迁移脚本无引用方属正常）。
- **协议对称性**：前端 138 条请求路径，逐条在后端源码检索静态资源段，**0 条无对应后端注册**（无 404 风险）。
- **前端 API wrapper**：20 个 api 对象调用点 ↔ 定义点双向比对，仅 §5.1 一处真实缺口。
- **关键修复已落地复核**：
  - setter 二阶段注入：`nodemgr/manager.go:106-108 / 116-118 / 127-129` 推送 sink 到已构建 parserConsumers；`cmd/server/main.go:212/215/238` 在 NewManager 之后调用 → 时序正确（不再是"引擎从未触发"的旧缺陷）。
  - latest_value_cache：`api/latest_value_cache.go:31/38/55` 已用 `rec.DeviceID`（uint）为键，与 unified_data 写入一致 → 缓存命中问题已修。
- **Phase 0+1（设备指令与操作体系演进）落地复核**：`api/command_intervals.go` 存在；`ehomectl command-intervals cleanup` 已注册（`cmd/ehomectl/main.go:34`、`command_intervals.go`）；`nodemgr.CommandIsManifestCandidate` 被生产路径 `sender_snapshot.go:174/197` 消费；`legacyUnsafeCommandTemplates` 全仓 0 命中（228 行死代码已删）；TechfineInverterDriver 已无 CommandTemplates（11 条 query_* 已删，驱动本体保留）。

### 4.2 缺口项

| # | 级别 | 问题 | 证据 |
|---|---|---|---|
| G1 | **高** | `PeripheralAssignForm.vue:187` 调用 `nodeApi.assignPeripheral(...)`，但 `api/node.ts` 无该方法（只有 `PeripheralAssignment` 接口）→ 一旦挂载必然 TypeError | grep `assignPeripheral` → 仅该组件命中 |
| G2 | **高** | 同一组件**零消费者**（无任何 .vue 引用，仅 components.d.ts 自动声明）→ 孤儿组件 + 坏调用叠加 | `grep -rn PeripheralAssignForm --include=*.vue` = 空 |
| G3 | 中 | 前端 vendor 体系整体未接线：`vendorApi`(6 方法) / `deviceModelApi`(7) / `deviceCategoryApi`(1) **全部零调用**，而后端 `/vendors`、`/device-models` 端点+测试存在 → 有后端无 UI | grep 三个对象调用点 = 0；`handler_ota_vendor_user_test.go` 有路由测试 |
| G4 | 中 | 3 个 dev demo 页**零引用且不在路由**：`src/dev/NewNodeDemoPage.vue`、`NodeDemoPage.vue`、`NodeBusConfigDemoPage.vue`（对比 BmsDemoPage/MockBmsPanel 有 `/dev/*` DEV 路由）→ 死代码或路由遗漏 | 全 src grep 无命中；`router/index.ts:137-148` 仅 2 条 dev 路由 |
| G5 | 中 | 根目录 `文档/` 目录仅含 1 个文件（自动化引擎S1S2验收报告-2026-08-26.md），与 `docs/规范/文档规范.md` 约定的单一 `docs/` 体系冲突 | `ls 文档/` |
| G6 | 中 | 前端执行覆盖率呈"整层为零"分布：154 个受测文件中 **33 个 0.00% 执行覆盖**，集中在 edge-device 详情子层（inverter/ 全部 6 个文件、bms/BmsMosStatus + BmsCellVoltageHistoryChart、shared/CommandFrequencySection）与 forms 层 | `coverage/coverage-summary.json`（权威源，log 表内短名会误导） |
| G6-a | 中 | **覆盖率指标口径陷阱**：7 个文件 `statements.total == 0`，json-summary 按约定输出 `pct: 100`——包括 `BmsDetailPage.vue`、`BmsCellVoltageChart.vue`。这 7 个"100%"不可解读为已覆盖，报告与门禁判断时须排除 | `coverage-summary.json` 原始字段 |
| G6-b | 中 | 91 个 spec 中 **31 个含 `?raw` 源码字符串断言**（如 BmsDetailLayout / BmsMobileResponsive / DeviceControlPanel / EdgeDeviceList 等，`mount(` = 0）。按项目测试纪律"源码断言 ≠ 行为覆盖"，这些 spec 对目标组件的模板逻辑、表单校验、交互路径零行为覆盖 | `grep -c 'mount(' vs '?raw'` 逐文件 |

低覆盖明细（lines，权威取 `coverage-summary.json`）：
- 0.00%：inverter/{InverterDetailPage,InverterEnergyCard,InverterMpptCard,InverterPowerFlow,InverterStatusGrid,InverterTempCard}.vue、bms/{BmsMosStatus,BmsCellVoltageHistoryChart}.vue、shared/CommandFrequencySection.vue、forms/{InitializeAdminForm,PeripheralAssignForm}.vue、charts/{GaugeChart,LineChart}.vue、common/{CountUp,ErrorBoundary,NetworkBanner,SkeletonCard}.vue、composables/{useHistoryData,useRealtimeData,useTimeRange}.ts、api/{alert,automation,vendor}.ts、stores/ui.ts、utils/{chartRange,constants}.ts、parser/ParserBrowser.vue、dev/* 3 页
- 0<x<15%：DeviceConfigForm 2.02 / HistoryChartSection 2.52 / OTAForm 2.64 / LoginForm 3.33 / DeviceHeader 3.65 / TimeRangeSelector 5.55 / DeviceControlPanel 9.80
- 页面级最低：FirmwareManage.vue 29.94% lines、NodeDetail.vue 48.14%、NodeList.vue 52.70%
- 阈值口径：lines/functions/statements 25、branches 20（`vitest.config.ts`）→ 总 49.52% 通过，但上述整层缺口不影响门禁判定

## 5. 工作树与可交付性

### 5.1 主工作树

干净，无未提交改动；无未跟踪产物；HEAD 即 main。

### 5.2 `EHomeSystem-bms-mobile-fix` worktree（08-09 遗留，需处置）

- 分支 `fix/bms-mobile-ui-fixes` 已全部合入 main（ahead=0, behind=87），但工作树残留：
  - M `bms/BmsCellVoltageChart.vue`（新增 `showBarLabels`：≤768px 关闭柱顶常驻数值，避免 16 节标签重叠）
  - M `bms/BmsDetailPage.vue`（KPI 卡等高 + 电芯电压卡头响应式 + 放电语义色 danger→primary）
  - M `edge-device/shared/DeviceInfoCard.vue`
  - ?? `bms/__tests__/BmsMobileResponsive.spec.ts`（149 行源码契约测试）
- 时间戳 2026-08-09 18:54–19:28，**未进入 main**。
- 复核 main 现状：`showBarLabels` 在 main 命中 0（修复未合入）；`cell-voltage-header` 已在 main（部分工作经其它路径合入）；`metric-value.negative` 在 main 已无（该页已迁 MetricStatCard）。
- 处置建议：先判定「移动端柱顶数值重叠是否仍是 main 现存缺陷」，是则把该修复 + spec 重做到当前 main 结构上；否则整树清理（`git worktree remove`），避免长期悬挂的 87-commit-old 残留。

## 6. 文档对账

- `check_doc_links.py docs`：465 链接 0 断裂。
- 文档诚实度良好：已如实标注未实现项（`docs/设计/GPIO与PWM外设控制.md:100-101` PWM fade / GPIO 中断；`docs/设计/自动化策略引擎接口设计.md:248,310,384` 接口面 §5.1-5.3 gap 与"有意不做"记录；`docs/设计/通道.md:232` SPI/GPIO 扫描 P3 未做）。
- 文档索引最新状态停在 2026-08-22 的 v3.4 表述（`docs/设计/README.md` 头部），未纳入 08-25~09-06 的自动化引擎与指令体系演进两批内容 → 建议补索引条目。

## 7. 待排期（方案已定，未实现）

设备指令与操作体系演进方案 v1.0（`docs/设计/设备指令与操作体系演进方案.md:287-290`）：

- Phase 2：C8 动作门禁台账 v1 + 证据命名规范 + 解禁 PR 模板；C9 补登记 SN-3001/BMS 已闭环动作证据
- Phase 3：C10 规则编辑器展示动作 Risk + medium 自动执行提示；C11 PeriphCmd 豁免理由 + Risk×ActorKind 矩阵文档化

## 8. 建议优先级

- **P0**：确认 ehome-web 的部署意图与目标版本；如需上线，先 `ehomectl command-intervals cleanup`（停服窗口）再部署含 PUT 校验的新二进制（见项目记忆运维注意）。
- **P0**：处置 G2/G1（PeripheralAssignForm）——要么补 `nodeApi.assignPeripheral` + 接线，要么删除组件，不能保留"坏调用 + 无消费者"状态。
- **P1**：BMS worktree 残留（§5.2）判定与收敛；G3 vendor 体系明确"做/不做"并把未接线从 api 客户端移除或标注。
- **P2**：G4 dev demo 死页清理、G5 `文档/` 归位到 `docs/`、docs/设计/README 索引补 08-25 后两批。
- **P3**：G6 覆盖缺口补测——优先 **inverter/ 6 文件整层（0% 且无 `__tests__` 目录）**，其次 forms/{DeviceConfigForm,OTAForm,LoginForm}（2-3%）与 FirmwareManage.vue（29.94%）；同时给 31 个 `?raw` spec 逐个体检：至少保留一条真实 mount/DOM/API 断言（G6-b）；`esp32-collector/build-host-tests/` 为 2026-07-19 旧配置（仅 16 项测试），建议删除或重配，避免"测试通过数"被旧构建误导。
