# 创建边缘设备向导问题台账（2026-08-08）

> 触发来源：少爷在真实浏览器操作新部署环境时发现「创建边缘设备」向导不可用/体验异常。  
> 处理原则：逐项定位根因、逐项修复、逐项补回归测试；子代理执行，小夏验收后统一提交。

## 状态说明

- `已定位`：根因已确认，等待或正在修复
- `修复中`：子代理已派发，等待测试报告
- `待验收`：代码已改，等待小夏抽验与真实浏览器复核
- `已关闭`：测试与验收均通过

## 问题清单

| 编号 | 优先级 | 问题 | 根因分析 | 当前处理 | 验收标准 | 状态 |
|---|---:|---|---|---|---|---|
| EDGE-WIZ-001 | P0 | 「选择设备型号」列表为空，无法创建边缘设备 | 创建向导通过 `parserApi.getList()` 只请求 `GET /api/v1/device-configs?status=active`，该接口只查数据库 `device_configs`。空库/新部署时没有 DB 模板，即使后端已有内置驱动也不会展示。后端 `/api/v1/device-configs/tree` 实际已能提供内置驱动树，但前端未消费。 | 子代理 `fix_empty_device_models` 已完成：合并 DB 配置与内置驱动树、去重、DB 优先、tree 失败保留 DB 兜底，并补 8 个回归测试。小夏已抽验 diff，并复跑 `api-wrappers.spec.ts` + `EdgeDeviceList.spec.ts`（182 passed）与 `pnpm typecheck`（通过）。 | 空 `device_configs` 时型号列表仍显示内置驱动；DB 配置与内置驱动同类型时保留 DB 配置及 `device_config_id`；tree 失败不影响 DB 列表；前端测试通过；真实浏览器可进入下一步。 | 待验收 |
| EDGE-WIZ-002 | P2 | 通道卡片字体样式异常 | 通道卡片使用原生 `<code>` 展示通道名，浏览器默认等宽字体与整体 UI 不协调；右侧又重复展示硬件 ID，观感混乱。代码位于 `EdgeDeviceList.vue` 通道卡片与 `.channel-select-card code`。 | UI 子代理已修复：`<code>` → `span.channel-name`（Element Plus 正文字体），保留硬件类型 tag 与总线 ID，选中态变绿。小夏已抽验 diff 与测试。 | 通道卡片字体与页面其他表单文本一致；不再出现突兀等宽字体；选中态仍清晰；相关快照/组件测试通过。 | 已修复待验收 |
| EDGE-WIZ-003 | P1 | 节点刚创建完通道后，不刷新页面，创建边缘设备选择节点看不到新通道 | `loadCreateWizardData()` 只有在 `channelStore.channels.length === 0` 时才调用 `fetchChannels()`。若此前已加载过旧缓存，向导会直接复用缓存，刚创建的通道不会进入列表。代码位于 `EdgeDeviceList.vue:844` 附近。 | UI 子代理已修复：每次打开向导强制 `fetchChannels(undefined, true)`（refreshWizardChannels + channelsRefreshing 防重复压栈）；切换节点 handleNodeChange 同样强制刷新；store 内 requestSequence/cacheEpoch 防竞态未破坏。小夏已抽验 diff 与测试。 | 在节点页创建通道后不刷新浏览器，立即打开创建边缘设备向导，选择该节点能看到新通道；不会因强制刷新引入重复请求/竞态；前端测试覆盖。 | 已修复待验收 |
| EDGE-WIZ-004 | P1 | 选择嘉佰达 BMS 后，创建向导只显示一个全局采集间隔 | 数据模型和详情页已支持每条轮询指令独立间隔：`edge_devices.command_intervals`、`GET/PUT /edge-devices/:id/commands`、详情页 `CommandList.vue`。但创建向导仍只提交单个 `interval_ms`，创建接口 `POST /edge-devices` 此前也不接收 `command_intervals`。 | 后端子代理 `backend_create_command_intervals` 已完成：创建接口支持可选 `command_intervals`，按最终设备类型校验 schedulable command id，负数归一为 0，未知/非轮询 id 原子拒绝；小夏已抽验 diff 与 8 个新测试，并复跑 `go test ./internal/api ./internal/drivers`、`go vet ./internal/api`（通过）。前端向导已由 UI 子代理接入：新组件 `CreateWizardCommandIntervals.vue`（逐指令间隔、0=禁用、切换型号清快照、加载失败拦截提交），创建三分支均携带 `command_intervals`。小夏已抽验全部 diff 并复跑前后端测试。 | 创建 BMS 时列出全部 schedulable 轮询指令；每条可独立设置毫秒值；`0 = 禁用`；创建后 `edge_devices.command_intervals` 持久化正确并随配置同步；详情页显示与创建时一致；后端/前端测试通过。 | 已修复待验收 |
| EDGE-WIZ-005 | P1 | 嘉佰达 BMS 轮询指令清单需在创建阶段可见 | 后端 `JiabaidaBMSDriver.GetCommandTemplates()` 已定义 5 条 schedulable 指令：`read_basic_info` 0x03、`read_cell_voltage` 0x04、`read_hardware_version` 0x05、`read_comprehensive` 0x0F、`read_protection_count` 0xAA。问题不在驱动缺失，而在创建向导未加载 `GET /api/v1/drivers/:type/commands`。 | 与 EDGE-WIZ-004 同批修复，但单独验收指令清单完整性。UI 子代理已实现：选型号后拉取 `GET /api/v1/drivers/:type/commands`，展示 schedulable 指令的名称/读写/命令字/说明/默认间隔。小夏已核对驱动源码 5 条指令（0x03/0x04/0x05/0x0F/0xAA）与前端展示一致。 | 创建向导至少展示上述 5 条指令的名称、读/写类型、命令字、说明和默认间隔；只允许 schedulable 指令配置；非轮询/受控写操作不出现在轮询配置中。 | 已修复待验收 |

## 当前执行队列

全部修复已完成，等待真实浏览器验收 + 少爷确认后统一提交：

1. ✅ `fix_empty_device_models`：EDGE-WIZ-001 前端合并 DB+内置驱动，已抽验。
2. ✅ `backend_create_command_intervals`：EDGE-WIZ-004 后端创建接口，已抽验。
3. ✅ UI 子代理：EDGE-WIZ-002/003/004/005 前端向导，已抽验（前端专项 219 项 + go test ./internal/api 全绿）。
4. ✅ Review 追加修复（2026-08-08 22:20，小夏直接修）：`/device-configs/tree` DB 补充查询补 `status='active'` 过滤，防止被禁用模板泄漏进型号列表；新增 `TestDeviceConfig_Tree_ExcludesInactiveConfigs` 回归测试。

## 已知遗留（另开任务）

- 节点页 QuickCreateDeviceDialog 仍为单一采集间隔，未展示逐指令配置（本轮范围外）。
- `LogicalDeviceList.spec.ts > filters by search keyword` 为 HEAD 既有失败，与本轮无关。

## 验收流程

1. 子代理提交修改说明与测试结果。
2. 小夏亲自抽验关键文件和测试，不只采信报告。
3. 运行必要前端/后端测试。
4. 在 dev 环境真实浏览器复核：
   - 空库仍显示设备型号；
   - 通道字体正常；
   - 新建通道无需刷新即可被创建向导看到；
   - 创建嘉佰达 BMS 时可逐指令设置轮询间隔；
   - 创建后详情页间隔保持一致。
5. 少爷确认后再统一 `git commit` / 推送。

## 验收结果（2026-08-08 23:15，小夏真实浏览器执行）

- ✅ 第 4 步全部完成：空库型号列表 / 通道字体 / 新建通道免刷新 / 嘉佰达逐指令轮询创建+落库+详情页一致，截图与 XHR 拦截证据齐全。
- EDGE-WIZ-001~005 全部「已验证」，等待少爷确认后提交。
