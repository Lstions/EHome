# 节点 (Node) — 验收标准 (v2.2)

> **模块**: 节点 (Node)
> **版本**: v2.2
> **设计**: [详细设计.md](详细设计.md) | [同步机制/详细设计.md](../同步机制/详细设计.md)

## 一、前端验收

### 1.1 节点列表 (v2.2)

- [ ] 列表显示: 名称 / node_id / 型号 / 固件版本 / 协议版本 / 状态 / 最后心跳 / RTT
- [ ] 4 个统计卡: 总数/在线/离线/今日新增
- [ ] 搜索: node_id / 名称
- [ ] 状态/平台/型号 筛选
- [ ] 表格视图/卡片视图切换
- [ ] 实时 RTT 显示 (60s 刷新)
- [ ] **v2.2 新增**: 显示该节点的边缘设备数量
- [ ] **v2.2 新增**: 点击进入"该节点的边缘设备列表"

### 1.2 节点详情 (v2.2)

- [ ] 基本信息卡 (含 `protocol_version` v2.2 字段)
- [ ] **v2.2 新增**: "配置同步状态" 卡
  - `config_sync_state` (in_sync/syncing/lag/error)
  - `config_epoch` (服务端当前 vs 节点当前, 落后时高亮)
  - `last_sync_at` / `last_manifest_id` / `last_sync_id`
- [ ] 实时数据卡 (wifi_rssi/free_heap/uptime)
- [ ] 通道列表 (按硬件类型分组, 5 总线)
- [ ] **v2.2 新增**: "该节点的边缘设备"列表
- [ ] 总线配置 (BusConfigPanel 嵌入)
- [ ] 触发 OTA 按钮
- [ ] 主动 Ping 按钮 → 立即显示 RTT

### 1.3 通道管理 (v2.2 强化)

- [ ] 5 种硬件类型自适应表单 (UART/I2C/SPI/GPIO/ADC)
- [ ] **v2.2 新增**: 通道详情页, 列出该通道挂的边缘设备
- [ ] **v2.2 新增**: 改 channel.bus_config → EmitConfigChange (v2.1 G1)

### 1.4 WS 事件订阅 (v2.2 修复 G4)

- [ ] 状态变化**实时**推到前端 (无刷新)
- [ ] 节点列表订阅 `node_status` (v2.2 替代 `node_status`)
- [ ] 节点详情订阅 `node_status`
- [ ] 订阅 `node_config_synced` 事件 (v2.1)
- [ ] **v2.1 兼容**: 同时订阅 `node_status` (灰度期, 6 个月后删除)

## 二、后端验收

### 2.1 端点 (v2.2 + v2.1 双路径)

- [ ] `GET /api/v1/nodes` 列表 (v2.2)
- [ ] `GET /api/v1/nodes` 列表 (v2.1 兼容, 6 个月)
- [ ] 两个端点返回相同数据
- [ ] `GET /api/v1/nodes/:id` 详情
- [ ] `POST /api/v1/nodes` 创建 (admin)
- [ ] `PUT /api/v1/nodes/:id` 编辑 (admin) → EmitConfigChange (v2.1)
- [ ] `DELETE /api/v1/nodes/:id` 注销 (admin)
- [ ] `GET /api/v1/nodes/:node_id/channels`
- [ ] `GET /api/v1/nodes/:node_id/edge-devices` (v2.2 替代 /devices)
- [ ] `GET /api/v1/nodes/:node_id/data` 历史数据
- [ ] `POST /api/v1/nodes/:node_id/ping`

### 2.2 MQTT 处理 (v2.1 + v2.2)

- [ ] `handler_hello.go` 解析 v2.2/v2.1 字段
- [ ] 调 `SyncGate.OnHello()` 决策
- [ ] `handler_status.go` 调 `SyncGate.OnStatusReport()`
- [ ] **v2.2 新增**: 解析 `node_id` (兼容旧 `device_id` 字段值, ID 数值不变)
- [ ] MQTT 消息解析失败 → WARN + 跳过

### 2.3 离线检测 (不变)

- [ ] Redis TTL 15s + DB last_seen 90s
- [ ] offline 时推 frontend 事件 (`node_status`)
- [ ] last_seen 自动更新
- [ ] 后端启动时检测 + push

### 2.4 配置下发 (v2.1)

- [ ] SyncGate 统一决策入口
- [ ] handler_hello.go 末尾 EmitConfigChange
- [ ] handler_status.go 末尾 EmitConfigChange (G5 修复)
- [ ] main.go 启动 push (G2 修复)
- [ ] epoch 存储在 config_meta 表

### 2.5 模型验证

- [ ] GORM struct 改名 `Node` (原 `Collector`)
- [ ] gorm tableName: `nodes` (v2.2) + `collectors` (v2.1 兼容视图)
- [ ] 单元测试: 旧 struct 名 `Collector` 仍可用 (alias)

## 三、节点端验收

### 3.1 上线握手 (v2.2)

- [ ] WiFi 连接成功 → 立即发 Hello (type=0x01)
- [ ] **v2.2**: Hello field 1 字符串 = `node_id` (实际值与 v2.1 `device_id` 相同, 字段名变了)
- [ ] **v2.1**: Hello 包含 8 字段 (含 config_epoch/nvs_has_config/last_manifest/protocol_version)
- [ ] **v2.2**: protocol_version = "2.2" (新固件)
- [ ] **v2.1 兼容**: protocol_version = "2.0" / "2.1" (老固件)
- [ ] 收到 HelloAck → 解析 ConfigManifest → 写 NVS → 发 ConfigResult

### 3.2 心跳 (v2.1 + v2.2)

- [ ] 60s/次发 StatusReport
- [ ] 包含 5 字段 (含 config_epoch/sync_state)
- [ ] status_flags 包含 wifi_connected/mqtt_connected/config_synced

### 3.3 数据采集 (v2.2 通道 + 边缘设备)

- [ ] 收到 ConfigManifest 含 channel.edge_devices[]
- [ ] 节点 NVS 存 (channel_id, hardware_id, parser_id, interval_ms) 列表
- [ ] 不存 edge_device_id (节点不感知)
- [ ] Scheduler 按 edge.interval_ms 周期采集
- [ ] DataReport 关联到 channel_id (中心端按 channel_id 找 edge)

### 3.4 SyncManager (v2.1)

- [ ] sync_manager_init 启动
- [ ] 7 种 sync_reason 决策
- [ ] 60s 周期 task
- [ ] NVS epoch/manifest_id 读写

## 四、集成验收 (E2E, v2.2 节点 + v2.2 中心端)

### 4.1 端到端

- [ ] 节点上电 → 几秒内中心端看到 online (`node_status` 事件)
- [ ] 前端列表 → 该节点状态变绿
- [ ] 关闭节点 → 90s 后看到 offline
- [ ] 主动 Ping → RTT < 100ms
- [ ] 改 channel.bus_config → 5s 内节点收到 ConfigManifest (G1 修复)
- [ ] 创建设备配置 → 引用它的所有边缘设备重新 init
- [ ] 节点 OTA → 完整 6 状态机跑通 (HTTPS + SHA256)

### 4.2 v2.1 同步场景 (11 个)

- [ ] SYNC-INT-001: 首次上线
- [ ] SYNC-INT-002: 重启 (epoch 一致) → 不下发
- [ ] SYNC-INT-003: 重启 (epoch 落后) → 下发
- [ ] SYNC-INT-004: 恢复出厂 (NVS 空) → 强制下发
- [ ] SYNC-INT-005: PUT /channels/123 → 5s 内节点收到
- [ ] SYNC-INT-006: 中心端重启 → 启动 push
- [ ] SYNC-INT-007: 短暂断网 → StatusReport 触发 epoch check
- [ ] SYNC-INT-008: 周期 sync (10min)
- [ ] SYNC-INT-009: 批量改 templates fanout
- [ ] SYNC-INT-010: HTTPS OTA
- [ ] SYNC-INT-011: WS 状态实时推送

### 4.3 协议兼容 (v2.1/v2.2 互操作)

- [ ] **Node=v2.2 + Center=v2.1**: 节点发 node_id (id 数值 = 旧 device_id), 中心端按 v2.1 schema 解
- [ ] **Node=v2.1 + Center=v2.2**: 中心端识别 v2.1 字段, 映射到 v2.2 schema
- [ ] **Node=v2.2 + Center=v2.2**: 完整功能
- [ ] 降级日志: `[degraded_mode] node_id_fallback_to_device_id`

## 五、性能验收

| 指标 | 目标 |
|------|------|
| 单节点稳态 CPU (1 通道) | ≤ 5% |
| 单节点稳态 CPU (10 边缘设备) | ≤ 15% |
| 数据采集延迟 (P99) | ≤ 200ms |
| MQTT 消息吞吐 | ≥ 100 msg/s |
| **CRUD → 节点收到 (P99, v2.1)** | **≤ 5s** |
| **Server restart → 节点收到 (P99, v2.1)** | **≤ 15s** |

## 六、回归验收

- [ ] v2.1 R7/R8 验收项不退化
- [ ] 现有 P0 (T01-T05) 不退化
- [ ] v2.1 节点固件可继续工作 (v2.2 中心端兼容)
- [ ] v2.1 中心端可处理 v2.2 节点 (降级路径)

## 七、CI 校验

- [ ] `scripts/check_ws_event_names.sh` 通过 (前后端 union match)
  - v2.2 事件名: `node_status`, `node_config_synced`
  - v2.1 兼容: `node_status` (双发, 6 个月)
- [ ] 所有 `go test ./...` 通过
- [ ] 节点 `idf.py build` 通过
- [ ] 前端 `npm run typecheck` 通过
