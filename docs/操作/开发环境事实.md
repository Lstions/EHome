# 本仓开发环境事实（2026-09-13 固化）

> **用途**: 派发给执行者/子代理前先读本文件，避免重复踩环境坑。
> **来源**: 全部由主控或执行者**实测**得出，不是推测。

## 1. Go 的 PATH（最容易误报的一处）

Go **不在默认 PATH** 中。两个可用入口指向同一个工具链：

| 路径 | 说明 |
|---|---|
| `/snap/bin/go` | **标准入口**（snap 维护的 shim，推荐） |
| `/snap/go/11262/bin/go` | 版本化内部路径（随 snap 更新可能失效） |

两者实测均为 `go version go1.26.7 linux/amd64`。

**后果**：若命令里不带 PATH，后端门禁全部返回 `exit 127 / bash: go: command not found`，
执行者会**误报「后端门禁失败」** —— 本轮已实际发生过（一位执行者在报告里把它列为"环境事实"）。

**统一写法**：
```bash
export PATH=/snap/bin:/home/sun/.local/share/pnpm/bin:$PATH
export HOME=/home/sun
```

## 2. 后端构建必须带 -buildvcs=false

`go build ./...` 在无 .git 元数据的环境会报 VCS 错误。统一用：
```bash
go build -buildvcs=false ./...
```

## 3. 数据库

| 库名 | 用途 | 是否可破坏 |
|---|---|---|
| `ehome` | 开发库（真实数据：2 节点 / 3 边缘设备 / 87k 数据点 / 1003 逻辑设备 / 3757 通知） | **只读参考，禁止修改** |
| `ehome_test` | 集成测试库（CI 与 `-tags=integration` 用） | 禁止碰 |
| `ehome_sim_pg` | **PG 验证专用库**（`ehome_sim_*` 命名，派发任务时可安全使用） | 可自由读写 |
| `ehome_uiux` | UI/UX 审计专用（从开发库克隆） | **可随意破坏** |
| `ehome_sim_*` | 场景仿真专用（harness 自建自删） | 可破坏 |

连接参数见 `.env`；PG 在 `127.0.0.1:5432`、EMQX 在 `127.0.0.1:1883`，
由 docker compose 提供（`make infra` 确保它们在跑）。

## 4. 前端

```bash
cd frontend-shared
pnpm typecheck        # vue-tsc --noEmit（含 .vue 真检查）
pnpm test:run         # vitest
pnpm build            # typecheck + vite build
```

- `pnpm` 在 `/home/sun/.local/share/pnpm/bin`。
- **静态产物目录是 `frontend-shared/dist`**：任何浏览器实测前必须先 `pnpm build`，否则看到的是旧产物。
- 浏览器：系统 chromium `/snap/bin/chromium`（Playwright 1.62，包名 `@playwright/test`）。

## 5. 审计后端起停（多任务共用，勿抢端口）

```bash
# 编译（含最新源码）
cd backend && go build -buildvcs=false -o /tmp/ehome-uiux ./cmd/server

# 启动（必须由**受管后台任务**持有，不要用 nohup —— 它会随 shell 退出被回收）
EHOME_SERVER_ADDR=:8082 EHOME_DB_NAME=ehome_uiux EHOME_JWT_SECRET=uiux-audit-secret \
  EHOME_EXTERNAL_HOST=127.0.0.1:8082 GIN_MODE=debug EHOME_ENV=development \
  EHOME_STATIC_DIR=<repo>/frontend-shared/dist LOG_LEVEL=warn /tmp/ehome-uiux
```

登录：`admin` / `UiuxAudit2026!`（由 `ehomectl auth reset-password` 设定）。

## 5.1 并发跑测试会互相干扰（已实测两次，**不是缺陷**）

多个执行者同时跑 `go test ./...` 时，`internal/api` 会**间歇性 FAIL**，
但**单独重跑立刻全绿**（连续 3 次通过）。

**原因**：该包的测试共享进程级资源（SQLite 内存/临时库、端口、全局 registry），
并发执行时互相踩踏。

**判据（不要误报为缺陷）**：
1. 看失败是否**在同一包内跳变**、用例名与你的改动无关；
2. **单独重跑 2-3 次** —— 全绿即 flake；
3. 检查是否有其他 `go test` 进程在跑：`pgrep -af 'go test|api.test'`。

**纪律**：执行者在**验证阶段**应避免与他人并发跑全量；
若必须并发，请在报告里标注"该次结果可能受并发干扰"，并在收尾时单独复跑确认。

> 同类现象也出现在前端：多个任务并发 `pnpm build` 会让 dist 处于"别人改到一半"的状态
> （曾导致全站白屏 `Cannot access '$' before initialization`）。
> **正解：由主控统一构建一次并广播 entry hash**，而不是各自重建。

**需要新后端行为时**：自己另起一个端口（如 8099）连同一个库，用完杀掉自己的，
**不要重启 8082**（它是主控持有的共享实例）。

## 6. 已踩过的构建陷阱

| 陷阱 | 现象 | 检查方法 |
|---|---|---|
| **CSS 规则被静默丢弃** | Vite 的 CSS 压缩器把某些规则误判为 `@keyframes` 名并整条丢弃：源码里有、构建成功无报错、**产物里没有** | `node frontend-shared/tools/css-survival-check.mjs`（读产物确认） |
| **构建成功 ≠ 规则生效** | 语法错会让构建**显式失败**（好事）；语法对但不生效则会**静默通过** | 必须读浏览器实测的计算样式 |

## 7. 派发纪律（血泪总结）

1. **禁止执行者运行 git 命令**（提交由主控统一做）。
2. **同一文件绝不允许两个执行者并发修改** —— 本仓有过"互相覆盖"事故。派发前先做**写者矩阵**核对文件集合是否互斥。
3. **通知执行者：`dist` 是共享构建产物** —— 并发 build 会让 DOM 实测看到"别人改到一半"的状态。
4. **要求变异自证**：任何修复都要能回答"把修复回退，哪个测试会变红"。看不到红点的断言不守护任何不变量。
5. **"计数为 0"必须能回答分母** —— 一个恒为 0 的统计量既可能意味着"没问题"，也可能意味着"选择器什么都没扫到"（本轮在探针上犯过这两个方向的错）。
