# 数据采集与数据链路

> **状态**: 已实现（v2.5 数据事件总线 + v2.7 事件驱动采集 + v3.3 生命周期 + v3.4 时序化已落地 5/5）
> **版本**: v3.4
> **日期**: 2026-08-22
> **关联**: [总体设计.md](总体设计.md) | [边缘设备数据生命周期.md](边缘设备数据生命周期.md) | [架构优化实施计划.md](架构优化实施计划.md) | [../测试/性能压测与容量验证记录.md](../测试/性能压测与容量验证记录.md)

## 1. 功能概述

采集 → 上报 → 解析 → 入库 → 实时推送的全链路。包含：周期采集调度、DataEventBus 分片扇出、统一解析器、服务端降采样、数据源健康管理、逻辑设备身份。

## 2. 数据链路全景

```
节点 scheduler（interval_ms）
  → bus_worker（事件驱动 RX，v2.7）
  → DataReport(0x03) → MQTT up
中心端:
  → MQTT consumer（ingest）
  → DataEventBus（分片，heavy/light 消费者分离）
      ├─ SensorParserConsumer：统一解析器 pkg/parser（按 DeviceConfig 规则）
      │     └─ 告警求值回调（v3.4，alertCallback）
      ├─ DBPersistConsumer：unified_data 入库（血缘+归属双列）
      ├─ WSPushConsumer：data_update 事件推送
      ├─ RollupConsumer（v3.4）：1min rollup UPSERT（PG-only，独立邮箱 fail-open）
      ├─ TerminalConsumer / DataMetricsConsumer / PendingWriteConsumer
      └─（被动数据快速通道：终端/调试数据不经重路径）
```

## 3. unified_data 模型

```sql
CREATE TABLE unified_data (
  id BIGSERIAL,
  edge_device_id BIGINT,      -- 血缘（采集来源实例，永不改写）
  logical_device_id BIGINT,   -- 归属（数据身份，合并时 UPDATE）
  sensor_name VARCHAR(32), value DOUBLE PRECISION, unit VARCHAR(16),
  timestamp TIMESTAMPTZ, created_at TIMESTAMPTZ
);
-- 索引：device_id/timestamp + logical_device_id/timestamp（CONCURRENTLY）
-- v3.4: PG 月分区已落地（分区键 timestamp，主键 (id, timestamp)）；SQLite 不分区间构
```

**不变量**：

- 纯追加时序表，无 GORM 软删（Raw SQL 路径绕过 scope + retention 本就硬删）。
- 大表零 FK 被引用（分区改造安全）。
- rollup 保持 device 维度；logical scope 查询强制 raw（保形去重语义在 raw 上）。

## 4. 查询链

| 场景 | 路径 |
|------|------|
| 历史趋势（>48h） | rollup 1min（precision=auto 强制） |
| 历史趋势（≤48h） | raw + 服务端降采样（max_points LTTB 类 + gzip） |
| logical scope | 强制 raw + ROW_NUMBER 保形去重（MAX(id) 锚点） |
| 最新值 | latestValueCache（persist 旁路更新 + 启动回填 + miss 回落）（v3.4） |
| 实时数据 | WS data_update 订阅 |
| 导出 CSV | 服务端批查 + 流式导出 |

## 5. 数据源健康管理

- /data-sources/:id[activate|deactivate|health|reset]。
- 故障切换日志（failover-logs）供排障：采集连续失败 → 健康状态转移 → 通知。

## 6. retention 与清理（v3.4 已落地）

- 配置：logical_devices.retention_days（默认 90；创建时快照系统级配置，可用环境变量 EHOME_DATA_RETENTION_DAYS 覆盖）。
- v3.3：每日 purge 任务 DELETE 分批。
- **v3.4**：unified_data 到期改整分区 DROP（O(1)，已落地）；device_data 表维持 DELETE。
- raw 默认保留 90 天（系统级默认值，可经 EHOME_DATA_RETENTION_DAYS 覆盖；30 天仅推荐配置示例）。
- rollup 保留期是独立策略（文档记录为 365 天），与 raw 默认 / EHOME_DATA_RETENTION_DAYS 不联动。注意：全仓代码中仅有 RollupConsumer UPSERT 与建表，未见 rollup 自动清理路径（此前的 "DELETE" 表述与代码不符，待确认）。

## 7. 实现记录

| 关注点 | 位置 |
|--------|------|
| 总线与消费者 | backend/internal/databus/（bus.go、consumers_heavy.go 等） |
| 解析器 | backend/pkg/parser/（parser.go、rule.go、modbus.go、builtin.go） |
| API | handler_data.go（historical/batch/export/categories） |
| 降采样 | handler_data.go downsampleUnifiedData |
| 生命周期 | internal/datalifecycle/（retention/purge/partition_mgr v3.4） |
| 前端 | views/data/DataPanel.vue（趋势图 + 实时流 + 导出） |
| 固件 | components/scheduler/ + bus_worker/（采集源） |

## 8. 验收标准

- [x] 采集闭环（传感器 → 入库 → 前端实时/历史可见）
- [x] 高频场景零丢失（S1 84%→0%，见性能压测记录）
- [x] 降采样图表流畅（10^7 行历史查询达标）
- [x] 数据源健康转移 + failover 日志
- [x] retention 到期清理；合并后 logical 查询口径正确

## 9. 已知限制

- 解析器饱和吞吐 ~500 evt/s（单机容量上限；家庭规模富余，扩容方向在性能记录）。
- rollup 只覆盖 device 维度（logical 长跨度查询仍 raw——设计裁决，不引入第三口径）。
