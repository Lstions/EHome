# 通知中心

> **状态**: 已实现
> **版本**: v3.4
> **日期**: 2026-08-22
> **关联**: [总体设计.md](总体设计.md) | [功能需求总览.md](功能需求总览.md) | [../架构优化实施计划.md](架构优化实施计划.md)

## 1. 功能概述

站内通知统一呈现：合并任务结果、告警（v3.4 起）、数据源健康异常、自动化规则动作、保留期临期提醒。前端铃铛 + 未读计数 + 面板 + 全部已读；点击跳转目标页。

> **注意**：「配置同步失败、节点离线、OTA 结果」为早期设计意图，**当前并未实现**
> （源码无写入点、审计库无记录），详见 §4。

## 2. 数据模型

```sql
CREATE TABLE notifications (
  id, type, title, description, message, source, source_id,
  read, created_at, ...
);
```

- type 语义：info / warning / error / success（前端按 type 映射图标）。
- source 用于回链：如 source="alert_rule"、source_id=ruleID 跳转告警规则页。

## 3. 端点

| 端点 | 用途 |
|------|------|
| /notifications | 列表（分页 + 未读过滤） |
| /notifications/:id/read | 单条已读 |
| /notifications/read-all | 全部已读 |
| /notifications/unread-count | 未读数 |

## 4. 触发场景

> **本表按「源码写入点 + 审计库实测数据分布」双向核实，不是按设计意图罗列。**
> D-2 的教训：文档写的是意图，代码实现的是另一回事；未实现的场景如实标注，不得假装存在。
> 核实方法：`grep -rn "models.Notification{" backend --include=*.go | grep -v _test.go`
> 取全部写入点，再用审计库 `select source, count(*) from notifications group by source` 对照。

| 事件 | Source | 状态 | 通知 |
|------|--------|------|------|
| 配置下发失败 | — | **未实现** | 无写入点，源码与审计库均无对应记录 |
| 节点离线 | — | **未实现** | 无写入点，源码与审计库均无对应记录 |
| OTA 结果 | — | **未实现** | 无写入点，源码与审计库均无对应记录 |
| 合并任务结果 | `merge_failed` | 已实现 | warning/error（datalifecycle/migrate.go 搬迁受阻与重试耗尽两条） |
| 告警 firing/resolved（v3.4） | `alert_rule` | 已实现 | critical/warning（alert/evaluator.go） |
| 数据源健康异常 | `data_source` | 已实现 | warning/info（datasource/service.go newNotification） |
| 自动化规则动作 | `automation_rule` | 已实现 | info/warning（automation/planner.go，审计库中占比最高） |
| 保留期临期提醒 | `retention_expiring` | 已实现，审计期内未触发 | warning（datalifecycle/retention_task.go，到期前 30/7 天） |

## 5. WebSocket 推送

- 事件：`notification`（id, level, message, timestamp）。
- 前端 MainLayout 铃铛监听推送实时刷新未读数。

## 6. 容错设计

- 通知写入失败不影响主业务路径（旁路写入）。
- 批量合并：同源同内容的连续事件合并计数（防刷屏）。
- 自动清理（D-2 补齐）：由既有每日 retention 任务（`RetentionTask.RunOnce`）顺带执行，
  实现见 `backend/internal/datalifecycle/notification_cleanup.go`，**无新增后台协程**。
  - **已读**通知：超过保留期即删（默认 **90 天**，取 `SystemRetentionDays()`，
    与 `config.DefaultDataRetentionDays` 同源，不新增独立配置项）。
  - **未读**通知：保留期为已读的 **2 倍**（默认 **180 天**）后同样删除。
    只删已读修不掉 D-2——用户从不点开铃铛时未读只增不减，表仍会无限膨胀；
    未读代表用户尚可能想看，故给更长窗口而非等长。
  - 影响面：告警通知的已读态落在 Notification 行上（`handler_alert.go`），
    删掉 180 天前的未读告警通知只会让回链列表重新显示未读；告警本体
    仍完整保留在 `alert_events` 表中，不丢失告警历史。
  - 分批删除：与 purge/retention 同一范式，每批独立事务
    （PG 1 万行 / SQLite 1 千行），批间 sleep 200ms，避免长事务与 WAL 膨胀；
    清理幂等，失败仅 `slog.Warn`，不影响主业务与 retention 主流程。

## 7. 实现记录

| 关注点 | 位置 |
|--------|------|
| 后端 | internal/api/handler_notification.go |
| 前端 | views/notification/、MainLayout.vue 铃铛组件（:138-174） |
| 事件 | internal/events/（notification 事件常量） |
| 清理（D-2） | internal/datalifecycle/notification_cleanup.go（挂载于 retention_task.go RunOnce） |

## 8. 已知限制

- 无通知推送偏好设置（渠道选择）——家庭单主体场景未做。
- 告警通知的新 type 映射（warning/critical）复用既有图标体系，不扩展通知中心组件。
