# 通道 — 详细设计

> **版本**: v2.2 (2026-06-05 同步字段更新)
> **关联**: [00-术语表.md](../00-术语表.md) | [节点/详细设计.md](../节点/详细设计.md)

## 1. 功能概述

通道 (Channel) 是物理总线的逻辑抽象。每个 channel 属于一个节点 (Node)，对应一种硬件类型 (UART/I2C/SPI/GPIO/ADC)。

## 2. 数据模型

```sql
CREATE TABLE channels (
  id              SERIAL PRIMARY KEY,
  node_id         INTEGER REFERENCES nodes(id) ON DELETE CASCADE,
  hardware_type   VARCHAR(16) NOT NULL,    -- uart/i2c/spi/gpio/adc
  hardware_id     VARCHAR(16) NOT NULL,    -- 物理 ID (0,1,2 / GPIO25) — VARCHAR(16) string
  bus_type        VARCHAR(16) NOT NULL,    -- v2.2: 总线类型 (同 hardware_type, 显式声明)
  bus_config      JSONB,                   -- v2.2: 总线配置 (引脚/速率/模式)
  template_ids    INTEGER[] DEFAULT '{}',  -- v2.2: 关联的模板 ID 列表
  config          JSONB,                   -- 硬件配置 (兼容 v2.1)
  enabled         BOOLEAN DEFAULT true,
  interval_ms     INTEGER DEFAULT 1000,
  parser_id       VARCHAR(64),             -- 关联的驱动
  created_at      TIMESTAMPTZ DEFAULT NOW(),
  updated_at      TIMESTAMPTZ DEFAULT NOW()
);

CREATE INDEX idx_channels_node ON channels(node_id);
CREATE INDEX idx_channels_hw ON channels(hardware_type, hardware_id);
```

**v2.2 新增字段说明**:

| 字段 | 类型 | 含义 |
|------|------|------|
| `bus_type` | VARCHAR(16) | 总线类型 (显式声明, 与 hardware_type 对应, 用于协议下发) |
| `bus_config` | JSONB | 总线物理配置 (引脚/速率/模式), 下发到节点用于硬件初始化 |
| `template_ids` | INTEGER[] | 关联的模板 ID 列表 (v2.2 Template Pool), 多设备可共享模板 |
| `hardware_id` | VARCHAR(16) | 物理 ID, 字符串类型 (支持 "0"/"1"/"GPIO25"/"0x76" 等格式) |

### 3.1 UART
| 字段 | 默认值 |
|------|--------|
| baudrate | 115200 |
| data_bits | 8 |
| stop_bits | 1 |
| parity | none |
| flow_control | none |
| rx_buffer_size | 1024 |
| tx_buffer_size | 1024 |

### 3.2 I2C
| 字段 | 默认值 |
|------|--------|
| clock_hz | 400000 |
| sda_pin | 21 |
| scl_pin | 22 |
| pullup_enabled | true |

### 3.3 SPI
| 字段 | 默认值 |
|------|--------|
| clock_hz | 1000000 |
| mosi_pin | 23 |
| miso_pin | 19 |
| sclk_pin | 18 |
| cs_pin | 5 |
| mode | 0 |

### 3.4 GPIO
| 字段 | 默认值 |
|------|--------|
| pin | 25 |
| direction | input/output |
| pull | none/up/down |
| interrupt | none/rising/falling/both |

### 3.5 ADC
| 字段 | 默认值 |
|------|--------|
| channel | 0 |
| attenuation | 0dB/2.5dB/6dB/11dB |
| sample_time | 100ms |
| voltage_range | 0-3.3V |

## 4. 关键流程

### 4.1 创建通道

```
前端 → POST /api/v1/channels
  ↓
[server] 校验 hardware_type 必填
  ↓
[DB] INSERT INTO channels
  ↓
[server] 发 ConfigManifest 给 collector (含新 channel 配置)
  ↓
[ESP32] 写 NVS → 发 ConfigResult
```

### 4.2 I2C 扫描

```
前端 → POST /api/v1/channels/i2c/scan {collector_id, hardware_id}
  ↓
[server] 发 ScanRequest (0x0D) 到 mqtt.down
  ↓
[ESP32] 扫描 0x01-0x7F → 发 ScanReport (0x0C) 含设备地址列表
  ↓
[server] 收到 ScanReport → 推 frontend 事件
  ↓
前端 → 显示发现的设备列表
```

## 5. 容错设计

| 场景 | 行为 |
|------|------|
| Channel 重复 (collector + hardware_type + hardware_id) | 409 Conflict + 提示 |
| 改配置失败 (ESP32 拒收) | 配置 rollback + notification |
| 资源冲突 (两设备用同一 channel) | 创建时检测, 阻止 |
| 扫描超时 | 30s 未回 ScanReport → 失败 |

## 6. 可观测性

| 事件 | 字段 |
|------|------|
| `channel.created` | channel_id, collector_id, hardware_type |
| `channel.config.changed` | channel_id, old_config, new_config |
| `channel.i2c.scan.complete` | collector_id, addresses_found |
| `channel.config.rollback` | channel_id, reason |

| 指标 | 类型 | 标签 |
|------|------|------|
| `ehome_channel_total_count` | gauge | hardware_type |
| `ehome_i2c_scan_duration_seconds` | histogram | collector_id |
