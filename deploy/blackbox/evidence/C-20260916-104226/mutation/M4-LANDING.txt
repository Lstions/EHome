源文件 md5=47ddff3e9504686960545fe7f1d876c9
[M4] F8 判据由「200 且 落库值变了 且 响应==落库值」放宽为「只要 200 就算过」:
  - if [[ "$ST_RC" == "200" && -n "$AFTER" && "$AFTER" != "$BEFORE" && "$RESP_BC" == "$AFTER" ]]; then
  + if [[ "$ST_RC" == "200" ]]; then
  变异体落地自证: if [[ "$ST_RC" == "200" ]]; then